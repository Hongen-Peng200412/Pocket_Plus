# @package _global_
# ============================================================
# CPC1 阶段一消融: trunk_no_real_density
# 启动命令:
#   python /home/penghongen/My_Project/Pocket_Plus/src/train.py +experiment=CPC1/trunk_no_real_density
# ============================================================
# 相对 trunk_main 仅关闭真实原子 density cube 调制, 保留 P anchor density。
defaults:
  - /experiment/CPC1/trunk_main
  - override /model/sparse_refine/density_cube: pseudo
  - _self_

name: trunk_no_real_density
tag: "trunk_no_real_density"
