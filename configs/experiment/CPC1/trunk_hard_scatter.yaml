# @package _global_
# ============================================================
# CPC1 阶段一消融: trunk_hard_scatter
# 启动命令:
#   python /home/penghongen/My_Project/Pocket_Plus/src/train.py +experiment=CPC1/trunk_hard_scatter
# ============================================================
# 相对 trunk_main 将 online pdb feature 从 gauss27 改为 legacy hard scatter。
defaults:
  - /experiment/CPC1/trunk_main
  - _self_

name: trunk_hard_scatter
tag: "trunk_hard_scatter"

model:
  backbone:
    online_pdb_feature_scatter_kernel: "legacy"
    online_pdb_feature_add_occupancy: false
    online_pdb_feature_add_centroid: false
