# @package _global_
# ============================================================
# 不冻结任何模块
# 训练主流程默认使用此配置；阶段化训练时用 `frozen_module=stage1|stage2|stage3` 覆盖。
# ============================================================
frozen_module: null
