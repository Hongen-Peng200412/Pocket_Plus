# ============================================================
# 主配置入口点: dense_h200
# ============================================================
# 在 sparse_h100 基础上:
#   覆盖 model → dense, train → default_h200
# 对应历史最佳效果的配置
# ============================================================

defaults:
  - sparse_h100
  - override train: default_h200
  - override model: dense
  - _self_


name: dense_h200
tag: "密集"
project_name: PV_dense_fused
