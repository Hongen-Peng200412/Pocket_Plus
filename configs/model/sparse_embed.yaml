# ============================================================
# sparse_embed 模型配置
# ============================================================
# 仅定义 name; embed_head 通过根入口选择 embed_head config group 启用
# ============================================================

name: sparse_embed
