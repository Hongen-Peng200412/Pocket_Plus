# ============================================================
# dense 模型配置 (通过 config group 组合)
# ============================================================
# 此文件定义模型基本骨架
# 各子模块配置通过 Hydra config group 由根入口文件引用
# ============================================================

name: dense
