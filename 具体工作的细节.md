# 当前实现对齐版说明

本文件是对 `sparse_h100` 主训练链路做实现规格说明，但以当前仓库已经落地的代码为准

若本文件与下列代码冲突，一律以代码为准：

- `sbatch/sparse_h100.sbatch`
- `configs/sparse_h100.yaml`
- `configs/model/dense.yaml`
- `configs/model/sparse.yaml`
- `configs/model/zeros.yaml`
- `configs/dataset/fused.yaml`
- `configs/train/default_h100.yaml`
- `src/datasets/box_point_dataset.py`
- `src/datasets/box_point_collate.py`
- `src/model/stage1_voxel_backbone.py`
- `src/model/stage1_point_backbone.py`
- `src/model/stage1_model.py`
- `src/wrappers/voxel_point_stage1.py`
- `src/train.py`


# 当前主线

当前最新版主训练入口固定为：

1. `sbatch/sparse_h100.sbatch`
2. `python Pocket_Plus/src/train.py --config=sparse_h100`
3. `configs/sparse_h100.yaml`
4. `model: sparse`
5. `dataset: fused`
6. `train: default_h100`

其中：

- `sparse_h100` 是当前主线，不再把它视为“未来要接上的版本”。
- `dense` 是更密集的融合配置，用于对照或消融。
- `zeros` 是点分支/融合的调试后端，不是主训练配置。

`sbatch/sparse_h100.sbatch` 会在运行时继续覆盖：

- `train.nnodes=$SLURM_JOB_NUM_NODES`
- `train.devices=$GPUS_PER_NODE`

因此文档中所有 batch 相关描述都必须区分：

- 配置文件里的“每卡 batch size”
- 由 Slurm 决定的 world size
- 梯度累积后的有效 batch



# 当前配置图

`configs/sparse_h100.yaml` 的组合关系如下：

1. `model: sparse`
2. `dataset: fused`
3. `post_process: none`
4. `output: standard`
5. `train: default_h100`

`configs/model/sparse.yaml` 基于 `dense.yaml` 做覆盖，当前只保留：

- `voxel_backbone.return_feature_keys = ["voxel_c4", "voxel_final"]`
- `point_fusion_map = {point_enc4: voxel_c4, point_feat: voxel_final}`
- `point_fusion_modes = ["concat_linear", "concat_linear"]`
- `sampler_modes = ["trilinear", "trilinear"]`

`configs/model/dense.yaml` 才是四融合点版本：

- `point_enc2 <- voxel_ds_2`
- `point_enc3 <- voxel_ds_3`
- `point_enc4 <- voxel_c4`
- `point_feat <- voxel_final`

`configs/model/zeros.yaml` 关闭点分支主干与全部融合：

- `point_backbone.backend = zeros`
- `point_fusion_map` 全部置空
- `point_fusion_modes = []`
- `sampler_modes = []`


# 数据子系统

## 1. 数据集定位

当前主数据集是 `src/datasets/box_point_dataset.py` 中的 `BoxPointDataset`。

它负责一次性返回 stage1 训练所需的完整样本字典，包含：

- voxel 输入
- voxel 标签
- voxel 辅助监督掩码
- atom 坐标
- atom 特征
- atom 标签
- atom 监督掩码
- 样本级元信息



## 2. 当前 `fused` 配置

`configs/dataset/fused.yaml` 当前主配置为：

- `_target_: src.datasets.box_point_dataset.BoxPointDataset`
- `data_folder_names = ["emdb_BOX", "pdb_feature_BOX", "pdb_label_BOX"]`
- `class_folder_names = ["metal_ion", "peptide", "nucleic", "small_molecule"]`
- `class_mapping = [0, 1, 0, 0, 1]`
- `atom_buffer_radius = 0.0`
- `valid_crop_margin = 0`
- `enable_random_rotation = true`

`class_mapping = [0, 1, 0, 0, 1]` 的当前含义是：

- 原始标签 `0` 映射到 `0`
- 原始标签 `1` 映射到 `1`
- 原始标签 `2` 映射到 `0`
- 原始标签 `3` 映射到 `0`
- 原始标签 `4` 映射到 `1`

因此当前训练虽然读取的是多类 `pocket_class_ids`，但经过映射后是二分类训练。


## 3. 样本名解析

当前样本名格式固定为：

`{pdb_id}_{instance_id}_{rxx}_{ryy}_{rzz}[_C]`

`BoxPointDataset._parse_sample_name()` 当前会解析出：

- `pdb_id`
- `instance_id`
- `rxx`
- `ryy`
- `rzz`
- `is_center_box`

其中 `pdb_id` 会统一转为小写。


## 4. BOX 侧数据读取

`_load_box_npz_triplet()` 的当前行为固定为：

1. 遍历 `data_folder_names`
2. `label` 文件夹读成 `voxel_label`
3. 其它文件夹沿 channel 维拼接成 `voxel_grid`
4. 含 `emdb` 的通道会做整图 z-score
5. 从 npz 中读取或反推：
   - `box_origin_world`
   - `voxel_size_world`
   - `x_range`
   - `y_range`
   - `z_range`
6. 基于非 EMDB 通道是否非零，生成 `hardmask`

当前 `hardmask` 的语义是：

- 只看非 EMDB feature 通道
- 某体素只要有任一非 EMDB feature 非零，则 `hardmask=1`
- 若没有任何非 EMDB feature 通道，则返回全零 `hardmask`

注意：

- `x_range / y_range / z_range` 当前会在内部读取，但不会进入最终 batch 主契约。
- 文档中不要把它们写成 `__getitem__()` 的返回字段。


## 5. 原子侧数据读取与缓存

`_load_structure_npz_cached()` 当前从：

- `atoms.npz`
- `labels.npz`

中读取：

- `coords -> atom_coord_world`
- `features -> atom_feature_raw`
- `binding_mask`
- `pocket_class_ids`
- `instance_ids`

但当前真正进入主训练标签的是：

- `pocket_class_ids`

不是：

- `binding_mask`

`binding_mask` 当前只是随原始标签一起被读取，并未作为主损失标签使用。

当前实现还维护每个 DataLoader worker 内部的轻量 LRU cache：

- key 为 `pdb_id`
- value 为对应结构的原子与标签字典


## 6. 原子筛选规则

`_select_atoms_for_box()` 当前固定按几何规则筛选：

1. 先用当前 BOX 的 `[origin, max)` 定义 `core_mask`
2. 再用 `atom_buffer_radius` 做外扩，得到 `selected_mask`
3. 返回：
   - `selected_idx`
   - `atom_is_in_core_box`

在当前主配置下：

- `atom_buffer_radius = 0.0`

因此现行训练里：

- `selected_mask == core_mask`
- 不存在额外 buffer atom

也就是说，代码支持 buffer 原子，但当前 `sparse_h100` 主线并没有启用这部分上下文。


## 7. 原子标签规则

当前原子标签来自：

1. `labels.npz["pocket_class_ids"]`
2. 再经过 `_apply_class_mapping()`

因此当前 `atom_label` 是：

- `selected_idx` 对应原子的类别 ID
- 经过 `class_mapping` 后的最终训练标签

当前主线不要再写成：

- “原子主标签直接用 `binding_mask`”


## 8. 原子特征规则

当前 `atom_feat` 由 `_build_atom_features()` 生成，第一版保持为：

- 从 `atom_feature_raw[selected_idx]` 直接取出
- dtype 为 `float32`
- 当前主配置下特征维度是 `49`

以下内容当前都没有被拼进底层 `atom_feat`：

- `atom_is_in_core_box`
- `atom_valid_mask`
- 相对 BOX 中心的几何坐标

这些信息的当前处理方式分别是：

- `atom_is_in_core_box` 作为单独字段保留
- `atom_valid_mask` 作为单独字段保留，并在 atom token 阶段再拼接
- 几何坐标通过 `atom_coord_centered_world` 单独提供


## 9. 坐标系统

当前代码中需要严格区分三套坐标：

### 9.1 `atom_coord_world`

- 形状 `(N_selected, 3)`
- 顺序 `(x, y, z)`
- 世界坐标系

### 9.2 `atom_coord_local_voxel`

- 形状 `(N_selected, 3)`
- 顺序仍是 `(x, y, z)`
- 连续 voxel 坐标
- 语义是 corner 语义，不是 center-index 语义

定义为：

`(atom_coord_world - box_origin_world) / voxel_size_world`

若某原子恰好落在体素中心，则该坐标会带 `0.5` 偏移。

### 9.3 `atom_coord_centered_world`

- 形状 `(N_selected, 3)`
- 顺序 `(x, y, z)`
- 以 BOX 中心为原点的世界坐标

当前 point 分支与 voxel->point 采样主链路，使用的是：

- `atom_coord_centered_world`

当前 `stage1_model.py` 在采样前会从：

- `atom_coord_centered_world`
- `voxel_size_world`
- `box_shape_zyx`

重新恢复出局部 voxel 坐标，再送入 `grid_sample()`。

因此：

- `atom_coord_local_voxel` 目前属于数据契约、调试字段和未来扩展接口
- 当前主线采样时并没有直接读取 batch 里的 `atom_coord_local_voxel`


## 10. 监督掩码

### 10.1 `voxel_valid_mask`

由 `_build_voxel_valid_mask()` 生成。

规则是：

- 初始为全 `True`
- 在六个面各裁掉 `valid_crop_margin` 个 voxel

当前主配置：

- `valid_crop_margin = 0`

因此当前主线中：

- `voxel_valid_mask` 全为 `True`

### 10.2 `atom_valid_mask`

由 `_build_atom_valid_mask()` 生成。

当前规则是：

1. 原子必须在 `core box` 内
2. 原子连续 voxel 坐标必须落在裁边后的有效区域内

在当前主配置下：

- `atom_buffer_radius = 0.0`
- `valid_crop_margin = 0`

因此当前主线中：

- `atom_valid_mask == atom_is_in_core_box`
- 又由于当前没有 buffer atom，实际等价于“当前样本中所有被选中原子都参与监督”

必须注意：

- 这是“当前配置结果”
- 不是“代码能力上永远没有 buffer/crop”


## 11. 随机旋转增强

当前训练态支持对 voxel 与 atom 做完全同步的 90 度旋转增强。

同步旋转覆盖的字段包括：

- `voxel_grid`
- `voxel_label`
- `hardmask`
- `voxel_valid_mask`
- `atom_coord_local_voxel`
- `atom_coord_centered_world`
- `atom_coord_world`
- `box_shape_zyx`
- `atom_valid_mask`

当前实现还带有两个约束：

1. `voxel_size_world` 必须近似各向同性
2. 若是奇数次 90 度轴交换，旋转平面的两轴长度必须一致

当前主配置里：

- `enable_random_rotation = true`

仅在训练模式下启用。


## 12. `__getitem__()` 当前返回契约

当前样本字典返回：

- `voxel_grid`: `(C, D, H, W)`, `float32`
- `voxel_label`: `(D, H, W)`, `int64`
- `hardmask`: `(D, H, W)`, `int64`
- `voxel_valid_mask`: `(D, H, W)`, `bool`
- `box_origin_world`: `(3,)`, `float32`
- `voxel_size_world`: `(3,)`, `float32`
- `box_shape_zyx`: `(3,)`, `int64`
- `atom_coord_world`: `(N, 3)`, `float32`
- `atom_coord_local_voxel`: `(N, 3)`, `float32`
- `atom_coord_centered_world`: `(N, 3)`, `float32`
- `atom_feat`: `(N, 49)`, `float32`
- `atom_label`: `(N,)`, `int64`
- `atom_is_in_core_box`: `(N,)`, `bool`
- `atom_valid_mask`: `(N,)`, `bool`
- `sample_name`
- `pdb_id`
- `class_name`
- `instance_id`
- `is_center_box`


# Collate 子系统

当前 `src/datasets/box_point_collate.py` 中的 `box_point_collate()` 已经是主线实现。

其规则固定为：

1. voxel 定长字段用 `torch.stack`
2. atom 变长字段用 `torch.cat`
3. 非 tensor 元信息保留成 list

## 1. stack 的 voxel 字段

- `voxel_grid`
- `voxel_label`
- `hardmask`
- `voxel_valid_mask`
- `box_origin_world`
- `voxel_size_world`
- `box_shape_zyx`

## 2. concat 的 atom 字段

- `atom_coord_world`
- `atom_coord_local_voxel`
- `atom_coord_centered_world`
- `atom_feat`
- `atom_label`
- `atom_is_in_core_box`
- `atom_valid_mask`

## 3. 额外生成的索引字段

- `atom_counts`: `(B,)`
- `atom_offsets`: `(B,)`
- `atom_batch_index`: `(sumN,)`

其中 `atom_offsets` 当前明确采用：

- PTV3 风格的结束偏移 `end offsets`

即第 `i` 个样本对应区间是：

`[start_i, atom_offsets[i])`

## 4. 空 atom batch 行为

当前实现显式处理了 `total_atoms == 0` 的情况：

- 不会在 `torch.cat([])` 时报错
- 会返回 shape 正确的空 tensor


# 模型子系统

## 1. `Stage1VoxelBackbone`

当前实现位于 `src/model/stage1_voxel_backbone.py`，继承 `SimpleUnet`。

它当前负责：

1. 接收 `voxel_grid`
2. 通过 `set_input_channels()` 支持 lazy 初始化输入通道
3. 返回命名字典形式的 voxel 中间特征
4. 返回 `voxel_logits_aux`
5. 返回 `voxel_recycle_out`

当前稳定的命名 voxel 特征包括：

- `voxel_ds_0`
- `voxel_ds_1`
- `voxel_ds_2`
- `voxel_ds_3`
- `voxel_ds_4`
- `voxel_c4`
- `voxel_c3`
- `voxel_c2`
- `voxel_c1`
- `voxel_c0`
- `voxel_final`

当前 `sparse` 主线只显式请求：

- `voxel_c4`
- `voxel_final`


## 2. `Stage1PointBackbone`

当前实现位于 `src/model/stage1_point_backbone.py`。

当前支持的后端只有两种：

- `backend: ptv3`
- `backend: zeros`

当前主线默认使用：

- `backend: ptv3`

当前点分支的稳定命名变量包括：

- `point_input_feat`
- `point_embed`
- `point_enc0`
- `point_enc1`
- `point_enc2`
- `point_enc3`
- `point_enc4`
- `point_dec3`
- `point_dec2`
- `point_dec1`
- `point_dec0`
- `point_feat`

这些变量名不是草案，而是当前融合 hook 真正依赖的命名契约。

当前点分支输入为：

- `atom_feat`
- `atom_coord_centered_world`
- `atom_batch_index`
- `atom_offsets`
- 可选 `recycle_in`

当前点分支输出为：

- `point_feat`
- `point_state`
- `point_recycle_out`
- `point_feature_dict`


## 3. `VolumePointStage1Model`

当前实现位于 `src/model/stage1_model.py`，是 stage1 总装配模块。

当前职责固定为：

1. 调用 voxel 分支
2. 调用 point 分支
3. 在命名点变量处触发 voxel->point 融合
4. 构造 atom token
5. 经过 `SerializedAttention` stack 做 atom head
6. 处理 recycle 循环

### 3.1 当前支持的采样与融合方式

当前仅支持：

- `sampler_modes = {"trilinear", "nearest"}`
- `point_fusion_modes = {"concat_linear"}`

其它 sampler/fusion 策略仍未落地。

### 3.2 当前 sparse 主线融合点

当前 `sparse` 主线只在两个点变量上做融合：

1. `point_enc4 <- voxel_c4`
2. `point_feat <- voxel_final`

不要再把 `enc2/enc3` 写成 `sparse_h100` 默认行为。

### 3.3 当前 atom token 组成

当前 atom token 明确定义为：

1. `fused_point_feat`
2. `atom_coord_centered_world`
3. `atom_valid_mask` 转成 float 后的一维标记

即：

`atom_tokens = concat(fused_point_feat, atom_coord_centered_world, atom_valid_mask)`

这里拼接的是：

- `atom_valid_mask`

不是：

- `atom_is_in_core_box`

### 3.4 当前 recycle 规则

当前默认启用 recycle，配置来自 `dense.yaml`：

- `enable_recycling: true`
- `max_recycles: 3`
- `randomize_recycles: true`
- `detach_recycle_states: true`

当前行为是：

- 若关闭 recycle，则只跑 1 轮
- 若训练态且开启随机 recycle，则从 `1..max_recycles` 中均匀采样轮数
- 否则固定跑 `max_recycles` 轮


## 4. 当前前向主链路

当前 `sparse_h100` 真实前向链路固定为：

1. `BoxPointDataset.__getitem__()` 返回 dict sample
2. `box_point_collate()` 组成 dict batch
3. `Stage1VoxelBackbone` 输出：
   - `voxel_features`
   - `voxel_logits_aux`
   - `voxel_recycle_out`
4. `Stage1PointBackbone` 在命名变量生成时，通过 hook 触发：
   - voxel 特征采样
   - `concat_linear` 融合
5. `VolumePointStage1Model` 构造：
   - `atom_tokens`
   - `atom_hidden`
   - `atom_logits`
6. `VoxelPointStage1Wrapper` 计算：
   - atom 主损失
   - voxel aux 辅助损失
7. `train.py` 负责：
   - DataModule
   - DDP
   - logger
   - checkpoint
   - optimizer/scheduler 注入


# 训练子系统

## 1. `VoxelPointStage1Wrapper`

当前 wrapper 位于 `src/wrappers/voxel_point_stage1.py`。

它不是待新增模块，而是当前主线训练封装。

当前职责固定为：

1. 接收 dict batch
2. 调用 `VolumePointStage1Model`
3. 计算 atom 主损失
4. 计算 voxel 辅助损失
5. 聚合 `val/atom_pr_auc`
6. 配置 optimizer/scheduler

## 2. 当前损失定义

当前 `dense.yaml` 中配置为：

- `atom_loss = BinaryFocalLossWithAlpha`
- `voxel_aux_loss = BinaryFocalLossWithAlpha`
- `atom_loss_weight = 1.0`
- `voxel_aux_loss_weight = 0.2`

当前两个损失的 `scale` 分别为：

- atom loss: `1000.0`
- voxel aux loss: `200.0`

### 2.1 atom loss

当前调用方式是：

- `outputs["atom_logits"]`
- `batch["atom_label"]`
- `hardmask=batch["atom_valid_mask"]`
- `reduction="mean"`

因此当前 atom loss 的真实监督范围由：

- `atom_valid_mask`

决定。

### 2.2 voxel aux loss

当前调用方式是：

- `outputs["voxel_logits_aux"]`
- `batch["voxel_label"]`
- `hardmask = hardmask.bool() & voxel_valid_mask.bool()`
- `reduction="mean"`

因此当前 voxel aux loss 的真实监督范围由：

1. `hardmask`
2. `voxel_valid_mask`

共同决定。

在当前 `fused` 主配置下：

- `valid_crop_margin = 0`

所以 `voxel_valid_mask` 全 True，当前实际有效范围主要由 `hardmask` 决定。


## 3. 当前验证指标

当前 wrapper 真实记录的关键指标是：

- `train/loss`
- `train/atom_loss`
- `train/voxel_aux_loss`
- `train/recycle_passes`
- `val/loss`
- `val/atom_loss`
- `val/voxel_aux_loss`
- `val/recycle_passes`
- `val/atom_pr_auc`

当前没有落地：

- `val/atom_precision`
- `val/atom_recall`



## 4. `train.py` 当前状态

`src/train.py` 当前已经实现以下行为：

### 4.1 dict sample lazy init

`_extract_input_tensor()` 已支持：

- tensor sample
- `dict["voxel_grid"]`
- tuple/list 第 0 个 tensor

因此当前 lazy init 与 dict batch 已经对齐。

### 4.2 DataLoader 与 `collate_fn`

`UnifiedDataModule._get_dataloader()` 当前逻辑为：

1. 根据 `dataloader_backend` 自动判断 PyG 或 torch DataLoader
2. 若不是 PyG，则读取 dataset 对象上的 `collate_fn`
3. 直接将该 `collate_fn` 传给 DataLoader

当前没有实现：

- 从 cfg 中显式实例化 `collate_fn`

因此文档必须写成“当前只支持 dataset 属性提供 `collate_fn`”。

### 4.3 lazy module materialization

`_initialize_lazy_modules_before_ddp()` 当前会：

1. `datamodule.setup("fit")`
2. 取 `train_ds[0]`
3. 从 sample 中提取 `voxel_grid`
4. 调用 `model.backbone.set_input_channels(in_channels)`

### 4.4 分布式策略

当前当且仅当：

- `train.devices > 1`
- 或 `train.nnodes > 1`

时启用 DDPStrategy，并固定：

- `process_group_backend="nccl"`


## 5. `default_h100` 当前训练超参

`configs/train/default_h100.yaml` 当前主配置为：

- `batch_size: 3`
- `num_workers: 20`
- `dataloader_backend: auto`
- `pin_memory: true`
- `max_epochs: 100`
- `accelerator: auto`
- `ddp_find_unused_parameters: false`
- `precision: bf16-mixed`
- `gradient_clip_val: 0.5`
- `accumulate_grad_batches: 12`
- `check_val_every_n_epoch: 1`
- `log_every_n_steps: 3`
- `optimizer: AdamW(lr=2e-5, weight_decay=0.01)`
- `scheduler: ReduceLROnPlateau(mode=max, factor=0.5, patience=10, threshold=0.001, min_lr=1e-7)`

当前这组值描述的是：

- 每卡 batch size = 3
- 每步梯度累积 = 12

因此单卡时的等效 batch 为：

- `3 * 12 = 36`

多卡或多节点时，还要再乘 world size。


# 当前主线与未来扩展的边界

以下内容仍然不属于当前 `sparse_h100` 主线实现：

1. 伪原子
2. whole-PDB point branch
3. `concat_linear` 之外的融合策略
4. `trilinear / nearest` 之外的采样策略
5. wrapper 内部的 precision / recall 指标
6. cfg 侧显式实例化 `collate_fn`
7. 第二阶段 rescoring 网络

可以把这些内容保留在“未来工作”里，但不能写成当前实现已对齐。


# 维护规则

后续只要修改了以下任一部分，就必须同步更新本文件：

1. `configs/model/sparse.yaml` 或 `configs/model/dense.yaml` 的融合点
2. `configs/dataset/fused.yaml` 的 `class_mapping`、`atom_buffer_radius`、`valid_crop_margin`
3. `src/datasets/box_point_dataset.py` 的主标签来源或坐标语义
4. `src/model/stage1_model.py` 的 token 拼接方式、采样方式或 recycle 行为
5. `src/wrappers/voxel_point_stage1.py` 的损失与指标
6. `src/train.py` 的 dataloader 或 lazy init 契约
7. 本项目的其他部分


# 一句话总结

当前 `Pocket_Plus` 的最新版主训练实现，不是“准备开发的 stage1 草案”，而是已经落地的：

`BoxPointDataset + box_point_collate + Stage1VoxelBackbone + Stage1PointBackbone(PTV3) + 命名点变量驱动的 voxel->point 融合 + atom head + VoxelPointStage1Wrapper + train.py + sparse_h100.sbatch`

本文件的全部描述，都必须围绕这条已经存在的真实链路展开。
