# Stage1 模型与训练实现：AI Agent 速读说明

本文面向后续接手本仓库的 AI agent。它不是实验记录，也不是论文式描述；它的目标是让 agent 在改代码前快速知道：当前 Stage1 网络读什么、输出什么、监督什么、哪些接口不能随便改。

如果本文与代码冲突，以代码为准。优先核对：

- `src/train.py`
- `src/wrappers/voxel_point_stage1.py`
- `src/datasets/box_point_dataset.py`
- `src/datasets/box_point_collate.py`
- `src/model/stage1_model.py`
- `src/model/stage1_embed_head.py`
- `src/model/stage1_voxel_backbone.py`
- `src/model/stage1_point_backbone.py`
- `src/model/pseudo_atoms.py`
- `configs/model/*`
- `configs/dataset/*`
- `configs/loss/*`
- `configs/train/*`

## 1. 当前网络到底在做什么

Stage1 不是单纯的 3D U-Net，也不是单纯的点云网络。它是一个“体素密度图 + PDB 原子点云”的联合模型。

当前网络有三种监督/输出选项：一条在点云分支 + atom head 上，两条在体素分支上。

1. **受体原子是否结合 / atom-level receptor binding**
   - 分支：point branch + atom head。
   - 粒度：atom-level。
   - 输出：`atom_logits`。
   - 真值：`atom_label` 或 backbone 输出里的 `atom_target`。
   - 有效掩码：`atom_valid_mask`。
   - 含义：判断哪些受体原子属于结合口袋区域。

2. **配体的大致区域 / ligand rough region**
   - 分支：voxel branch。
   - 粒度：voxel-level。
   - 输出：`voxel_logits_ligand`。
   - 真值来源：`ligand_dist_map`，即体素到最近 ligand 原子的距离图。
   - 有效掩码：`voxel_valid_mask`。
   - 含义：不是精确配体分类，而是用距离图派生监督，让体素分支学习 ligand 附近的大致空间区域。

3. **体素化结合原子语义监督 / voxelized binding-atom semantic supervision**
   - 分支：voxel branch。
   - 粒度：voxel-level。
   - 输出：`voxel_logits_aux`。
   - 真值：`voxel_label`，来自上游 `pdb_label_BOX`，本质是把受体结合原子的语义标签投影到体素网格。
   - 有效掩码：`hardmask & voxel_valid_mask`。
   - 含义：监督体素分支识别哪些受体原子占据体素属于结合区域；它是 atom 标签的体素化语义辅助监督，不是 ligand 距离监督。

因此，当前训练 wrapper 能同时汇总三类 loss：

- `atom_loss`：受体原子是否结合的 atom-level 主监督。
- `voxel_ligand_loss`：配体大致区域的 voxel-level 监督。
- `voxel_aux_loss`：体素化结合原子语义监督。

是否启用某一项由配置决定；但模型和 wrapper 已经支持这三条输出/监督路径。

## 2. 总入口和职责边界

### 2.1 训练入口

训练链路是：

```text
sbatch / python src/train.py
  -> Hydra 实例化 DataModule + Wrapper
  -> VoxelPointStage1Wrapper
  -> VolumePointStage1Model.forward(batch)
```

`src/train.py` 负责运行时装配，不负责领域机制：

- Hydra 配置解析。
- DataModule / Dataset / DataLoader 构建。
- Lightning Trainer、logger、checkpoint 配置。
- DDP 前 lazy channel materialization。
- 可选 batch size tuning。

### 2.2 Wrapper

`src/wrappers/voxel_point_stage1.py` 中的 `VoxelPointStage1Wrapper` 负责训练语义：

- 调用 backbone。
- 计算 atom loss。
- 计算 voxel auxiliary loss。
- 计算 voxel ligand region loss。
- 记录训练/验证日志。
- 维护 PR-AUC 指标。
- 实例化 optimizer/scheduler。

不要把 backbone 内部机制写进 wrapper；wrapper 应只消费 backbone 输出契约。

### 2.3 Backbone

`src/model/stage1_model.py` 中的 `VolumePointStage1Model` 是真正的 Stage1 总装配：

```text
embed head（可选，循环外）
  -> voxel backbone
  -> point backbone（可在命名层位融合 voxel 特征）
  -> atom head
  -> recycle 多轮迭代（可选）
```

它负责：

- voxel/point 两条分支编排。
- voxel -> point 融合。
- atom head 判别。
- pseudo atom 生命周期。
- recycle 状态流。

## 3. 数据契约

### 3.1 Dataset 读什么

`src/datasets/box_point_dataset.py::BoxPointDataset` 同时读取：

1. BOX 级体素数据：
   - `emdb_exp_BOX`
   - `emdb_sim_BOX`
   - `pdb_label_BOX`
   - `ligand_dist_BOX`

2. 结构级 PDB 原子数据：
   - `{sample_root_path}/{pdb_id}/atoms.npz`
   - `{sample_root_path}/{pdb_id}/labels.npz`

这些数据的来源见：

- `Make_Data/notes_of_dataset.md`
- `processedPDB_EMDB_binder/notes_of_dataset.md`

### 3.2 单样本核心字段

`BoxPointDataset.__getitem__()` 返回一个 `dict[str, Any]`，不是单一 tensor。

Voxel 侧：

- `voxel_grid`: `(C, D, H, W)`，float32。模型输入体素特征，主要由实验/模拟密度通道构成。
- `voxel_label`: `(D, H, W)`，int64。体素口袋标签。
- `hardmask`: `(D, H, W)`，表示真实受体原子占位区域。
- `voxel_valid_mask`: `(D, H, W)`，体素监督有效区域，通常由边界裁剪得到。
- `ligand_dist_map`: `(D, H, W)`，可选。由多类别 ligand 距离图按配置归约成单通道，用于配体区域监督。

BOX 几何：

- `box_origin_world`: `(3,)`，BOX 左下近角点世界坐标，顺序 `(x, y, z)`。
- `voxel_size_world`: `(3,)`，体素世界尺寸，顺序 `(x, y, z)`。
- `box_shape_zyx`: `(3,)`，体素空间尺寸，顺序 `(Z, Y, X)`。

Atom 侧：

- `atom_coord_world`: `(N, 3)`，绝对世界坐标。
- `atom_coord_local_voxel`: `(N, 3)`，连续 voxel 坐标，字段顺序 `(x, y, z)`，数值是 corner 语义。
- `atom_coord_centered_world`: `(N, 3)`，以 BOX 中心为原点的世界坐标。
- `atom_feat`: `(N, F_atom)`，原子特征，通常来自 `atoms.npz/features`。
- `atom_label`: `(N,)`，atom 监督标签。
- `atom_is_in_core_box`: `(N,)`，该原子是否在当前 BOX 内部。
- `atom_valid_mask`: `(N,)`，该原子是否参与 atom loss / metric。

元信息：

- `sample_name`
- `pdb_id`
- `class_name`
- `instance_id`
- `is_center_box`

### 3.3 Collate 之后的 batch

`src/datasets/box_point_collate.py::box_point_collate()` 负责把 `list[dict]` 变成模型可消费的 batch：

- voxel 字段固定大小，走 `torch.stack`，得到 `(B, ...)`。
- atom 字段长度可变，走 `torch.cat`，得到 `(sumN, ...)`。
- 元信息保留为 Python list。

额外生成：

- `atom_counts`: `(B,)`，每个样本 atom 数。
- `atom_offsets`: `(B,)`，PTV3 风格累计结束偏移。
- `atom_batch_index`: `(sumN,)`，每个 atom 属于哪个 batch 样本。

这三个字段是 point backbone、pseudo atom 注入/移除、real-only/mixed 视图切换的基础契约，不能随便改语义。

## 4. 坐标与轴顺序，不要混淆

仓库里同时存在三套坐标语义：

1. 世界坐标：`(x, y, z)`
   - 典型字段：`atom_coord_world`、`box_origin_world`、`voxel_size_world`。

2. 体素数组轴：`(Z, Y, X)`，也就是 tensor 空间轴 `(D, H, W)`
   - 典型字段：`voxel_grid`、`voxel_label`、`hardmask`。

3. 连续 voxel 坐标：字段顺序仍是 `(x, y, z)`，但数值表达相对 BOX origin 的 voxel corner 坐标
   - 典型字段：`atom_coord_local_voxel`。
   - 若原子在某个 voxel 中心，数值是 `(ix+0.5, iy+0.5, iz+0.5)`。
   - 送入 `grid_sample(align_corners=True)` 前，需要先减 `0.5` 转成 center-index 语义，再归一化到 `[-1, 1]`。

模型中 `_voxel_xyz_to_grid_sample_xyz()` 和 `_centered_world_xyz_to_local_voxel_xyz()` 封装了这套转换。改融合采样时优先复用这些函数。

## 5. 数据筛选与监督掩码

### 5.1 进入样本的 atom 和参与监督的 atom 是两回事

Dataset 会按 BOX 范围筛出：

- core atom：位于当前 BOX 内。
- buffer atom：位于 BOX 外扩 buffer 范围内。

进入样本的 atom 可以包含 buffer atom；但参与 atom loss 的 atom 还需要满足 `atom_valid_mask=True`。

### 5.2 `hardmask`

`hardmask` 是由真实受体原子几何投影得到的体素占位掩码，不是随便从某个输入通道 threshold 出来的。

它用于 voxel auxiliary supervision，与 `voxel_valid_mask` 合并后形成有效监督区域。

### 5.3 `voxel_valid_mask`

`voxel_valid_mask` 通常通过 `valid_crop_margin` 从 BOX 六个边界向内裁掉一圈，用于避免边界附近监督不稳定。

### 5.4 `ligand_dist_map`

`ligand_dist_map` 来自 `ligand_dist_BOX`，原始数据是按 ligand class 分通道的距离图。Dataset 会根据 `class_mapping` 选择/归约通道，得到单通道距离图。

它服务于 `voxel_ligand_loss`，用来监督配体附近的大致体素区域。

## 6. 数据增强

训练态可启用同步 90 度旋转增强。旋转必须同时作用于：

- `voxel_grid`
- `voxel_label`
- `hardmask`
- `voxel_valid_mask`
- `ligand_dist_map`
- `atom_coord_local_voxel`
- `atom_coord_centered_world`
- `atom_coord_world`
- `atom_valid_mask` 需要在旋转后重算

只支持近似各向同性体素和立方体 BOX。不要只旋转 voxel 不旋转 atom，否则 voxel->point 融合会系统错位。

## 7. 主模型前向流程

`VolumePointStage1Model.forward(batch)` 的高层流程：

```text
1. 决定 recycle 轮数
2. 可选：在 embed 阶段注入 pseudo atoms
3. embed head 在 recycle 外执行一次，并可能裁剪 atom
4. 回到 real-only canonical batch
5. 组装 voxel_input = voxel_grid + embed voxel grid 或 online raw atom scatter
6. 对每个 recycle pass：
   6.1 voxel backbone 前向，输出 voxel features / aux logits / ligand logits / recycle state
   6.2 可选：在 point 阶段注入 pseudo atoms
   6.3 point backbone 前向，并通过 hook 做 voxel->point 融合
   6.4 根据生命周期决定 atom head 用 mixed 视图还是 real-only 视图
   6.5 更新 voxel 和 point recycle state
7. 最后一轮后执行 atom head
8. 返回输出字典
```

注意：embed head 不参与 recycle；它始终在循环外只执行一次。

## 8. Backbone 输出契约

`VolumePointStage1Model.forward()` 返回的关键字段：

- `atom_logits`: `(sumN_head, C)`，atom-level 结合原子预测 logits。若 `enable_atom_head=False`，为 `None`。
- `atom_hidden`: atom head attention stack 后的隐藏特征。
- `atom_tokens`: atom head 输入 token。
- `fused_point_feat`: 最后一轮进入 atom head 的点特征。
- `atom_target`: atom 监督标签，通常来自当前 head batch。
- `atom_valid_mask`: atom 监督有效掩码。
- `voxel_logits_aux`: `(B, 1, D, H, W)` 或兼容形状，体素口袋辅助 logits。
- `voxel_logits_ligand`: `(B, 1, D, H, W)` 或 `None`，配体大致区域 logits。
- `voxel_outputs`: voxel backbone 最后一轮输出字典。
- `point_outputs`: real-only 点分支输出字典。
- `embed_output`: embed head 输出；未启用时为 `None`。
- `sampled_point_fusion_feat_dict`: 每个融合点采样到的 voxel 特征，仅供 debug/记录。
- `voxel_recycle_out`: 最后一轮 voxel recycle 输出。
- `point_recycle_out`: 最后一轮 point recycle 输出。
- `recycle_passes_used`: 实际执行的 recycle 轮数。

## 9. Embed head

文件：`src/model/stage1_embed_head.py`

Embed head 是 recycle 外的前置模块。它有两个身份：

1. 原子编码器。
2. 原子过滤器。

输入主要是：

- `atom_feat`
- `atom_coord_centered_world`
- `atom_batch_index`
- `atom_offsets`
- `atom_coord_local_voxel`
- `box_shape_zyx`
- `voxel_size_world`
- `atom_is_in_core_box`

输出主要是：

- `voxel_pdb_embed_grid`：scatter 到体素网格的 PDB 原子嵌入，可拼接到 voxel input。
- `embed_point_feat`：给 point backbone 的原子嵌入。
- 裁剪后的 atom 字段。
- `global_keep_mask`：从输入 atom 到最终保留 atom 的全局掩码。

关键机制：渐进 buffer 裁剪。

- trunk、voxel path、point path 都可以在每个 block 后按 buffer radius 裁剪 atom。
- 裁剪会改变下游 point branch 和 atom supervision 所面对的 atom 集合。
- 如果改裁剪逻辑，必须同步维护 `global_keep_mask`、`atom_offsets`、`atom_batch_index` 以及监督字段裁剪。

## 10. Voxel backbone

文件：`src/model/stage1_voxel_backbone.py`

`Stage1VoxelBackbone` 继承 RAUNet 风格的 `SimpleUnet`。

输入：

- `voxel_grid`: `(B, C, D, H, W)`。
- `recycle_in`: `(B, C_recycle, D, H, W)` 或 `None`。
- `return_feature_keys`: 需要导出的命名体素特征。

输出：

- `voxel_features`: 命名体素特征字典。
- `voxel_logits_aux`: 体素口袋辅助 logits。
- `voxel_logits_ligand`: 配体大致区域 logits，可为 `None`。
- `voxel_recycle_out`: 下一轮 voxel recycle state。

命名体素特征包括但不限于：

- `voxel_ds_0` 到 `voxel_ds_4`
- `voxel_c4` 到 `voxel_c0`
- `voxel_final`

这些名字会被 fusion 配置引用；改名会影响 `point_fusion_map`。

## 11. Point backbone

文件：`src/model/stage1_point_backbone.py`

`Stage1PointBackbone` 当前支持：

- `backend="ptv3"`
- `backend="zeros"`，用于消融/占位

输入：

- `atom_feat`
- `atom_coord_centered_world`
- `atom_batch_index`
- `atom_offsets`
- `recycle_in`
- `point_feature_hook`
- `return_feature_names`

输出：

- `point_feat`
- `point_state`
- `point_recycle_out`
- `point_feature_dict`

点分支 recycle 逻辑：

- 上一轮 `point_recycle_out` 先经过可选 LayerNorm。
- 再通过 `recycle_input_proj` 投影回 atom feature 维度。
- 与当前 `atom_feat` 相加。
- 再进入 `atom_input_proj` 和 PTV3。

点分支融合逻辑：

- point backbone 在命名点变量产生后调用 `point_feature_hook(feature_name, point_like)`。
- 主模型在 hook 内按 `point_fusion_map` 判断是否做 voxel->point 融合。
- 因此融合发生在 PTV3 内部命名层位，而不是点分支结束后统一发生。

## 12. Voxel -> Point 融合

主实现位于 `src/model/stage1_model.py`。

融合配置控制三件事：

1. 哪个 point feature name 需要融合。
2. 它读取哪个 voxel feature name。
3. 使用什么 sampler 和 fusion mode。

当前 sampler：

- `trilinear`
- `nearest`

当前 fusion mode：

- `concat_linear`

融合流程：

```text
point centered-world coord
  -> 转 local voxel coord
  -> 转 grid_sample normalized coord
  -> 在指定 voxel feature map 上采样
  -> 与 point_like.feat concat
  -> Linear/LayerNorm/Act/Dropout/Linear 投影回 point channel
```

目前是单向 `voxel -> point`，没有 `point -> voxel` 回写。

## 13. Atom head

Atom head 位于 `src/model/stage1_model.py`。

若 `enable_atom_head=True`，流程是：

1. 构造 `atom_tokens`：
   - 如果 `atom_head_append_coord_mask=True`：`[point_feat, atom_coord_centered_world, atom_valid_mask]`
   - 否则只使用 `point_feat`
2. `atom_token_proj` 投影到 hidden dim。
3. `Stage1SerializedAttentionStack` 复用 point branch 的 `point_state` 做 serialized attention。
4. `atom_logit_head` 输出 `atom_logits`。

Atom head 是当前结合原子预测的主输出头。

## 14. Pseudo atom 机制

文件：`src/model/pseudo_atoms.py`

Pseudo atom 是额外消息节点，不是监督目标。

核心性质：

- `pseudo_valid_mask=False`。
- `pseudo_label=0`。
- 不参与 loss。
- 可以参与 embed head、point backbone、atom head 的消息传递，具体由 `lifecycle=[embed_head, point_backbone, atom_head]` 控制。

生成逻辑：

- 每个 BOX 生成 `base_count + scale_factor * n_real_atoms` 个候选。
- 可按密度通道加权采样。
- 可删除离真实原子太近、离真实原子太远、伪原子之间太密的点。
- 特征初始化支持 `zero` 和 `neighbor_mean`。

注入布局：

```text
[real_0, pseudo_0, real_1, pseudo_1, ...]
```

`split_info = [(n_real_i, n_pseudo_i), ...]` 是 mixed/real-only 视图切换的关键结构。

生命周期约束：

- 如果 `lifecycle[2]` 即 atom head 启用伪原子，那么 `lifecycle[1]` point backbone 也必须启用。
- 当前不支持“只在 atom head 启用伪原子”。

Recycle policy：

- `non`：每轮重新采样位置和特征。
- `pos`：保留位置，重新初始化特征。
- `all`：保留位置和 point recycle state，但特征会重新初始化。
- `fixed`：保留位置、特征和 point recycle state。

## 15. Recycle 机制

Recycle 位于 `VolumePointStage1Model.forward()`。

轮数语义：

- `enable_recycling=False`：固定 1 轮。
- 训练态且 `randomize_recycles=True`：在 `[1, max_recycles]` 随机采样轮数。
- 否则使用 `max_recycles`。

跨轮状态：

- voxel 分支：`voxel_recycle_out -> voxel_recycle_in`。
- point 分支：real-only 的 `point_recycle_out -> point_recycle_in`。
- pseudo atom 的 point recycle state 单独存储，不混进 real-only state。

`detach_recycle_states=True` 时，跨轮 state 会 detach，截断跨轮梯度。

重要边界：

- embed head 不参与 recycle。
- pseudo atom 是否跨轮保留位置/特征/state 由 `pseudo_atom` 的 recycle policy 决定，不由 recycle 本身决定。

## 16. Loss 与 metric

Wrapper 当前支持三条 loss：

### 16.1 Atom loss

- 预测：`outputs["atom_logits"]`
- 真值：`outputs.get("atom_target", batch["atom_label"])`
- 掩码：`outputs.get("atom_valid_mask", batch.get("atom_valid_mask"))`
- 主要指标：`val/atom_pr_auc`

### 16.2 Voxel auxiliary loss

- 预测：`outputs["voxel_logits_aux"]`
- 真值：`batch["voxel_label"]`
- hardmask：`batch["hardmask"]`
- valid mask：`batch["voxel_valid_mask"]`
- metric 掩码：`hardmask & voxel_valid_mask`
- 指标：`val/voxel_aux_pr_auc`

### 16.3 Voxel ligand region loss

- 预测：`outputs["voxel_logits_ligand"]`
- 距离图：`batch["ligand_dist_map"]`
- valid mask：`batch["voxel_valid_mask"]`
- 不使用 `hardmask`
- 指标：`val/voxel_ligand_pr_auc`

如果某个 loss 配置为 `None`，对应训练项和验证指标会跳过。

## 17. 配置控制面

只把配置当控制面，不要把当前某个 YAML 值写成机制定义。

- `configs/model/voxel_backbone/*`
  - 体素主干、导出层位、aux head、ligand head、voxel recycle。

- `configs/model/point_backbone/*`
  - PTV3/zeros backend、点特征维度、PTV3 编码器/解码器、point recycle。

- `configs/model/fusion/*`
  - `point_fusion_map`、sampler、fusion mode。

- `configs/model/embed_head/*`
  - embed trunk、voxel path、point path、渐进裁剪、scatter/soft splatting、occupancy/centroid encoding。

- `configs/model/pseudo_atom/*`
  - pseudo atom 数量、采样、删除、初始化、生命周期、recycle policy。

- `configs/model/atom_head/*`
  - atom token、serialized attention、CPE、FFN、logit head。

- `configs/dataset/*`
  - 数据路径、类别目录、split JSON、class mapping、buffer 半径、valid crop、密度通道、旋转增强。

- `configs/loss/*`
  - atom loss、voxel aux loss、voxel ligand loss 及权重。

- `configs/train/*`
  - batch、DDP、optimizer、scheduler、logger、checkpoint、fault tolerance。

## 18. 改代码前的检查清单

如果你准备改 Stage1，先判断会不会影响这些契约：

1. 是否改变 `BoxPointDataset` 返回字段？
2. 是否改变 `box_point_collate()` 中 atom 展平方式或 `atom_offsets` 语义？
3. 是否改变坐标语义，尤其是 `(x,y,z)` 与 `(Z,Y,X)` 的转换？
4. 是否改变 `atom_valid_mask`、`voxel_valid_mask`、`hardmask` 的含义？
5. 是否改变 `ligand_dist_map` 的通道归约或 loss 使用方式？
6. 是否改变 `labels.npz` / `atoms.npz` 依赖字段？若是，还要更新数据说明文档。
7. 是否新增或改名 point/voxel feature name？若是，同步 fusion 配置。
8. 是否影响 pseudo atom 的 mixed/real-only 视图？若是，重点检查 `split_info`。
9. 是否影响 recycle state 的 shape 或 detach 语义？
10. 是否影响 wrapper 的 loss / metric 输出键？若是，同步 checkpoint monitor 和日志配置。

## 19. 维护规则

- 只换实验配置但不改接口/机制时，本文通常不用改。
- 改了字段、shape、坐标、mask、输出键、loss 路径、pseudo atom 生命周期、fusion 层位或 recycle 状态流时，必须更新本文。
- 不要把“当前正在训练的配置组合”写成实现事实。
- 不要在本文复制大段 YAML；这里只写模块契约和机制边界。
- 数据集路径和落盘字段不要写在本文，改看：
  - `Make_Data/notes_of_dataset.md`
  - `processedPDB_EMDB_binder/notes_of_dataset.md`
