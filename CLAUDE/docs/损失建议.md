


也就是：

refined positive score 应该比 refined negative score 高至少 m
rank
	​

	​

2.1 它和 BCE / focal 的区别

普通 BCE 是逐点监督：

ℓ
i
	​

=−y
i
	​

logp
i
	​

−(1−y
i
	​

)log(1−p
i
	​

)

它关心每个点自身是否分类正确。

pairwise ranking 关心的是相对顺序：

r
positive
	​

>r
negative
	​


这更接近 AP / PR-AUC 的思想。因为 PR-AUC 本质上很依赖所有正负样本的 score 排序：正样本排得越靠前，AP 越高。

所以 pairwise ranking 的作用可以概括为：

优化正负候选之间的相对排序，而不仅是逐点分类
	​

2.2 “加入相对 base 的 improvement margin”怎么写？

截图中写的是：

并可加入相对 base 的 improvement margin。

这可以形式化成两种版本。

版本 A：refine 的正负间隔至少不小于 base

base 的正负 margin 是：

d
ij
base
	​

=b
i
	​

−b
j
	​


refine 的正负 margin 是：

d
ij
ref
	​

=r
i
	​

−r
j
	​


希望：

d
ij
ref
	​

≥d
ij
base
	​

+m
imp
	​


对应损失：

L
rank_imp
	​

=
∣Q
C
	​

∣
1
	​

(i,j)∈Q
C
	​

∑
	​

max(0,m
imp
	​

+d
ij
base
	​

−d
ij
ref
	​

)
	​


展开为：

L
rank_imp
	​

=
∣Q
C
	​

∣
1
	​

(i,j)∈Q
C
	​

∑
	​

max(0,m
imp
	​

+(b
i
	​

−b
j
	​

)−(r
i
	​

−r
j
	​

))

这个更严格。它要求 refine 不只是排对正负，而且要比 base 排得更开。

版本 B：只要求 refine 绝对排序正确

也就是截图中的版本：

L
rank
	​

=
∣Q
C
	​

∣
1
	​

(i,j)∈Q
C
	​

∑
	​

max(0,m
rank
	​

−r
i
	​

+r
j
	​

)

这个更稳，更常用。

对你的当前项目，我建议先用版本 B；如果 validation AP / PR-AUC 仍不理想，再尝试版本 A。

2.3 pair 怎么采样？

不能对所有：

∣P
C
	​

∣×∣N
C
	​

∣

都算，太多，也会被 easy negatives 淹没。

建议采样 hard pairs：

N
C
hard
	​

={j∈N
C
	​

∣p
j
base
	​

>τ
neg
	​

}

或者：

N
C
hard
	​

=TopK
j∈N
C
	​

	​

(p
j
base
	​

)

即选 base 最容易误判成 ligand 的负样本。

正样本也可以选：

P
C
hard
	​

={i∈P
C
	​

∣p
i
base
	​

<τ
pos
	​

}

或者选所有正样本。

更稳的 pair 集合：

Q
C
	​

=P
C
	​

×TopK
j∈N
C
	​

	​

(p
j
base
	​

)

它的含义是：

让所有正样本的 refined score 高于最危险的一批负样本。