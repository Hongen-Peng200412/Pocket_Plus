# Find_1 不同受体条件推理与评估总日志

本文汇总同一 `Find_1` PDB-centric-v2 checkpoint 在不同受体来源下的 Stage1 校准与测试。每种受体来源仍由独立实验日志保存完整参数、正式命令、产物和运行历史；本文件只维护当前状态和可直接比较的最终结论。

## 实验索引

| 受体来源 | 实验日志 | 当前状态 |
| --- | --- | --- |
| 真实受体 | `文档/exec_plan/2026-09-12_Find_1真实受体推理与评估.md` | calibration、`test_0`、派生 `test_1` 与 scored-centered 产物已完成 |
| CryoAtom2 最终预测受体 | `文档/exec_plan/2026-09-15_Find_1_CryoAtom2受体推理与评估.md` | calibration、179-PDB `test_0`、149-PDB 派生 `test_1` 与 calibration scored-centered 均已完成并通过最终门控 |

## 当前状态快照

2026-09-15：AdaLigand 与 Pocket Plus 已分别完成实现历史和学习历史整理，两个实现端点均与学习端点树对象完全相同。Job `368455` 第 12 次执行完成 CryoAtom2 `6bgi` 的适配与 Stage1 probability 集成门控；第 13 次执行因入口继承到缺少 `gemmi` 的 Pocket Plus Python，在写入任何正式资产前结束。环境窄修复进入 AdaLigand `Learn/CUMULATIVE` 后，第 14 次执行完成 calibration 100 项与 `test_0` 179 项正式适配，第 15 次执行完成全量只读门控，第 16 次执行已经启动 Stage1 calibration 与测试。H100×2、64 CPU 继续由该任务使用，`after_lock_368455` 始终存在。

16:05 状态快照：calibration probability 34/100；两个 GPU 分片均保持约 73.9 GiB 显存和 99%–100% 利用率，正式执行稳定。

17:36 状态快照：calibration probability 83/100；双 GPU 均为 100% 利用率，执行继续稳定。

18:37 状态快照：calibration probability、F1 blobs 与 F2 blobs 已全部完成，F2 centered 为 75/100；F1 basic 参数已经冻结，剩余 centered 分片继续运行。

19:38 状态快照：calibration 全链和 Gaussian score-only 回填均为 100/100，F1 basic 与 F2 Gaussian 参数均已冻结；`test_0` probability 为 30/179。

20:42 状态快照：`test_0` probability 69/179；双 H100 均为 100% 利用率，正式执行继续稳定。

22:12 状态快照：`test_0` probability 122/179；双 H100 均为 100% 利用率，正式执行继续稳定。

23:43 状态快照：`test_0` probability 159/179；一个分片完成，另一个分片以 100% GPU 利用率处理剩余 20 个较大样本。

2026-09-16 00:00 状态快照：`test_0` probability 165/179；剩余分片继续以 100% GPU 利用率运行，日期续接未影响 Job。

01:01 状态快照：`test_0` probability 与 F1 blobs 已全部完成，F1 basic evaluate 为 52/179；F2 测试链按顺序等待。

02:02 关键事件：第 16 次正式执行成功结束。`test_0` 两套评估均为 179/179，相关全局 JSON/JSONL 已生成；第 17 次执行已启动 149-PDB `test_1` 保序派生。

02:17 关键事件：第 17 次 `test_1` 保序派生与第 18 次最终只读门控均已成功。CryoAtom2 受体实验全部完成，Job `368455` 停回 `try_lock`，`after_lock` 保留。

## 主要结果

以下是两种受体条件在 `test_1` 上的语义 F1。完整实例指标、PRAUC 与 top-K 由各实验日志和本地收口文档分别保存。

| 受体来源 | 流水线 | semantic micro F1 | semantic macro F1 |
| --- | --- | ---: | ---: |
| 真实受体 | F1 blobs+basic | 0.593502 | 0.578422 |
| 真实受体 | F2 blobs+Gaussian | 0.588878 | 0.575036 |
| CryoAtom2 最终预测受体 | F1 blobs+basic | 0.540695 | 0.490388 |
| CryoAtom2 最终预测受体 | F2 blobs+Gaussian | 0.526640 | 0.480093 |

该表只描述两套分别在自身 calibration 上选参的完整流水线结果，不能把差异归因于单一受体结构因素。

## 2026-09-17 F2–F1–basic 消融状态

第三条方法固定使用各受体条件自身的 F2 语义阈值与 F2 centered 候选轴，候选分数仅为 `source_probability_mean`。它不执行 GPU 前向，不改变已有 Gaussian `score/selected`，也不在测试集重新选参。

只读门控已证明两种受体条件的 calibration 与 `test_0` 中，所有 `voxel_count >= 8` 的 F2 blob 均与 centered 候选轴逐项一致。真实受体对应 2,792/2,251 个 calibration/test 候选，CryoAtom2 对应 2,678/2,407 个候选。

首次提交的 Job `384991` 与 `384992` 各请求 64 CPU，因单节点同时空闲 64 核的门槛导致排队；用户要求改用 8 CPU 后，两项均在获得节点前取消，运行时间为零，没有生成 release、launch、预映像或正式产物。

替代 Job `385001` 与 `385002` 已分别以 8 CPU 提交，当前均为 `PENDING (Priority)`。两项均使用 `cpu` partition、QOS=`Cpu96`，且 `pre_hold=0`、`after_hold=0`；一次任务配置使用 8 个 calibration 线程，并保持全部科学参数不变。正式命令分别为：

```bash
exec bash "${TASK_PROJECT_ROOT}/tmp/find1_f2_basic_ablation_20260917/run_f2_basic.sh" real
```

```bash
exec bash "${TASK_PROJECT_ROOT}/tmp/find1_f2_basic_ablation_20260917/run_f2_basic.sh" cryoatom2
```

结果完成前，现有两条流水线的指标与收口文档保持不变。

2026-09-17 23:27 关键事件：替代 Job `385001/385002` 分别在 cnode05/cnode01 同时启动，均为 8 CPU、无 GPU，并复用 release `/home/penghongen/Feedback/Pocket_Plus/releases/Pocket_Plus_10765b4330e6/Pocket_Plus`。真实受体与 CryoAtom2 的 launch 分别为 `run_f2_basic_job385001_20260917T232757_a1` 和 `run_f2_basic_job385002_20260917T232747_a1`；两项 3,443 文件预映像门控均通过。

两种受体条件的 F2-basic 参数已经独立冻结：真实受体为 `score_threshold=0.6524679660797119`、`min_voxels=27`，CryoAtom2 为 `score_threshold=0.7009013295173645`、`min_voxels=26`；共同使用 `prefiltered_min_voxel=8`、`objective_beta=1`。23:42 状态快照中，真实受体 `test_0` 为 31/179，CryoAtom2 为 179/179；两项均无 traceback。

23:43 关键事件：CryoAtom2 Job `385002` 已完成独立调参与完整 179-PDB `test_0` 评估，但在正式入口返回后的 `test_1` 临时派生步骤因 Python 环境缺少 `scipy` 而失败。已生成结果可直接复用；恢复任务只负责派生 149-PDB `test_1` 和执行既有文件保护校验。真实受体 Job `385001` 仍在正常执行 `test_0`。

23:47 关键事件：CryoAtom2 窄恢复 Job `385054` 已以 8 CPU 提交。恢复范围只包含 `test_1` 保序派生和既有文件保护校验；提交前固定 Python 环境的导入门控已通过。

23:55 关键事件：CryoAtom2 恢复 Job `385054` 已成功完成，149-PDB `test_1` 与既有 3,443 文件保护校验均通过；本实验预期新增 185 个文件完整存在。真实受体 Job `385001` 也已完成 179-PDB `test_0`，在相同临时环境位置退出；8 CPU 窄恢复 Job `385055` 已提交，不重复调参或评估。

2026-09-18 00:04 完成事件：真实受体恢复 Job `385055` 成功结束，149-PDB `test_1` 与 3,443 个既有文件保护校验通过。两种受体条件的 F2–F1–basic 均已完成独立 calibration 选参、179-PDB `test_0` 评估和 149-PDB `test_1` 派生；最终总门控状态为 `PASS`。

| 受体条件 | 测试集 | semantic micro F1 | semantic macro F1 |
| --- | --- | ---: | ---: |
| 真实受体 | `test_0` | 0.603989 | 0.581018 |
| 真实受体 | `test_1` | 0.588084 | 0.574930 |
| CryoAtom2 | `test_0` | 0.559646 | 0.481263 |
| CryoAtom2 | `test_1` | 0.535073 | 0.487698 |

全部 P/R、F1、PRAUC 和 top-K 已写入两种受体条件各自的本地 `主要结果.md`、`说明.md` 与 `补充结果.md`；这些文档保持未提交且未上传服务器。

最终总门控使用以下只读命令；它不是正式运行命令：

```bash
/home/penghongen/anaconda3/envs/Pocket_Plus_centos7_cu121_allgpu/bin/python -u /home/penghongen/My_Project/Pocket_Plus/tmp/find1_f2_basic_ablation_20260917/final_gate.py
```
