# Find_1 真实受体训练第 5 片推理执行记录

本文记录固定 50 片训练 PDB 定义中用户第 5 片的唯一实验。中断或失败后重试仍追加在本文后半部分，不按 attempt 新建日志。跨实验状态见 `文档/exec_plan/2026-09-16_Find_1真实受体训练分片推理总日志.md`；第 1—4 片的已完成实验见 `文档/exec_plan/2026-09-13_Find_1真实受体训练分片推理.md`。

## 人类检查摘要

- 当前状态：正式 attempt a2 的 probability 稳定运行约 7 小时 32 分钟；100/275 个 PDB 完成，GPU 显存 67,962 MiB、利用率 100%，`after_lock_378587` 保留。
- 唯一处理范围：用户片号 5，即 CLI `shard-index=4`。不得加入第 6 片或改变 seed `3407` 的固定 50 片定义。
- 正式产物根：`/storage/penghongen/AdaLigand_stage1_inference/Find_1/真实受体/artifacts/Find_1/train`。
- 指标：本实验只生成 Stage2、Stage3 所需输入，不计算 calibration、validation 或测试指标。
- W&B：本实验不启动训练，不使用 W&B。
- 正式流程：probability → 冻结阈值 F2 blobs → centered → 冻结 Gaussian score-only；当前位于稳定 probability 前向。

## 冻结输入与科学契约

- 训练 PDB 清单：`/storage/penghongen/AdaLigand/Ori_Data/stage1_preparation_box_pool_3/split/pdb_split/train.json`。完整清单固定以 seed `3407` 洗牌一次，再按 `shuffled[shard_index::50]` 分片。
- checkpoint：`/home/penghongen/Feedback/Pocket_Plus/logs/AdaLigand_Stage1_pdb_centric_resume-Find_1-pdb_centric_2/Find_1-pdb_centric_2_resume____Find_1_pdb_centric_2_job368455_20260909T071535_a2_pdb_centric_2_resume/checkpoints/TOP_epoch_04_score_0.6654.ckpt`，SHA-256 为 `3f5dd715da76a2337ddb94017f442783798566cac584b8dd16882d8afd792ee4`。
- resolved config：同一训练目录中的 `config.yaml`，SHA-256 为 `9a4ea5cf95d50deb3d42c8420e4dddf1d3a7eb0ebb6d0ee4edbff0388f1884da`。
- F2 语义参数：`/storage/penghongen/AdaLigand_stage1_inference/Find_1/真实受体/artifacts/Find_1/tuning/F2_semantic.json`，阈值为 `0.431304931640625`，SHA-256 为 `ac9d4b058700906db8a5c74a46094422b87ba684197c84af20b23ee46000ffd2`。
- F2 Gaussian 参数：同一 tuning 目录中的 `F2_gaussian.json`，固定 `objective_beta=1`、`score_threshold=0.9002149105072021`、`lambda_positive=0.064`、`lambda_negative=0.0128`、`tau_angstrom=1.25`、`prefiltered_min_voxel=8`、`min_voxels=27`，SHA-256 为 `6f116ba857b578aa5a70bade8af9fa6e47790a7ad55020636194747e1a062f23`。
- centered 固定使用 `forward_min_voxels=8` 和 `--continue-on-blob-exceed`。超框候选仍前向并保留标识；候选顺序、来源索引、四组 offsets 和数组字段必须与第 1—4 片一致。

## 门控与只读核查命令

本节只记录门控或只读核查，不得把这些命令误作正式运行命令。

2026-09-16 15:41 已通过 `scontrol show job -dd 378587`、allocation 控制文件检查、计算节点进程快照和 `nvidia-smi` 核验接管前状态。

第 5 片身份门控在 Job `378587` 的 allocation 内重建全部 50 片，并与第 1—4 片使用的冻结 manifest 逐片比较。门控报告位于 `/storage/penghongen/tmp/find1_real_receptor_train_shard_05_20260916/preflight.json`，SHA-256 为 `53491f3ef53eed7f978863c8b7c3cfdf87ddbd83969d83c3cdd5a16bd582f0ae`。

- 训练清单仍含 13,717 个唯一 PDB，SHA-256 仍为 `8e7f975ea49ee94e6819f2b35bf596c9bc4abacac9afc0aaebe2018b90d94d00`。
- 第 5 片含 275 个唯一 PDB，成员序列 SHA-256 为 `70ff0eb03dbd8bcb9ce1cc99a69a7afb2191dc68d041cb93f9d2be13c8e120b2`。
- 第 1—4 片合计 1,100 个 PDB，成员并集序列 SHA-256 为 `5f7301e2d34aa51147b79b1f79f1bfd14a52c5adf11ec29707fda4f9af2b374b`；第 5 片与该并集无交集。
- 完整 50 片身份与 `/storage/penghongen/tmp/find1_real_receptor_train_shards_20260913/preflight.json` 逐片一致。正式产物根当前有 1,100 个直接 PDB 目录，第 5 片没有既有目录。

当前入口与资源配置的本地门控命令如下。这些命令不执行正式推理。

```powershell
& 'C:\Program Files\Git\bin\bash.exe' -n '训练与运行/sh/infer/find1_real_receptor_train_shard_05.sh'
```

```powershell
& 'D:\Anaconda\envs\Pocket_Plus_windows\python.exe' -c "from omegaconf import OmegaConf; a=OmegaConf.to_container(OmegaConf.load(r'configs/inference/stage1_v3.yaml'), resolve=True); b=OmegaConf.to_container(OmegaConf.load(r'configs/inference/stage1_v3_h100_32cpu.yaml'), resolve=True); a['blob_workers']=b['blob_workers']; a['calibration']['workers']=b['calibration']['workers']; assert a==b"
```

```powershell
& 'D:\Anaconda\envs\Pocket_Plus_windows\python.exe' -m pytest tests/inference -q
```

2026-09-16 16:16 的结果分别为 Shell 语法通过、除 `blob_workers` 与 `calibration.workers` 外配置字段逐项相同，以及 `57 passed in 15.88s`。测试同时覆盖未授权片号拒绝、五项 SHA 失败时零 Stage1 调用、错误 `TASK_PROJECT_ROOT` 隔离、逐阶段冻结参数和失败传播。

## 正式运行命令

```bash
exec bash "/home/penghongen/Feedback/Pocket_Plus/releases/Pocket_Plus_b354f7562994/Pocket_Plus/训练与运行/sh/infer/find1_real_receptor_train_shard_05.sh" 5
```

该命令已于 2026-09-16 16:25 原子写入 `/home/penghongen/Feedback/PocketXMol/allocations/378587/run_cmd_378587.sh`，文件 SHA-256 为 `70b02f7ad33dc53246dadc64809ea9ca600274ce1571931f3735c1bcba772e46`。本节只记录唯一正式命令；前述 PowerShell、Slurm、SHA-256 和测试命令均属于门控或只读核查。

## 状态快照

### 2026-09-16 15:41 接管前

- Job `378587` 名称为 `F-5`，实际资源为 hnode02 上 H100×1、32 CPU，partition 为 `h100`，QOS 为 `h100g2`。
- 该 Job 属于 PocketXMol：任务根为 `/home/penghongen/My_Project/PocketXMol`，反馈根为 `/home/penghongen/Feedback/PocketXMol`。原动态命令调用 `训练与运行/sh/train_docking.sh F-5`，SHA-256 为 `1a7600217e2e6fc8da80751778575fa3bcc40edf70341dc6678f8799097e4553`。
- 原任务仍在运行。主训练进程 PID/PGID/SID 均为 `143161`，命令为 `/storage/penghongen/PocketXMol/runtime/venv/bin/python scripts/train_pl.py configs/docking/F-5.yml --logdir /storage/penghongen/PocketXMol/training/F-5`；快照时主进程 CPU 使用率约 98.2%。
- 分配到的 GPU 为 NVIDIA H100 PCIe，索引 0，UUID 为 `GPU-b461d126-0913-c6fe-c25a-e7f125155ad6`；快照显存占用 80,114/81,559 MiB，瞬时利用率为 0%。
- `after_lock_378587` 存在；`try_lock_378587`、`pre_lock_378587` 与 `kill_lock_378587` 均不存在。原任务尚未停止，因此此时禁止改写动态命令或启动 Find_1 推理。

### 2026-09-16 15:42:43 发出接管终止信号

- 已在 `/home/penghongen/Feedback/PocketXMol/allocations/378587/kill_lock_378587` 创建本次获授权的 `kill_lock`；同目录的 `after_lock_378587` 前后均存在。
- 创建后没有改写动态命令、删除 `try_lock` 或使用 `scancel`。只有原训练进程组消失、四锁执行器自动删除 `kill_lock_378587` 并创建根目录 `try_lock_378587` 后，才进入新任务发布阶段。

### 2026-09-16 15:43 原任务停止

- 四锁执行器已终止 PID/PGID `143161` 对应的 PocketXMol 训练进程组，并以预期的退出码 137 结束原任务第 1 次执行。
- `kill_lock_378587` 已由执行器自动删除，根目录 `try_lock_378587` 已创建，`after_lock_378587` 继续存在。计算节点上不再存在 `train_pl.py`，GPU 索引 0 的显存占用为 1 MiB、利用率为 0%。
- 原任务已经完全停止，可以在完成第 5 片身份、冻结输入、代码和 release 门控后原子改写动态命令；此时仍未启动 Find_1 推理。

### 2026-09-16 15:53 单片入口通过本地门控

- 新增 `训练与运行/sh/infer/find1_real_receptor_train_shard.sh`，只接收一个用户片号。入口从自身路径定位 Pocket Plus release，不使用 PocketXMol allocation 注入的 `TASK_PROJECT_ROOT`；因此跨项目接管不会误调用 PocketXMol 文件。
- 用户片号在入口内严格减一。以用户第 5 片运行时，probability、F2 blobs、centered 和 Gaussian score-only 四个阶段均只收到 `--shard-count 50 --shard-index 4`；入口不含第 6 片、不执行 tune、evaluate 或 overwrite。
- `bash -n 训练与运行/sh/infer/find1_real_receptor_train_shard.sh` 已通过。`Pocket_Plus_windows` 环境执行 `python -m pytest tests/inference/test_find1_real_receptor_evaluation.py -q`，结果为 6 项全部通过；伪官方入口验证了四阶段顺序、冻结参数边界、片号拒绝和失败传播。
- 代码仍在实现分支整理中，尚未发布 Learn release，也没有改写动态命令或删除 `try_lock_378587`。

### 2026-09-16 16:08 独立审查问题已修复

- 单片入口改为 `训练与运行/sh/infer/find1_real_receptor_train_shard_05.sh`，并只接受用户片号 5。输入 6、其他片号、缺参或多参都会在任何产物写入前以退出码 2 拒绝。
- 新增 `configs/inference/stage1_v3_h100_32cpu.yaml`。完整图 batch 仍为 18，centered batch 仍为 12；两类 GPU 阶段各使用 26 个请求物化线程，blobs 与未使用的 calibration 上限为 30 个 worker，不再在 32 CPU allocation 上启动 56 个 blobs worker。
- 每次正式 attempt 都在四阶段开始前核验训练清单、checkpoint、resolved config、F2 语义参数和 F2 Gaussian 参数的既定 SHA-256。任一外部输入变化都会在续写产物前终止。
- 单片入口从自身位置解析冻结 Pocket Plus release，不采用 PocketXMol allocation 注入的 `TASK_PROJECT_ROOT`。后续动态命令必须以冻结 Pocket Plus release 的绝对路径调用该入口，并在重试时复用同一 release。
- 修订后 Shell 语法、YAML 解析和 6 项定向测试全部通过。动态测试逐阶段核对 checkpoint、训练代码快照、F2 阈值、`forward_min_voxels=8`、`--continue-on-blob-exceed`、Gaussian 参数、片号和错误环境变量隔离。

### 2026-09-16 16:16 第二轮全面审查问题已修复

- 配置目录 README 已纳入 H100/32 CPU 文件及其 30/26/18/12 资源值；配置文件不再写入一次 Job 的状态，并点名三张 48³ 数组字段。
- 测试现在深比较 H100/32 CPU 配置与基准配置，只有 `blob_workers` 和 `calibration.workers` 被允许不同；还模拟 SHA 核验失败，确认任何 Stage1 阶段均未调用。
- `tests/inference` 全量 57 项全部通过。第二轮逻辑审查已经批准正式执行逻辑；代码、注释和 Git 布局只剩审查所提问题的窄口径复核。

### 2026-09-16 16:25 Learn release 与动态命令就绪

- 实现端点 `71004c6` 与 Learn 端点 `691674b` 逐文件等价；`Learn/CUMULATIVE` 已快进到 `691674b1930f4dbc220541be0d1cad1985105ab1`，Learn 端点再次通过 Shell 语法和 `tests/inference` 的 57 项测试。
- 安全同步后的代码冻结为 `/home/penghongen/Feedback/Pocket_Plus/releases/Pocket_Plus_b354f7562994/Pocket_Plus`，release 内容 SHA-256 为 `b354f75629946bcf7550e29b37443bce9781719f773eab72d488e2c93993e171`。
- release 内单片入口、H100/32 CPU 配置和定向测试文件 SHA-256 分别为 `1d533e940513bd23482dc2532d612a9fa6280ffa232de2eaaf97950d5f01ffaf`、`ddbdf3067a8bac1b5b6531a5b59b50222602bde39b3c6e0d3b7c41d4fe44da08`、`3ab78a85d0614c33cd76bed4207bcddf80b16c2b237a91a254bfac6d3d6e1243`，与本地 Learn 文件一致。
- 五个外部冻结输入与第 5 片 preflight 哈希再次一致。旧动态命令按原 SHA `1a7600217e2e6fc8da80751778575fa3bcc40edf70341dc6678f8799097e4553` 备份到 `/storage/penghongen/tmp/find1_real_receptor_train_shard_05_20260916/run_cmd_378587.before_find1_shard_05.sh`。
- 新动态命令已经原子发布，但 `try_lock_378587` 尚未删除；`after_lock_378587` 继续存在，`kill_lock_378587` 不存在。因此正式推理仍未启动。

### 2026-09-16 16:27 正式 attempt a2 启动

- 在动态命令、release、五个冻结输入、preflight 和锁状态再次通过门控后，删除 `/home/penghongen/Feedback/PocketXMol/allocations/try_lock_378587` 启动。没有创建新的 `kill_lock`，没有触碰 `after_lock_378587`，也没有使用 `scancel`。
- 控制器 launch 为 `/home/penghongen/Feedback/PocketXMol/launches/378587/train_docking_job378587_20260916T162710_a2`。其 `run_cmd.sh` 与唯一正式命令一致；launch 元数据继续记录原控制器的 PocketXMol release，这是跨项目 allocation 的预期行为。
- 计算节点主进程 PID/PGID/SID 为 `153737`，命令是冻结 Pocket Plus release 的 `find1_real_receptor_train_shard_05.sh 5`。子进程已经进入 `python -u -m src.inference.cli probability`，参数仅含 `--shard-count 50 --shard-index 4`，配置路径是同一 release 的 `stage1_v3_h100_32cpu.yaml`。
- 启动后首次检查仍在模型与数据初始化，GPU 显存为 1 MiB、瞬时利用率为 0%。这不是稳定前向证据；需继续观察首批 probability 产物和 GPU 活动后再进入长间隔守护。

### 2026-09-16 16:30 probability 稳定

- 第 5 片已有 1/275 个 PDB 同时存在 `probability/probability_map.npz` 和 `status/probability/_COMPLETE`；F2 blobs、F2 centered 及其完成标记均为 0/275，符合顺序阶段的当前边界。
- probability Python 进程继续使用 CLI `shard-index=4` 和 H100/32 CPU 配置，CPU 使用率约 553%。H100 显存占用为 67,892/81,559 MiB，瞬时利用率为 100%，已形成正常 GPU 前向。
- `after_lock_378587` 存在，`try_lock_378587` 与 `kill_lock_378587` 不存在。未发现任务退出或锁状态异常；按约定进入由连续 300 秒睡眠组成的 60 分钟静默等待。

### 2026-09-16 17:33 首次静默守护结束

- probability 完成数由 1 增至 10/275；F2 blobs、F2 centered 及其完成标记仍为 0/275，说明正式流程仍处于第一个阶段。
- 主脚本已运行约 66 分钟，probability Python 进程继续使用冻结 checkpoint、训练代码快照、H100/32 CPU 配置和 CLI `shard-index=4`。该进程 CPU 使用率约 544%；H100 显存占用 67,948/81,559 MiB、瞬时利用率 100%。
- 从 attempt a2 起算的 stdout/stderr 没有 traceback、CUDA OOM、`OutOfMemoryError`、`RuntimeError`、磁盘不足、冻结输入错误或新 `Killed`。`after_lock_378587` 存在，`try_lock_378587` 与 `kill_lock_378587` 不存在。
- 任务保持稳定，下一轮以连续 18 次 `Start-Sleep -Seconds 300` 组成 90 分钟静默等待。

### 2026-09-16 19:04 第二轮静默守护结束

- probability 完成数由 10 增至 37/275；F2 blobs、F2 centered 及其完成标记仍为 0/275，阶段顺序保持不变。
- 主脚本已运行约 2 小时 38 分钟，probability Python 进程 CPU 使用率约 542%；H100 显存占用 67,956/81,559 MiB、瞬时利用率 100%。
- 当前 attempt 的 stdout/stderr 中，traceback、CUDA OOM、`OutOfMemoryError`、`RuntimeError`、磁盘不足、冻结输入错误和新 `Killed` 的计数均为 0。`after_lock_378587` 存在，`try_lock_378587` 与 `kill_lock_378587` 不存在。
- release、checkpoint、H100/32 CPU 配置和 CLI `shard-index=4` 均未漂移。任务保持稳定，继续 90 分钟静默等待。

### 2026-09-16 20:36 第三轮静默守护结束

- probability 完成数由 37 增至 48/275；F2 blobs、F2 centered 及其完成标记仍为 0/275。不同 PDB 的完整图尺寸不同，本窗口完成数低于上一窗口，但产物计数持续增加。
- 主脚本已运行约 4 小时 9 分钟，probability Python 进程 CPU 使用率约 542%；H100 显存占用 67,956/81,559 MiB、瞬时利用率 97%。
- 当前 attempt 的 stdout/stderr 严格错误计数仍全部为 0。`after_lock_378587` 存在，`try_lock_378587` 与 `kill_lock_378587` 不存在；release、配置和 CLI `shard-index=4` 未漂移。
- 没有停滞或失败证据，继续 90 分钟静默等待。

### 2026-09-16 22:07 第四轮静默守护结束

- probability 完成数由 48 增至 69/275；F2 blobs、F2 centered 及其完成标记仍为 0/275，阶段顺序保持不变。
- 主脚本已运行约 5 小时 40 分钟，probability Python 进程 CPU 使用率约 542%；H100 显存占用 67,962/81,559 MiB、瞬时利用率 100%。
- 当前 attempt 的 stdout/stderr 严格错误计数仍全部为 0。进程命令、冻结 release、H100/32 CPU 配置、CLI `shard-index=4` 和三类锁状态均未漂移。
- 任务保持稳定，继续 90 分钟静默等待。

### 2026-09-16 23:59 第五轮静默守护结束

- probability 完成数由 69 增至 100/275；F2 blobs、F2 centered 及其完成标记仍为 0/275，阶段顺序保持不变。
- 主脚本已运行约 7 小时 32 分钟，probability Python 进程 CPU 使用率约 541%；H100 显存占用 67,962/81,559 MiB、瞬时利用率 100%。
- 当前 attempt 的 stdout/stderr 严格错误计数仍全部为 0。进程命令、冻结 release、H100/32 CPU 配置、CLI `shard-index=4` 和三类锁状态均未漂移。
- 任务保持稳定，继续 90 分钟静默等待。

### 2026-09-17 01:30 第六轮静默守护结束

- probability 完成数由 100 增至 117/275；F2 blobs、F2 centered 及其完成标记仍为 0/275，阶段顺序保持不变。
- 主脚本已运行约 9 小时 3 分钟，probability Python 进程 CPU 使用率约 540%；H100 显存占用 67,962/81,559 MiB、瞬时利用率 97%。
- 当前 attempt 的 stdout/stderr 严格错误计数仍全部为 0。进程命令、冻结 release、H100/32 CPU 配置、CLI `shard-index=4` 和三类锁状态均未漂移。
- 任务保持稳定，继续 90 分钟静默等待。

### 2026-09-17 03:11 第七轮静默守护结束

- probability 完成数由 117 增至 143/275；F2 blobs、F2 centered 及其完成标记仍为 0/275，阶段顺序保持不变。
- 主脚本已运行约 10 小时 45 分钟，probability Python 进程 CPU 使用率约 539%；H100 显存占用 67,962/81,559 MiB、瞬时利用率 96%。
- 当前 attempt 的 stdout/stderr 严格错误计数仍全部为 0。进程命令、冻结 release、H100/32 CPU 配置、CLI `shard-index=4` 和三类锁状态均未漂移；`after_lock_378587` 继续存在。
- 任务保持稳定，继续 90 分钟静默等待。

### 2026-09-17 04:45 第八轮静默守护结束

- probability 完成数由 143 增至 173/275；F2 blobs、F2 centered 及其完成标记仍为 0/275，阶段顺序保持不变。
- 主脚本已运行约 12 小时 18 分钟，probability Python 进程 CPU 使用率约 539%；H100 显存占用 67,962/81,559 MiB、瞬时利用率 100%。
- 当前 attempt 的 stdout/stderr 严格错误计数仍全部为 0。进程命令、冻结 release、H100/32 CPU 配置、CLI `shard-index=4` 和三类锁状态均未漂移；`after_lock_378587` 继续存在。
- 任务保持稳定，继续 90 分钟静默等待。

### 2026-09-17 06:17 第九轮静默守护结束

- probability 完成数由 173 增至 205/275；F2 blobs、F2 centered 及其完成标记仍为 0/275，阶段顺序保持不变。
- 主脚本已运行约 13 小时 51 分钟，probability Python 进程 CPU 使用率约 538%；H100 显存占用 67,962/81,559 MiB、瞬时利用率 100%。
- 当前 attempt 的 stdout/stderr 严格错误计数仍全部为 0。进程命令、冻结 release、H100/32 CPU 配置、CLI `shard-index=4` 和三类锁状态均未漂移；`after_lock_378587` 继续存在。
- 任务保持稳定，继续 90 分钟静默等待。

### 2026-09-17 07:49 第十轮静默守护结束

- probability 完成数由 205 增至 222/275；F2 blobs、F2 centered 及其完成标记仍为 0/275，阶段顺序保持不变。本窗口完成量受后段较大完整图影响，但产物计数持续增长。
- 主脚本已运行约 15 小时 23 分钟，probability Python 进程 CPU 使用率约 537%；H100 显存占用 67,962/81,559 MiB、瞬时利用率 100%。
- 当前 attempt 的 stdout/stderr 严格错误计数仍全部为 0。进程命令、冻结 release、H100/32 CPU 配置、CLI `shard-index=4` 和三类锁状态均未漂移；`after_lock_378587` 继续存在。
- 任务保持稳定，继续 90 分钟静默等待。

### 2026-09-17 09:20 第十一轮静默守护结束

- probability 完成数由 222 增至 242/275；F2 blobs、F2 centered 及其完成标记仍为 0/275，阶段顺序保持不变。
- 主脚本已运行约 16 小时 54 分钟，probability Python 进程 CPU 使用率约 537%；H100 显存占用 67,962/81,559 MiB，检查瞬间利用率为 0%，属于样本之间的 CPU/数据切换，进程和产物均继续推进。
- 当前 attempt 的 stdout/stderr 严格错误计数仍全部为 0。进程命令、冻结 release、H100/32 CPU 配置、CLI `shard-index=4` 和三类锁状态均未漂移；`after_lock_378587` 继续存在。
- probability 余下 33 个 PDB，下一轮改用连续 12 次 `Start-Sleep -Seconds 300` 组成的 60 分钟静默等待，以便更及时核验阶段切换。

### 2026-09-17 10:22 第十二轮静默守护结束

- probability 完成数由 242 增至 255/275；F2 blobs、F2 centered 及其完成标记仍为 0/275，阶段顺序保持不变。
- 主脚本已运行约 17 小时 56 分钟，probability Python 进程 CPU 使用率约 536%；H100 显存占用 67,962/81,559 MiB，检查瞬间利用率为 0%，但进程和产物计数继续推进。
- 当前 attempt 的 stdout/stderr 严格错误计数仍全部为 0。进程命令、冻结 release、H100/32 CPU 配置、CLI `shard-index=4` 和三类锁状态均未漂移；`after_lock_378587` 继续存在。
- probability 余下 20 个 PDB，继续 60 分钟静默等待；第 6 片仍由第 5 片最终验收门控，不会提前启动。

### 2026-09-17 11:16 probability 完成并进入 F2 blobs

- 第 5 片 275/275 个成员均同时具备 `probability/probability_map.npz` 和 `status/probability/_COMPLETE`；F2 blobs 与 F2 centered 在阶段切换检查点仍为 0/275。
- 正式入口已自动进入 `python -u -m src.inference.cli blobs`。命令继续使用训练清单、`alpha=2`、冻结的 `F2_semantic.json`、H100/32 CPU 配置及 `--shard-count 50 --shard-index 4`，没有进入第 6 片。
- blobs 进程启动约 20 秒时 CPU 使用率约 229%；H100 显存降至 1 MiB、利用率 0%，符合该 CPU 阶段。主脚本累计运行约 18 小时 49 分钟。
- 当前 attempt 的 stdout/stderr 严格错误计数全部为 0。`after_lock_378587` 存在，`try_lock_378587`、`pre_lock_378587` 与 `kill_lock_378587` 不存在。F2 blobs 属于关键阶段切换，后续先短间隔核查完成计数。

### 2026-09-17 11:23 F2 blobs 完成并进入 centered

- 冻结阈值 F2 blobs 已完成 275/275，全部成员同时具备 `blobs/F2_blobs.npz` 与 `status/F2_blobs/_COMPLETE`；centered 已完成 10/275。
- centered 正式命令继续使用冻结 checkpoint 与训练代码快照，并明确传入 `alpha=2`、`forward_min_voxels=8`、`--continue-on-blob-exceed`、`--shard-count 50 --shard-index 4` 和 `stage1_v3_h100_32cpu.yaml`。
- centered 进程启动约 5 分钟时 CPU 使用率约 282%；H100 显存占用 46,476/81,559 MiB。检查瞬间利用率为 0%，但已有 10 个正式 centered 产物与完成标记，阶段正在推进。
- 当前 attempt 的 stdout/stderr 严格错误计数全部为 0。`after_lock_378587` 保留，其他三类锁不存在；第 6 片仍未启动。centered 已形成稳定产物，后续进入 60 分钟静默守护。

### 2026-09-17 12:25 centered 静默守护结束

- centered 完成数由 10 增至 246/275；probability 与 F2 blobs 保持 275/275。
- centered 进程 CPU 使用率约 287%；H100 显存占用 46,492/81,559 MiB、瞬时利用率 100%。正式命令仍明确包含 `forward_min_voxels=8`、`--continue-on-blob-exceed` 和 CLI `shard-index=4`。
- 当前 attempt 的 stdout/stderr 严格错误计数全部为 0。冻结 release、进程身份和锁状态未漂移；`after_lock_378587` 继续保留。
- centered 余下 29 个成员，已接近 Gaussian score-only 阶段边界，后续采用连续 300 秒睡眠组成的短间隔核查；第 6 片仍由第 5 片最终验收门控。

### 2026-09-17 12:41 Gaussian score-only 启动

- centered 前向已为 275/275 个成员生成 `centered/F2_centered.npz`，正式入口已自动进入冻结 Gaussian score-only；进程命令使用 `F2_gaussian.json`、`--score-only`、`--shard-count 50 --shard-index 4` 和同一冻结配置。
- 12:43 的数组键只读核查显示 173/275 个 NPZ 已同时含有 `score` 与 `selected`。score-only 在更新当前成员时会暂时移除对应 centered 完成标记，因此此刻为 274/275 个完成标记；这是非破坏性续写的瞬时状态，不是成员遗漏。
- score-only 进程 CPU 使用率约 92%，GPU 已释放至 1 MiB，符合 CPU 评分阶段。当前 attempt 的 stdout/stderr 严格错误计数全部为 0，`after_lock_378587` 保留，其他三类锁不存在。
- 第 6 片仍未启动。score-only 已接近完成，继续以单次 `Start-Sleep -Seconds 300` 短间隔核查全部数组键、完成标记和 allocation 回锁。

### 2026-09-17 13:14 正式流程完成并通过全量验收

- 正式 attempt a2 已成功退出。第 5 片 275/275 个成员均具备 `probability_map.npz`、冻结阈值 `F2_blobs.npz`、`F2_centered.npz`、`score/selected` 和三个完成标记；正式日志没有 traceback、CUDA OOM、`OutOfMemoryError`、磁盘不足、`RuntimeError` 或本入口错误。
- 四锁执行器的成功日志出现后，终态锁曾短暂缺失；12:51 已补建 `try_lock_378587`，并再次确认 `pre_lock=false`、`try_lock=true`、`kill_lock=false`、`after_lock=true`。GPU 显存为 1 MiB、利用率为 0%，没有残留推理进程。
- 全量只读验收逐 PDB 精确复算正式 blobs 与 Gaussian 分数，并核对候选顺序、四组 offsets、无法完整放入 80³ BOX 的来源体素裁切、三张 48³ 数组和 `selected`。汇总为 9,638 个来源 blob、9,387 个 centered 候选、2,005,773 个完整来源体素、1,348,302 个归档来源体素、5 个超框候选、0 个 blob 数量超过 1,000 的 PDB，以及 7,059 个最终选择。
- 最终验收报告为 `/storage/penghongen/tmp/find1_real_receptor_train_shard_05_20260916/final_verification.json`，SHA-256 为 `2626a2e26193182ee85a1da007328d674d1f290c055fb6237d78aada5ba95537`。报告冻结记录训练清单、checkpoint、resolved config、F2 语义参数、Gaussian 参数、动态命令和 preflight 的实际 SHA-256。
- 第 5 片至此完成。只有在本门控通过并确认 `try_lock_378587` 后，才开始准备用户第 6 片；未创建新的 `kill_lock_378587`，未使用 `scancel`。

## 之前的尝试与接管历史

截至完成，第 5 片没有失败或中断的正式推理 attempt。被接管的 PocketXMol 原任务因本次获授权的 `kill_lock` 以退出码 137 结束；这是 allocation 接管历史，不是 Find_1 实验失败。最终验收器的前两次只读运行分别暴露了成员摘要编码、密度根目录和空候选特征宽度的验收器假设错误；它们均在读取正式产物阶段中止，没有修改任何正式文件。修正为固定清单的紧凑 JSON 摘要、正式 `Ori_Data/density` 根目录和 `pack_centered_entries()` 的空候选契约后，第三次全量验收通过。
