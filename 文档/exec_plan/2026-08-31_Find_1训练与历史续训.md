# Find_1 训练与历史续训执行记录

本记录对应 `文档/规划文档/Find_1训练与历史续训.md`。它保存实现、部署、资源接管、训练运行与故障处理的可追溯事实；没有状态变化的周期检查不写成流水账。

## Git 与本地实现坐标

- 本地生产工作树：`C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra`。
- 实现分支：`codex/find1-training`。
- 开始实现的 `Learn/CUMULATIVE`：`275fcec06ad088e7c8ebf5f52224a68e0dabac34`。
- 跨节点 DDP 实现提交：`1d231f2`，提交说明为“补齐跨节点多卡 DDP 启动链路”。
- 跨节点 DDP 回归：基础设施与配置 45 项通过；训练侧 DDP、checkpoint 和实验目录 6 项通过；10 个 Shell 文件通过 `bash -n`；`git diff --check` 通过。

## 隔离服务器任务根部署

- 第一次上传尝试：2026-08-31 22:52:00.359（Asia/Shanghai）。
- 第二次上传开始：2026-08-31 22:52:40.798（Asia/Shanghai）；上传、权限和行尾处理在 22:53:07 前完成。
- 哈希与 Shell 语法核验：2026-08-31 22:53:23.179—22:53:32.311（Asia/Shanghai）。
- 服务器任务根：`/home/penghongen/My_Project/Pocket_Plus_Find1`。
- 服务器反馈根：`/home/penghongen/Feedback/Pocket_Plus_Find1`。
- 部署方式：使用本机项目 SSH helper 建立目录，再使用不含 `--delete` 的 `rsync -av` 上传当前生产工作树；共享 `/home/penghongen/My_Project/Pocket_Plus` 未被改写。
- 第一次 `rsync` 因本机子进程的 `PATH` 中没有 `sshpass` 而在发送前失败。远端只建立了空目录，没有收到项目文件。把 `D:\msys64\usr\bin` 加入该调用的 `PATH` 后，以同一非删除式命令重试成功。
- 上传成功后执行 `chmod -R 755`，并只对服务器任务根内的 `.sh` 与 `.sbatch` 文件去除 CRLF 行尾。
- 这两次历史上传命令的 `rsync` 远端 Shell 使用了 `StrictHostKeyChecking=accept-new`。该参数不符合当前项目要求的 `StrictHostKeyChecking=yes`；此次没有发生主机密钥变化。后续部署必须使用项目统一 helper 的严格主机密钥检查，不复用这里的历史偏差。

### 原始部署命令

第一次调用先建立服务器目录，再尝试上传；上传在发送文件前失败：

```powershell
$remoteRoot = '/home/penghongen/My_Project/Pocket_Plus_Find1'; & '.\与服务器交互\other\Invoke-PasswordSsh.ps1' -Command "mkdir -p '$remoteRoot'"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $msysBin = 'D:\msys64\usr\bin'; $rsync = Join-Path $msysBin 'rsync.exe'; $cygpath = Join-Path $msysBin 'cygpath.exe'; $sshpassFile = Join-Path $env:USERPROFILE '.ssh\pocket_plus_sshpass.txt'; $localPosix = (& $cygpath -u (Get-Location).Path).Trim(); $passPosix = (& $cygpath -u $sshpassFile).Trim(); $env:MSYS2_ARG_CONV_EXCL = '*'; $remoteShell = "sshpass -f $passPosix ssh -p 10022 -o StrictHostKeyChecking=accept-new -o WarnWeakCrypto=no -o PreferredAuthentications=password -o PubkeyAuthentication=no -o NumberOfPasswordPrompts=1"; & $rsync -av --exclude='.git' --exclude='.git/' --exclude='__pycache__/' --exclude='*.pyc' --exclude='.pytest_cache/' --exclude='.ruff_cache/' -e $remoteShell "$localPosix/" "penghongen@10.102.33.220:${remoteRoot}/"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & '.\与服务器交互\other\Invoke-PasswordSsh.ps1' -Command "set -euo pipefail; chmod -R 755 '$remoteRoot'; find '$remoteRoot' -type f \( -name '*.sbatch' -o -name '*.sh' \) -exec sed -i 's/\r$//' {} +"
```

第二次调用只增加了 MSYS2 工具目录到本次进程的 `PATH`，随后上传成功：

```powershell
$remoteRoot = '/home/penghongen/My_Project/Pocket_Plus_Find1'; $msysBin = 'D:\msys64\usr\bin'; $rsync = Join-Path $msysBin 'rsync.exe'; $cygpath = Join-Path $msysBin 'cygpath.exe'; $sshpassFile = Join-Path $env:USERPROFILE '.ssh\pocket_plus_sshpass.txt'; $localPosix = (& $cygpath -u (Get-Location).Path).Trim(); $passPosix = (& $cygpath -u $sshpassFile).Trim(); $env:Path = "$msysBin;$env:Path"; $env:MSYS2_ARG_CONV_EXCL = '*'; $remoteShell = "sshpass -f $passPosix ssh -p 10022 -o StrictHostKeyChecking=accept-new -o WarnWeakCrypto=no -o PreferredAuthentications=password -o PubkeyAuthentication=no -o NumberOfPasswordPrompts=1"; & $rsync -av --exclude='.git' --exclude='.git/' --exclude='__pycache__/' --exclude='*.pyc' --exclude='.pytest_cache/' --exclude='.ruff_cache/' -e $remoteShell "$localPosix/" "penghongen@10.102.33.220:${remoteRoot}/"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & '.\与服务器交互\other\Invoke-PasswordSsh.ps1' -Command "set -euo pipefail; chmod -R 755 '$remoteRoot'; find '$remoteRoot' -type f \( -name '*.sbatch' -o -name '*.sh' \) -exec sed -i 's/\r$//' {} +"
```

哈希与 Shell 语法核验使用的原始命令为：

```powershell
$files = @('训练与运行/submit_task.sh','训练与运行/sbatch/task.sbatch','训练与运行/runtime/allocation_runner.sh','训练与运行/runtime/create_launch.sh','训练与运行/runtime/launch_training_python.sh'); foreach ($file in $files) { $hash = (Get-FileHash -Algorithm SHA256 -LiteralPath $file).Hash.ToLower(); Write-Output "$hash  $file" }; & '.\与服务器交互\other\Invoke-PasswordSsh.ps1' -Command "set -euo pipefail; cd /home/penghongen/My_Project/Pocket_Plus_Find1; sha256sum '训练与运行/submit_task.sh' '训练与运行/sbatch/task.sbatch' '训练与运行/runtime/allocation_runner.sh' '训练与运行/runtime/create_launch.sh' '训练与运行/runtime/launch_training_python.sh'; bash -n '训练与运行/submit_task.sh' '训练与运行/sbatch/task.sbatch' '训练与运行/runtime/allocation_runner.sh' '训练与运行/runtime/create_launch.sh' '训练与运行/runtime/launch_training_python.sh'"
```

### 跨节点控制文件哈希

下列 SHA-256 在本地与服务器逐项一致，服务器端 `bash -n` 同时通过：

| 文件 | SHA-256 |
| --- | --- |
| `训练与运行/submit_task.sh` | `f4e8694bfef3a1a61175d651aafdc55f89044acbc0da2cee4462b1b77d7836a7` |
| `训练与运行/sbatch/task.sbatch` | `badab7d448a96249dd52be8959da6bf330041c47dcf798b57193ed25056c5d78` |
| `训练与运行/runtime/allocation_runner.sh` | `46a8e2ae510f37d9fa2f7dbed756da686f13e5c7ffec3220ec102b982e42a0ef` |
| `训练与运行/runtime/create_launch.sh` | `0c08b2bc799712a7596dea7c641464f7965fbe0943950ee94bdc0ea773e867b5` |
| `训练与运行/runtime/launch_training_python.sh` | `a5b766cfd92a9371413a845e09d38a993a2c0c73366725840d77903b75c56118` |

### 双节点提交 dry-run

监视子代理在服务器隔离根使用 `SBATCH_BIN=/bin/echo` 做了无副作用 dry-run。正式命令形状为：

```bash
bash /home/penghongen/My_Project/Pocket_Plus_Find1/训练与运行/submit_task.sh \
  --task-root /home/penghongen/My_Project/Pocket_Plus_Find1 \
  --feedback-root /home/penghongen/Feedback/Pocket_Plus_Find1 \
  --sh Find_1.sh \
  --job-name Find_1_resume_multinode \
  --resource a800 \
  --nodes 2 \
  --gpus 1 \
  --cpus C \
  --nodelist gnode09,gnode10 \
  --multi-node-ddp \
  --pre_hold \
  --after_hold
```

其中 `C` 必须在窗口出现时根据两个节点的实时状态重新计算，不能沿用早先快照。

## 资源监视事件

### 2026-08-31 22:48 A800 首个即时快照

- `nvlink` 仍有等待作业 `366147`，因此接管窗口未出现。
- `350302`：RUNNING，`gnode09`，16 CPU，1×A800；存在 `try_lock` 与 `after_lock`。
- `356946`：RUNNING，`gnode10`，16 CPU，1×A800；正在执行，仅存在 `after_lock`。
- `gnode09`：CPU 已分配 46/48，GPU 已分配 4/8。
- `gnode10`：CPU 已分配 42/48，GPU 已分配 7/8。
- 仅用于校验公式的当时估算：取消两个目标作业后，两节点预测空闲 CPU 分别为 18 与 22，故 `F=18`、`C=17`、每 rank workers=16。该值不作为未来提交参数。
- 没有提交、取消、写锁或释放任何作业。

### 2026-09-01 01:41—01:51 H100 Job 366071 获得资源并回到 try_lock

#### 作业身份与动作边界

- Slurm 记录的准确开始时间为 2026-09-01 01:41:37（Asia/Shanghai）。监视器在 01:41:51 首次观察到 Job 366071 从 PENDING 进入 RUNNING。
- `scontrol show job -dd 366071` 在 01:42:18 核实：`JobState=RUNNING`、`NodeList=hnode02`、`NumNodes=1`、`NumCPUs=32`、`CPUs/Task=32`、`JOB_GRES=gpu:h100:1`、`GRES=gpu:h100:1(IDX:0)`。提交入口仍是共享任务根 `/home/penghongen/My_Project/Pocket_Plus/训练与运行/sh/Find_1.sh`，并启用了 `--after_hold 1`。
- 正式 PDB-centric-1 实现和 AUTO 动力学门禁尚未验收。监视器因此只对 Job 366071 创建具名 `kill_lock_366071`，没有操作任何其他 Job 或锁。
- `kill_lock` 的创建发生在 01:42:18 之后、allocation runner 于 01:43:14 记录消费之前。原始操作没有返回可证明的更精确 touch 时间，本记录不伪造时间点。
- allocation runner 在 01:43:14 记录向旧 attempt a1 的进程组 257942 发送终止信号；a1 退出码为 137。runner 随后创建根级 `try_lock_366071`，没有启动 attempt a2。
- 2026-09-01 01:51:57 的独立复核确认 Job 仍为 RUNNING，资源为 hnode02 的 1×H100 与 32 CPU。`try_lock` 和 `after_lock` 均保留，`kill_lock` 已被 watcher 消费，`pre_lock` 不存在。
- 同期 H100 分区仍有他人作业 366156 PENDING，hnode01 与 hnode02 的节点级 `GRES_USED` 都是 3/3，因此不满足双 H100 切换条件，没有提交或取消其他 H100 作业。

#### 旧 attempt a1 产物身份

此次 a1 只用于解释为何必须立刻回到 `try_lock`，不属于新 PDB-centric-1 的科学结果：

- release：`/home/penghongen/Feedback/Pocket_Plus/releases/Pocket_Plus_5b8ca0631e1c/Pocket_Plus`。
- launch：`/home/penghongen/Feedback/Pocket_Plus/launches/366071/Find_1_job366071_20260901T014214_a1`。
- run：`/home/penghongen/Feedback/Pocket_Plus/logs/AdaLigand_Stage1_pdb_centric-Find_1-CPC1/Find_1-CPC1____Find_1_job366071_20260901T014214_a1_CPC1`。
- allocation 日志：`/home/penghongen/Feedback/Pocket_Plus/allocations/366071/out` 与 `err`。
- run 配置：`config.yaml` SHA-256 为 `3a0d342ece0e09214d084c8a0256cd6fe1c68a8102bb168ff655dfb43404da0a`；`train.yaml` SHA-256 为 `4ca1c9b05f6ba313df92ba43736089d0303fce532f02813a108c5d1aade422d7`。
- 动态命令：`/home/penghongen/Feedback/Pocket_Plus/allocations/366071/run_cmd_366071.sh`，SHA-256 为 `feef2e59312130978c1dc298c51915a6c19259b067fead9f29e57327259db7b3`。

动态命令的完整内容为：

```bash
#!/usr/bin/env bash
set -euo pipefail

# 本文件可以在 pre_lock 或 try_lock 存在、任务未运行时编辑。
# TASK_PROJECT_ROOT 会在每次执行前指向该次刚创建的 release。
exec bash "${TASK_PROJECT_ROOT}"/训练与运行/sh/Find_1.sh
```

a1 已创建运行目录、配置和 `src_snapshot`，日志显示 Dataset 仍是旧 `stage1_find`，随后在模型实例化阶段收到 `kill_lock`。它没有进入正式优化步骤，也没有产生可用于本轮结论的 checkpoint。

#### 监视器执行的准确远端载荷

01:41:51 的状态变化轮询：

```bash
printf '[timestamp]\n'; date -Is; printf '[job_366071]\n'; squeue -j 366071 -h -o '%i|%T|%M|%D|%R|%C|%b|%j'; printf '[h100_pending]\n'; squeue -p h100 -t PD -h -o '%i|%T|%M|%D|%R|%C|%b|%j'; printf '[h100_nodes]\n'; sinfo -N -p h100 -h -O 'NodeList:16,StateLong:16,CPUsState:24,CPUsLoad:12,Gres:28,GresUsed:52'
```

01:42:18 的身份核对：

```bash
date -Is; squeue -j 366071 -h -o '%i|%T|%M|%D|%R|%C|%b|%j'; scontrol show job -dd 366071; squeue --steps -j 366071 -h -o '%i|%T|%M|%N|%C|%j'; allocation=/home/penghongen/Feedback/Pocket_Plus/allocations/366071; if [[ -d "$allocation" ]]; then find "$allocation" -maxdepth 2 -type f -printf '%TY-%Tm-%TdT%TH:%TM:%TS|%p|%s bytes\n' | sort; find "$allocation" -maxdepth 1 -type f \( -name '*lock*' -o -name 'run_cmd*' \) -printf '%f|%s bytes|%TY-%Tm-%TdT%TH:%TM:%TS\n' | sort; for f in "$allocation"/run_cmd_366071.sh "$allocation"/run_cmd.sh; do if [[ -f "$f" ]]; then printf 'path=%s\n' "$f"; sed -n '1,120p' "$f"; fi; done; fi
```

该 Slurm 版本不接受 `squeue --steps` 中的 `%T/%C` 格式；同一载荷中的其他核对均成功，不影响作业身份判断。

紧接身份核对后、创建 `kill_lock` 前的第二次只读核对：

```bash
allocation=/home/penghongen/Feedback/Pocket_Plus/allocations/366071; sed -n '1,200p' "$allocation/out"; sed -n '1,200p' "$allocation/err"; find /home/penghongen/Feedback/Pocket_Plus -maxdepth 4 -type f \( -name 'release.json' -o -name 'launch.json' \) -newermt '2026-09-01 01:40:00' -printf '%TY-%Tm-%TdT%TH:%TM:%TS|%p\n' | sort
```

唯一改变服务器状态的远端载荷如下：

```bash
set -euo pipefail; job_id=366071; allocation=/home/penghongen/Feedback/Pocket_Plus/allocations/366071; resolved=$(readlink -f -- "$allocation"); [[ "$resolved" == "$allocation" ]]; state=$(squeue -j "$job_id" -h -o '%T|%N'); [[ "$state" == RUNNING\|hnode02 ]]; [[ -f "$allocation/after_lock_$job_id" ]]; [[ ! -e "$allocation/try_lock_$job_id" ]]; [[ ! -e "$allocation/kill_lock_$job_id" ]]; printf '[precondition] job=%s state=%s allocation=%s after_lock=present\n' "$job_id" "$state" "$resolved"; touch -- "$allocation/kill_lock_$job_id"; printf '[kill_lock_created]\n'; stat -c '%n|%s bytes|%y' "$allocation/kill_lock_$job_id"; for attempt in $(seq 1 36); do sleep 5; kill_state=absent; try_state=absent; after_state=absent; [[ -e "$allocation/kill_lock_$job_id" ]] && kill_state=present; [[ -e "$allocation/try_lock_$job_id" ]] && try_state=present; [[ -e "$allocation/after_lock_$job_id" ]] && after_state=present; printf '[wait_%02d] kill=%s try=%s after=%s\n' "$attempt" "$kill_state" "$try_state" "$after_state"; if [[ "$kill_state" == absent && "$try_state" == present && "$after_state" == present ]]; then exit 0; fi; done; exit 42
```

该载荷的写操作本身成功，runner 也在 01:43:14 消费了 `kill_lock`。但等待循环错误地在 Job 子目录寻找 `try_lock`；实际 `try_lock` 位于 allocations 根，因此循环没有命中并最终使本机 SSH helper 报出空 stderr 异常。这个错误只影响等待与回显，没有第二次写入，也没有触碰其他锁。01:46:23、01:47:23、01:48:29 和 01:50:21 的后续只读检查逐步确认了退出日志、正确的根级 `try_lock`、三份身份目录、动态命令哈希、H100 排队和节点级 GRES。

#### 当前保留锁

| 锁 | 最终状态 | 绝对路径 | mtime |
| --- | --- | --- | --- |
| `pre_lock` | 不存在 | `/home/penghongen/Feedback/Pocket_Plus/allocations/pre_lock_366071` | — |
| `try_lock` | 存在 | `/home/penghongen/Feedback/Pocket_Plus/allocations/try_lock_366071` | 2026-09-01 01:43:14 +08:00 |
| `after_lock` | 存在 | `/home/penghongen/Feedback/Pocket_Plus/allocations/366071/after_lock_366071` | 2026-09-01 01:41:37 +08:00 |
| `kill_lock` | 不存在 | `/home/penghongen/Feedback/Pocket_Plus/allocations/366071/kill_lock_366071` | — |

后续只有在生产实现审查、部署哈希核验和真实 AUTO 动力学门禁通过后，才允许删除 `try_lock_366071` 启动新 attempt。当前不得删除 `after_lock_366071`，也不得释放 H100。

## 2026-09-01 01:51—03:12 审查收口与门禁加固

### 唯一一轮全面审查

- 表达、代码结构与四项 skill 合规全面审查已经完成；修正报告项后的窄口径复核为通过。本轮不再进行第二次全面表达审查。
- 科学逻辑全面审查确认 P0 为零，并报告六项 P1 与一项 P2：轨迹身份可伪造、完整模型独有损失和其他参数组可由 NaN 假通过、固定位置抽样不能证明逐元素等价、自然 recycle 首批分离可形成零证据假通过、历史续训跨目录丢失 `ModelCheckpoint` 状态、规划文档允许门禁前放行 Job `366071`，以及旧 checkpoint 不包含 DataLoader worker 的随机增强状态。
- 针对上述报告项的逻辑窄口径复核已经完成；身份门禁最后补充进程级 nonce、PID 与两个受信源码树摘要后，复核者确认没有残留缺陷。本轮不再扩大为第二次全面逻辑审查。
- 配置数值、V1/V2 validation、一个 AdamW、353/1,241 两组互斥穷尽、每组 0.5 裁剪、无采样条件分支和 Lightning 每个 optimizer step 只调用一次裁剪均经审查确认正确。
- `tests/test_project_task_runner.py` 的 kill_lock 测试仍要求假 `srun` 收到 TERM 后以 143 退出并出现 `try_lock`；两秒宽限只消除 Windows 调度抖动，没有掩盖 TERM 路径。

### 生产动力学门禁修复

- 每条轨迹自动记录进程级 `process_nonce`、PID、目标项目 `src/` 与 `configs/` 的内容 SHA-256、完整解析配置 SHA-256、role、experiment 和提交身份。比较器拒绝相同输入文件、相同项目根、相同进程 nonce、相同 PID、错误 AUTO 提交或错误生产提交，并要求两个项目源码摘要分别等于正式执行前从只读 release 独立核验并记录的预期值。
- 每个体素输出、累计梯度、裁剪前后梯度、参数、参数增量和 AdamW 状态都计算原始 dtype 完整张量字节的 SHA-256，并记录 `all_finite`。正式比较要求 SHA-256 完全相同；这比固定位置抽样或容差比较更严格，任何未抽样位置差异都会直接失败，不需要发布大型完整张量副本。
- 完整模型强制存在 `atom` 与 `pseudo` 两项损失；两项分别调用 `torch.autograd.grad` 测量对 353 个体素参数的回灌，必须各自严格为零，不能由两项梯度相消得到假零。
- 1,241 个其他参数的裁剪前后梯度、参数、参数增量、AdamW `exp_avg`、`exp_avg_sq` 和 state step 全部记录并检查有限性；其他组裁剪后范数必须不超过 0.5。
- 自然轨迹记录实际 recycle 序列。若 AUTO 与完整模型的自然序列分离，必须再让两模型共同重放 AUTO 序列和完整模型序列；两份 replay 比较都通过且其来源 SHA-256 分别对应两份自然 JSON 后，自然归因比较才可通过。第 0 个 microbatch 即分离时也不能靠零个比较项通过。
- 正式门禁由八条轨迹命令和四条比较命令组成，完整顺序位于 `tmp/find1_optimization_dynamics/README.md`；真实 GPU 门禁尚未执行，本记录不提前宣称通过。

### 历史 checkpoint 与不可恢复边界

- 历史隔离分支新增 `ops/find1_historical_resume/rebase_checkpoint.py`。新 attempt 的 node rank 0 在新运行目录复制历史 top-k checkpoint，重写 callback 的 `dirpath`、`best_model_path`、`best_k_models`、`kth_best_model_path` 与 `last_model_path`，再原子发布 `resume_state.ckpt` 和 `resume_state_manifest.json`；其余节点只等待共享产物。
- 真实 Lightning `ModelCheckpoint` 跨目录测试已证明 `best_model_score`、top-k 映射、`kth_value` 和 last 路径完整恢复。旧运行目录保持只读。
- 源 `last.ckpt` 客观上没有 DataLoader worker 的 Python/NumPy RNG 状态，因此无法重建未中断旧进程本应产生的逐位随机旋转序列。本轮不伪造精确恢复，也不改变原训练的随机旋转制度：首轮剩余 BOX 身份按 45,150 个 rank-local batch 连续，旋转由新 worker RNG 继续。该科学边界已经同时写入历史 shell、迁移 manifest 与历史 `训练与运行/README.md`。
- 规划文档已改为：Job `366071` 在门禁前只能由自身 `kill_lock` 回到根级 `try_lock`，生产实现、部署哈希和动力学门禁通过后才可启动正式 PDB-centric-1。

### 本地验证命令与结果

生产工作树在 2026-09-01 03:11 完成最终验证：

```powershell
& 'D:\Anaconda\envs\Pocket_Plus_windows\python.exe' -m pytest -q `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\tests' `
  --ignore='C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\tests\test_stage1_producers.py' `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\tmp\find1_optimization_dynamics\tests'
```

结果为 `367 passed, 11 warnings in 75.05s`。其中包括身份门禁新增的错误源码摘要与相同 PID 回归测试；该测试文件单独复核为 `8 passed in 0.14s`。`tests/test_stage1_producers.py` 在本轮基点即因不存在既有 `src.artifacts` 包而无法收集，不属于 Find_1 差异；其余全部可收集测试已通过。

```powershell
& 'D:\Anaconda\envs\Pocket_Plus_windows\python.exe' -m compileall -q `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\src' `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\tests' `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\tmp\find1_optimization_dynamics'
& 'C:\Program Files\Git\bin\bash.exe' -n 'C:/Users/15919/Desktop/Pocket_Plus_worktrees/cross_node_ddp_infra/训练与运行/sh/Find_1.sh'
& 'C:\Program Files\Git\bin\bash.exe' -n 'C:/Users/15919/Desktop/Pocket_Plus_worktrees/cross_node_ddp_infra/训练与运行/sh/Find_1_pdb_centric_2.sh'
git -C 'C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra' diff --check
```

Python 编译、两份 Find_1 shell 及通用 runtime/submit shell 语法、差异检查均以退出码 0 完成。

生产实现与文档在审查、复核和上述最终验证后提交：主要实现提交为 `10463559fd147f700a4e9b1d91f6458e8043d9f4`。该提交因强制纳入一次性动力学门禁目录而误纳入其中的 Python 字节码缓存；清理提交 `2bcbb13b285d71adbde8cd6d237dbad743baea28` 随即删除全部缓存文件，不改变任何源码或科学配置。服务器隔离部署只允许使用清理后的端点 `2bcbb13b285d71adbde8cd6d237dbad743baea28`。

历史隔离工作树执行：

```powershell
& 'D:\Anaconda\envs\Pocket_Plus_windows\python.exe' -m pytest -q `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\find1_historical_resume\tests\test_adaligand_stage1_configs.py' `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\find1_historical_resume\tests\test_find1_historical_resume.py' `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\find1_historical_resume\tests\test_multinode_ddp_runtime.py' `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\find1_historical_resume\tests\test_project_task_runner.py'
```

结果为 `48 passed, 15 warnings in 65.19s`；历史 helper 编译、`Find_1.sh`、allocation runner、跨节点 Python launcher 的 Bash 语法和 `git diff --check` 同样以退出码 0 完成。

## 2026-09-01 03:12—03:28 生产端点提交与隔离部署

### Git 与部署身份

- 生产实现提交：`10463559fd147f700a4e9b1d91f6458e8043d9f4`。
- 字节码缓存清理后的可执行端点：`2bcbb13b285d71adbde8cd6d237dbad743baea28`。
- 历史续训端点：`5b4aa5f52d699d904fa82e97e22099047e607e4c`。
- 本地从可执行端点创建归档：`C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\tmp\find1-production-2bcbb13b285d.tar`，大小 6,942,720 bytes，SHA-256 为 `00a98c6b368972a16ec12b8184578251a76daadd514d3b61b69afcc10b961b85`。
- 归档上传地址：`/home/penghongen/Feedback/Pocket_Plus/uploads/find1-production-2bcbb13b285d.tar`，服务器 mtime 为 2026-09-01 03:17:52 +08:00；服务器复算 SHA-256 与本地完全相同。
- 隔离项目根：`/home/penghongen/Feedback/Pocket_Plus/task_roots/find1-production-2bcbb13b285d/Pocket_Plus`，原子发布完成时间为 2026-09-01 03:20:25 +08:00。部署没有修改共享 `/home/penghongen/My_Project/Pocket_Plus`。
- 归档解包后检查了 `src/wrappers/voxel_point_stage1.py`、两份动力学程序和 `训练与运行/sh/Find_1.sh`，并确认部署树不存在 `.pyc` 或 `__pycache__`。归档内相同文件与服务器文件的 SHA-256 一致；文本文件因 Windows 工作树 CRLF 转换而不以本地工作区文件字节为核验对象。

创建和上传使用的准确本地命令为：

```powershell
git archive --format=tar --prefix=Pocket_Plus/ `
  --output='C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\tmp\find1-production-2bcbb13b285d.tar' `
  2bcbb13b285d71adbde8cd6d237dbad743baea28
```

随后使用项目统一 SSH 入口，以 `StrictHostKeyChecking=yes` 创建具名服务器目录；使用本机 MSYS2 `rsync` 和同一严格 SSH 参数把上述归档上传到具名地址。服务器先核对归档 SHA-256，再解包到具名临时目录、检查必要文件与字节码缓存，最后原子重命名为正式隔离根。首次直接拼接远端命令时因 PowerShell 引号被统一 SSH 入口拒绝，没有建立连接或产生服务器写入；之后改用 LF 脚本 stdin，成功完成上述步骤。

### 两个受信源码树摘要

动力学程序的 `_source_tree_sha256()` 按相对路径与原始文件字节遍历 `src/**/*.py`、`configs/**/*.py`、`configs/**/*.yaml` 和 `configs/**/*.yml`。正式执行前在服务器只读复算得到：

- AUTO B_trunk release：195 个文件，`5c6fb1ea40a1471b9c0ba0cfe9c251697318c6cae72b11fe5268234dbc63fc75`。
- Find_1 生产隔离根：195 个文件，`90c0874db54d7bf058aba5f5039989f5c924e3ebe11b91b34a1cfa8cec0f27b7`。

四条正式比较命令必须分别匹配这两个预期摘要，错误或陈旧代码树不能只靠填写正确提交标签通过。

### Job 366071 动力学 attempt 待执行命令

2026-09-01 03:26:46 +08:00 再次核对 Job `366071`：状态 RUNNING，节点 `hnode02`，H100×1，CPU×32；根级 `try_lock_366071` 与 allocation 内 `after_lock_366071` 存在，`pre_lock_366071` 与 `kill_lock_366071` 不存在。旧动态命令 SHA-256 仍为 `feef2e59312130978c1dc298c51915a6c19259b067fead9f29e57327259db7b3`。

03:26:06 +08:00 把新的完整门禁命令写入：

`/home/penghongen/Feedback/Pocket_Plus/allocations/366071/run_cmd_366071.sh`

文件大小 8,216 bytes，SHA-256 为 `df252894d8251fc357ad40a182998ebbd68cbf9cee4f1430d257607c1da98a2c`，本地与服务器一致；服务器 `bash -n` 通过。脚本冻结以下公共参数：BF16、batch size 6、8 次累积、2 个 optimizer step、seed 3407、AUTO 提交 `7aae1f126ee0c1636a3f14455f8239a072ac28c9`、生产提交 `2bcbb13b285d71adbde8cd6d237dbad743baea28`、checkpoint SHA-256 `8b00e3def507119a8e39473d64533617c340922146a392ed1defc0e36547e770` 和上述两个源码树摘要。

该动态命令将依次执行以下十二条 Python 命令；每条都由 `set -x` 展开到 `gate.log`，allocation runner 还会把完整 `run_cmd` 复制到本 attempt 的 launch：

1. AUTO `CPC1/Find_1_trunk` controlled 轨迹 → `trunk_controlled.json`。
2. 生产 `CPC1/Find_1` controlled 轨迹 → `full_controlled.json`。
3. AUTO natural 轨迹 → `trunk_natural.json`。
4. 生产 natural 轨迹 → `full_natural.json`。
5. AUTO 重放 AUTO natural recycle 序列 → `trunk_replay_trunk_sequence.json`。
6. 生产模型重放 AUTO natural recycle 序列 → `full_replay_trunk_sequence.json`。
7. AUTO 重放生产 natural recycle 序列 → `trunk_replay_full_sequence.json`。
8. 生产模型重放生产 natural recycle 序列 → `full_replay_full_sequence.json`。
9. 比较两份 controlled 轨迹 → `controlled_comparison.json`。
10. 比较两份 AUTO 序列 replay 轨迹 → `trunk_sequence_replay_comparison.json`。
11. 比较两份生产序列 replay 轨迹 → `full_sequence_replay_comparison.json`。
12. 读取两份 replay 结果比较 natural 轨迹 → `natural_comparison.json`。

脚本的输出根固定为 `/home/penghongen/Feedback/Pocket_Plus/validation/find1_optimization_dynamics/${TASK_RUN_STAMP}`；其中保存 `identity.txt`、`gate.log`、八份轨迹、四份比较、`sha256sums.txt` 和 `_COMPLETE`，失败则写 `_FAILED`。本节记录的是已经部署但尚未删除 `try_lock` 的待执行命令；实际 attempt、launch、准确开始时间和结果在事件发生后补录。

### attempt a2：Dataset 插值失败并安全返回

主任务在以下条件全部再次成立后，只删除了 Job `366071` 的根级 `try_lock`：Job 为 `RUNNING|hnode02|CPU32|H100×1`，`after_lock_366071` 存在，动态命令 SHA-256 为 `df252894d8251fc357ad40a182998ebbd68cbf9cee4f1430d257607c1da98a2c`。准确触发命令是：

```bash
rm -- /home/penghongen/Feedback/Pocket_Plus/allocations/try_lock_366071
```

- 触发时间：2026-09-01 03:31:55 +08:00。
- attempt：a2；launch 创建时间：2026-09-01 03:32:32 +08:00。
- allocation runner 自动创建的共享源 release：`/home/penghongen/Feedback/Pocket_Plus/releases/Pocket_Plus_6ea13863d422/Pocket_Plus`。该 release 只属于 runner 留证；实际动态命令显式使用本节记录的生产隔离根。
- launch：`/home/penghongen/Feedback/Pocket_Plus/launches/366071/Find_1_job366071_20260901T033232_a2`；其中 `run_cmd.sh` SHA-256 与上述动态命令一致。
- 动力学输出根：`/home/penghongen/Feedback/Pocket_Plus/validation/find1_optimization_dynamics/Find_1_job366071_20260901T033232_a2`。
- 产物：`identity.txt`、`gate.log` 和 `_FAILED`；没有轨迹 JSON、比较 JSON 或 `_COMPLETE`。
- 失败时间：2026-09-01 03:32:47 +08:00；退出码 1。allocation runner 随即重建根级 `try_lock_366071`，`after_lock_366071` 始终保留。

首条 AUTO controlled 命令尚在构造 Dataset 时失败，没有执行 forward、backward 或 optimizer step。根因是 `_build_dataset()` 先以 `resolve=False` 把 `config.dataset` 从 Hydra 根配置分离，随后读取 `split_train`；该字段仍引用根作用域的 `${dataset.box_pool_root}`，分离后的配置已经没有 `dataset` 作用域，因而抛出 `InterpolationKeyError`。

### attempt a3 前的窄口径修复与重新部署

- 修复提交：`acdf3f5671aa7439097d70d50e2357cd91b5432f`。唯一运行逻辑变化是在分离 `config.dataset` 前以 `resolve=True` 完成插值；新增回归测试直接覆盖 `${dataset.box_pool_root}/split_train.npz`，动力学工具测试结果为 `9 passed in 1.69s`，Python 编译与差异检查通过。
- 新归档：`C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\tmp\find1-production-acdf3f5671aa.tar`，大小 6,952,960 bytes，SHA-256 为 `b595a867ba233d86900e2bc270b7568f7e8125b478154e01c00311b26a06830d`。
- 服务器上传：`/home/penghongen/Feedback/Pocket_Plus/uploads/find1-production-acdf3f5671aa.tar`，mtime 为 2026-09-01 03:35:19 +08:00。
- 新隔离项目根：`/home/penghongen/Feedback/Pocket_Plus/task_roots/find1-production-acdf3f5671aa/Pocket_Plus`，原子发布完成时间为 2026-09-01 03:37:16 +08:00；旧 a2 根和产物均未覆盖。
- 本次只修改 `tmp/find1_optimization_dynamics/`，所以 AUTO 与生产 `src/`、`configs/` 受信摘要仍分别是 `5c6fb1ea40a1471b9c0ba0cfe9c251697318c6cae72b11fe5268234dbc63fc75` 与 `90c0874db54d7bf058aba5f5039989f5c924e3ebe11b91b34a1cfa8cec0f27b7`；新归档和新动力学程序另由归档 SHA-256 与文件 SHA-256 绑定。
- 新动态命令仍位于 `/home/penghongen/Feedback/Pocket_Plus/allocations/366071/run_cmd_366071.sh`，大小 8,216 bytes，SHA-256 为 `e53fb90607ada6406880e64c6129368951ee3e60ee951964eed3371b8cd6f47b`；服务器 `bash -n` 通过。十二条命令和科学参数不变，只把生产隔离根与提交身份改成上述修复端点。

截至 2026-09-01 03:39:35 +08:00，Job `366071` 仍在 `hnode02` 运行，根级 `try_lock` 和 allocation 内 `after_lock` 均存在，新动态命令尚未触发。

### attempt a3：超大张量抽样索引越界并安全返回

主任务按与 a2 相同的具名锁、Job 资源和动态命令摘要核验顺序，只删除了根级 `try_lock_366071`：

```bash
rm -- /home/penghongen/Feedback/Pocket_Plus/allocations/try_lock_366071
```

- 触发时间：2026-09-01 03:45:08 +08:00。
- attempt：a3；launch 创建时间：2026-09-01 03:45:37 +08:00。
- launch：`/home/penghongen/Feedback/Pocket_Plus/launches/366071/Find_1_job366071_20260901T034537_a3`；其中 `run_cmd.sh` SHA-256 为 `e53fb90607ada6406880e64c6129368951ee3e60ee951964eed3371b8cd6f47b`。
- 动力学输出根：`/home/penghongen/Feedback/Pocket_Plus/validation/find1_optimization_dynamics/Find_1_job366071_20260901T034537_a3`。
- 产物：`identity.txt`、`gate.log` 和 `_FAILED`；没有轨迹 JSON、比较 JSON 或 `_COMPLETE`。
- 失败时间：2026-09-01 03:57:31 +08:00；退出码 1。根级 `try_lock` 随即恢复，`after_lock` 始终保留。

a3 已越过 a2 的配置插值问题，载入 checkpoint 并进入首个 AUTO controlled 轨迹的实际 forward。失败发生在给体素输出生成审计摘要时：旧实现用 CUDA float32 `torch.linspace()` 产生 16 个抽样位置；超大展平张量的末端整数超过 float32 连续精确表示范围后，舍入可能把合法的 `numel-1` 变成越界的 `numel`，最终在 `index_select()` 触发 `srcIndex < srcSelectDimSize` device-side assert。错误发生在第一个 microbatch 的输出摘要阶段，尚未形成一次 optimizer step。

### attempt a4 前的整数索引修复与重新部署

- 修复提交：`f4e4608f5063b0022cf892143c97427fd257fa4f`。新增 `_sample_positions()`，只用 Python 整数运算生成最多 16 个首尾覆盖位置，再把这 16 个合法整数复制到目标设备；不再让超大索引经过 float32。
- 回归测试覆盖空张量、单元素、小张量和 `2^24` 以上的大 `numel`，要求首项为 0、末项为 `numel-1`、全部严格递增且合法。动力学工具测试结果为 `10 passed in 1.69s`，Python 编译和差异检查通过。
- 新归档：`C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\tmp\find1-production-f4e4608f5063.tar`，大小 6,963,200 bytes，SHA-256 为 `36d6ded78992c9748747bd014aac8d7c86862b4621e1123aeb204acffa1595bb`。
- 服务器上传：`/home/penghongen/Feedback/Pocket_Plus/uploads/find1-production-f4e4608f5063.tar`，mtime 为 2026-09-01 04:02:28 +08:00。
- 新隔离项目根：`/home/penghongen/Feedback/Pocket_Plus/task_roots/find1-production-f4e4608f5063/Pocket_Plus`，原子发布完成时间为 2026-09-01 04:04:45 +08:00；旧 a2/a3 根与产物保持不变。
- AUTO 与生产 `src/`、`configs/` 受信摘要仍分别为 `5c6fb1ea40a1471b9c0ba0cfe9c251697318c6cae72b11fe5268234dbc63fc75` 与 `90c0874db54d7bf058aba5f5039989f5c924e3ebe11b91b34a1cfa8cec0f27b7`；新门禁程序文件 SHA-256 为 `a50222bf04f5b712e2c7204574473091c89488f556c873dd7bb84f63c7a01f26`。
- a4 待执行动态命令仍位于 `/home/penghongen/Feedback/Pocket_Plus/allocations/366071/run_cmd_366071.sh`，mtime 为 2026-09-01 04:05:09 +08:00，大小 8,216 bytes，SHA-256 为 `b73553218c96cc2c63806af9b0095337746842970448f3fa7d41fa10c3e1430f`；服务器 `bash -n` 通过。十二条科学命令不变，只更新生产隔离根与提交身份。

截至 2026-09-01 04:08:45 +08:00，Job `366071` 仍在 `hnode02` 运行，根级 `try_lock` 与 allocation 内 `after_lock` 均存在，a4 尚未触发。

### attempt a4：部分参数没有 AdamW state 并安全返回

主任务完成具名 Job、锁与动态命令 SHA-256 核验后，只删除了：

```bash
rm -- /home/penghongen/Feedback/Pocket_Plus/allocations/try_lock_366071
```

- 触发时间：2026-09-01 04:12:18 +08:00。
- attempt：a4；launch 创建时间：2026-09-01 04:12:41 +08:00。
- launch：`/home/penghongen/Feedback/Pocket_Plus/launches/366071/Find_1_job366071_20260901T041241_a4`；其中 `run_cmd.sh` SHA-256 为 `b73553218c96cc2c63806af9b0095337746842970448f3fa7d41fa10c3e1430f`。
- 动力学输出根：`/home/penghongen/Feedback/Pocket_Plus/validation/find1_optimization_dynamics/Find_1_job366071_20260901T041241_a4`。
- 产物：`identity.txt`、`gate.log` 和 `_FAILED`；没有完整轨迹 JSON、比较 JSON 或 `_COMPLETE`。
- 失败时间：2026-09-01 04:15:21 +08:00；退出码 1。根级 `try_lock` 随即恢复，`after_lock` 始终保留。

a4 已越过超大张量签名，完成首个 AUTO controlled 轨迹的 8 个梯度累积 microbatch 和第一次 `optimizer.step()`，随后在审计 AdamW state step 时失败。旧审计器直接读取每个体素参数的 `optimizer.state[parameter]["step"]`；该 batch 没有使用的参数没有梯度，AdamW 合法地没有为其创建 state，直接索引因此抛出 `KeyError: 'step'`。这是审计表示错误，不是 optimizer 或模型更新错误；原子轨迹 JSON 尚未发布。

### attempt a5 前的逐参数 AdamW state 修复与重新部署

- 修复提交：`5280bec889d35bbfa8b314ad12100a0f665d3479`。体素组和其他组现在都用完整参数名映射记录 state step；参与本次更新的参数记录整数 step，未参与的参数记录 `null`。比较器要求 AUTO 与完整模型的共同体素映射逐项一致，并校验 `step=null` 时一、二阶矩也必须为空、step 存在时两项矩必须存在且 step 等于预期值；每组至少一个参数实际参与更新。
- 回归测试新增“双方相同未参与参数可通过、单边 state 不同必须失败”，动力学工具测试结果为 `11 passed in 1.70s`；Python 编译和差异检查通过。
- 新归档：`C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\tmp\find1-production-5280bec889d3.tar`，大小 6,973,440 bytes，SHA-256 为 `cf083cbc02a7505b04a80ca1d23e6bf16246498a745de75c34840e2c1c9b5eec`。
- 服务器上传：`/home/penghongen/Feedback/Pocket_Plus/uploads/find1-production-5280bec889d3.tar`，mtime 为 2026-09-01 04:22:28 +08:00。
- 新隔离项目根：`/home/penghongen/Feedback/Pocket_Plus/task_roots/find1-production-5280bec889d3/Pocket_Plus`，原子发布完成时间为 2026-09-01 04:24:09 +08:00；旧 a2—a4 根与产物保持不变。
- AUTO 与生产 `src/`、`configs/` 受信摘要仍不变；新轨迹程序和比较器 SHA-256 分别为 `bf2ad67847b70e30947a8af3b3a6bb8e29f527585b5ad2693c1898f8b688502d` 与 `41578a63b616c5bed984828ce010c07bd7f908f229c3e332bda40796debfcf9f`。
- a5 待执行动态命令仍位于 `/home/penghongen/Feedback/Pocket_Plus/allocations/366071/run_cmd_366071.sh`，mtime 为 2026-09-01 04:24:33 +08:00，大小 8,216 bytes，SHA-256 为 `2a952f9999a8e05956dbcfae7710128bbcda9b6233351555966bb9ae4acb05eb`；服务器 `bash -n` 通过。十二条科学命令不变，只更新生产隔离根与提交身份。

截至 2026-09-01 04:26:05 +08:00，Job `366071` 仍在 `hnode02` 运行，根级 `try_lock` 与 allocation 内 `after_lock` 均存在，a5 尚未触发。

### attempt a5：标量参数原始字节视图失败并安全返回

主任务完成具名 Job、锁与动态命令 SHA-256 核验后，只删除了：

```bash
rm -- /home/penghongen/Feedback/Pocket_Plus/allocations/try_lock_366071
```

- 触发时间：2026-09-01 04:29:42 +08:00。
- attempt：a5；launch 创建时间：2026-09-01 04:30:10 +08:00。
- launch：`/home/penghongen/Feedback/Pocket_Plus/launches/366071/Find_1_job366071_20260901T043010_a5`；其中 `run_cmd.sh` SHA-256 为 `2a952f9999a8e05956dbcfae7710128bbcda9b6233351555966bb9ae4acb05eb`。
- 动力学输出根：`/home/penghongen/Feedback/Pocket_Plus/validation/find1_optimization_dynamics/Find_1_job366071_20260901T043010_a5`。
- 成功产物：`trunk_controlled.json`，大小 11,132,411 bytes，发布完成时间 2026-09-01 04:34:03 +08:00；该文件证明完整两步 AUTO controlled 轨迹已经原子完成。
- 其他产物：`identity.txt`、`gate.log` 和 `_FAILED`；没有 `full_controlled.json`、比较 JSON 或 `_COMPLETE`。
- 失败时间：2026-09-01 04:35:55 +08:00；退出码 1。根级 `try_lock` 随即恢复，`after_lock` 始终保留。

完整 Find_1 controlled 轨迹进入第一次 optimizer step 前的其他参数快照时遇到 0 维标量参数。旧原始字节摘要直接对 0 维 tensor 调用 `view(torch.uint8)`；PyTorch 不允许 0 维张量在改变元素大小时这样查看，因而抛出 `self.dim() cannot be 0 to view Float as Byte`。该错误只发生在审计序列化；完整模型尚未执行第一次 optimizer step。

### attempt a6 前的标量原始字节修复与重新部署

- 修复提交：`91f0833b0bfb1126c4b36c2bcc9a3151ac4fd4f3`。张量先 `reshape(-1)` 形成一维连续视图，再 `view(torch.uint8)` 读取原始 dtype 字节；普通张量摘要不变，float32 与 BF16 标量现在同样可审计。
- 新增两种 dtype 的 0 维标量回归测试；动力学工具测试结果为 `12 passed in 1.69s`，Python 编译和差异检查通过。
- 新归档：`C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\tmp\find1-production-91f0833b0bfb.tar`，大小 6,973,440 bytes，SHA-256 为 `247f2295d4b7afcfce0eb0065ff0ef8003ed1e8029ba738a50096a86c00bc240`。
- 服务器上传：`/home/penghongen/Feedback/Pocket_Plus/uploads/find1-production-91f0833b0bfb.tar`，mtime 为 2026-09-01 04:38:30 +08:00。
- 新隔离项目根：`/home/penghongen/Feedback/Pocket_Plus/task_roots/find1-production-91f0833b0bfb/Pocket_Plus`，原子发布完成时间为 2026-09-01 04:40:18 +08:00；旧 a2—a5 根与产物保持不变。
- AUTO 与生产 `src/`、`configs/` 受信摘要仍不变；新轨迹程序 SHA-256 为 `90f861a08d1274037001966bcadac4790271b9029786f139b55fbd13d53e9d79`。
- a6 待执行动态命令仍位于 `/home/penghongen/Feedback/Pocket_Plus/allocations/366071/run_cmd_366071.sh`，mtime 为 2026-09-01 04:40:45 +08:00，大小 8,216 bytes，SHA-256 为 `202e2771e3dbcba8b5e77e956da39c690dbe129a39150958241713691b7876a9`；服务器 `bash -n` 通过。十二条科学命令不变，只更新生产隔离根与提交身份。

截至 2026-09-01 04:42:19 +08:00，Job `366071` 仍在 `hnode02` 运行，根级 `try_lock` 与 allocation 内 `after_lock` 均存在，a6 尚未触发。

## AUTO 当前保留最优与参数边界

- AUTO 仓库：`C:\Users\15919\Desktop\AUTO\Pocket_Plus-v3-macro`。
- autoresearch run：`a44e7f6b849c4f34888835aef1f0c191`。
- 当前候选 HEAD：`1c8a4bac5ed27111ab2415c12f13824606f3e001`，RAUNet 多尺度输出候选；尚未产生保留分数，不能称为当前最优。
- 当前保留端点：`2ad913b5ff945739f85c192f6fcd1ff6cceb7883`。
- 当前正式最优：B_trunk，`macro_joint_score=1.6670933207345788`。
- B_trunk 正式训练提交：`7aae1f126ee0c1636a3f14455f8239a072ac28c9`；W&B run `du58fxi4`；checkpoint SHA-256 `8b00e3def507119a8e39473d64533617c340922146a392ed1defc0e36547e770`。
- 实际训练配置：单张 A800、BF16、物理 batch size 6、8 次梯度累积、全局 batch size 48、AdamW、学习率 `5e-5`、全局梯度裁剪 0.5、2,018 个 optimizer steps。
- 在关闭本地缺失的 FlashAttention 后，仅实例化配置用于参数名称审计：完整 Find_1 有 1,594 个可训练参数张量，B_trunk 有 353 个；二者交集与 B_trunk 完全相同。三个正式体素前缀选中 353 个张量，也与该交集完全相同。

### AUTO 精确参考实现与 checkpoint

- B_trunk 精确代码工作树：`C:\Users\15919\Desktop\AUTO_worktrees\find1_b_trunk_reference`，分离 HEAD 为 `7aae1f126ee0c1636a3f14455f8239a072ac28c9`。该工作树只用于动力学对照，不跟随 AUTO 当前候选分支变化。
- 服务器 checkpoint：`/storage/penghongen/tmp/AUTO/TUNE--Find-v3-macro/trials/baseline-7aae1f126ee0-20260828T034121771-b6/training/logs/Pocket_Plus_AUTO_tune_stage1_v3_macro/baseline-7aae1f126ee0-20260828T034121771-b6____baseline-7aae1f126ee0-20260828T034121771-b6/checkpoints/BEST.ckpt`。
- 原始 launch：`/home/penghongen/Feedback/AUTO/Pocket_Plus-v3-macro/launches/356946/Find_1_job356946_20260828T114043_a11`。
- 原始 release：`/home/penghongen/Feedback/AUTO/Pocket_Plus-v3-macro/releases/Pocket_Plus-v3-macro_2eec850361c7/Pocket_Plus-v3-macro`。
- 动力学对照必须在同一张 GPU 上先后执行 AUTO B_trunk 与完整 Find_1 的受控、自然随机和必要的双向反事实 replay。对照尚未运行，因此本记录不提前宣称动力学等价。

## 当前生产实现

### 配置入口

- PDB-centric-1：`configs/dataset/stage1_find_pdb_centric_1.yaml`、`configs/train/stage1_find_pdb_centric_1.yaml`、`configs/experiment/CPC1/Find_1.yaml` 与 `训练与运行/sh/Find_1.sh`。
- PDB-centric-2：`configs/dataset/stage1_find_pdb_centric_2.yaml`、`configs/train/stage1_find_pdb_centric_2.yaml`、`configs/experiment/CPC1/Find_1_pdb_centric_2.yaml` 与 `训练与运行/sh/Find_1_pdb_centric_2.sh`。该入口只接受配置与测试，不在本轮提交训练。
- 两份 Shell 没有按采样模式分支；科学参数由各自 Dataset、训练和实验 YAML 固定。

### 最小分组梯度裁剪

- `src/wrappers/voxel_point_stage1.py` 以三个已核实参数前缀划分体素组, 其余所有 `requires_grad=true` 参数进入另一组; Find_1 专属边界不放入通用 `src/utils/`。
- `src/wrappers/voxel_point_stage1.py` 仍使用一个 AdamW; `find_voxel_point` 模式仅把同一个 optimizer 中的两个互斥参数集合分别执行 0.5 范数裁剪。
- 其他实验默认继续使用 `global`，历史续训分支也保持原全局 0.5 裁剪。
- 真实配置实例化测试得到 1,594 个可训练参数张量，其中体素组 353 个；三个前缀分别选中 6、4、343 个张量。体素参数名称 SHA-256 为 `57cb249c0410369a60c17b09950432a23069c541ac4ddf56e9ef4cf9e9467e2e`，与 AUTO B_trunk 完全一致。

### 验收工具

- `tmp/find1_optimization_dynamics/optimization_trace.py` 负责在真实 Stage1 batch 上记录受控、自然随机或反事实 replay 的 BF16 优化轨迹。
- `tmp/find1_optimization_dynamics/compare_optimization_traces.py` 比较代码与进程身份、请求身份、共同体素输出、共同体素损失、完整张量摘要、两组梯度与裁剪、AdamW 状态和逐步参数增量，并把非有限值、点监督回灌和未经双向 replay 证明的自然 recycle 分离视为硬失败。
- `tmp/find1_optimization_dynamics/README.md` 说明命令、JSON 契约和一次性生命周期。这些代码只在实现分支与服务器验收副本中留证, 实际对照完成后会在最终学习端点前退出活动代码树。

## 历史续训实现

- 隔离工作树：`C:\Users\15919\Desktop\Pocket_Plus_worktrees\find1_historical_resume`。
- 分支：`codex/find1-historical-resume`。
- 历史基点：`1315d301c867c99e2dc0736feffde86e9cd7fa0a`。
- 跨节点基础设施提交：`e312e2c`；完整 checkpoint 恢复与首轮跳批提交：`604608f`；跨节点信号测试稳定化提交：`fb7e6c3`；跨目录 callback 状态迁移提交：`5b4aa5f52d699d904fa82e97e22099047e607e4c`。历史隔离工作树在提交后保持干净。
- `last.ckpt` 记录 `epoch=0`、`global_step=11287`，首轮 rank-local 已完成 45,150 个 microbatch。恢复时只在第一个 epoch 跳过这些已完成 batch，之后恢复完整采样；模型、optimizer、scheduler、callback 与 Lightning 循环状态通过迁移后的 `resume_state.ckpt` 恢复。
- 原运行使用双卡、物理 batch size 6、梯度累积 4；`45150=11287×4+2`。checkpoint 没有保存最后两个累积梯度，按用户选择接受其丢失，不补算、不修改 global step。
- 历史续训相关 48 项测试、Python 编译、Shell 语法和差异检查已经通过。正式双节点任务尚未获得，因此没有 release、launch 或新训练产物可记录。

## 后续事件模板

每次正式启动或重试必须新增一个小节，并至少写明：

- 服务器准确开始与结束时间、耗时；
- 完整命令；
- Slurm Job、节点、GPU、CPU、workers；
- `pre_lock`、`try_lock`、`after_lock`、`kill_lock` 的实际动作；
- release、launch、run_cmd 路径和 SHA-256；
- Git 提交、配置、checkpoint 和代码哈希；
- W&B run、训练日志、checkpoint 与 validation 产物目录；
- 异常、修复、科学契约变化和复核结果。

## 2026-09-01 05:10—07:27 attempt a6 事实归因、schema 4 门禁与 a7 启动

### attempt a6 的有效事实与失败边界

attempt a6 的 launch 为：

`/home/penghongen/Feedback/Pocket_Plus/launches/366071/Find_1_job366071_20260901T044525_a6`

输出根为：

`/home/penghongen/Feedback/Pocket_Plus/validation/find1_optimization_dynamics/Find_1_job366071_20260901T044525_a6`

a6 完成了八条真实轨迹，但旧比较器要求共同路径逐字节相同，最终于 2026-09-01 05:10:27 +08:00 写入 `_FAILED` 并安全返回根级 `try_lock_366071`；`after_lock_366071` 始终保留。该失败不是训练逻辑差异：共同体素路径含 CUDA `scatter_add_`，同一角色、同一请求与同一 recycle 序列的独立进程重复运行也不逐字节相同。跨角色与同角色重复的数值差异处于相同数量级：共同输出 `l2` 相对差最大值分别为 `1.67e-4` 与 `1.83e-4`，共同损失绝对差最大值分别为 `3.57e-4` 与 `8.05e-4`，累计梯度逐参数 `l2` 相对差 P95 最大值分别为 `2.45e-2` 与 `2.99e-2`。a6 因不含后续新增的稠密逐元素证据，不能追认为最终通过。

### schema 4 的逐元素硬边界

提交 `2fc8f15b142a5ada879b47399434ed4c3f256a98` 把轨迹 schema 升为 4。所有需要跨角色比较的共同体素输出、累计梯度、参数、参数增量与 AdamW 状态都保存可恢复的 float32 数值：小数组内联 Base64，大数组写入每条轨迹唯一的 `*.dense-float32.bin` 旁车；JSON 记录字节偏移、长度和 SHA-256。比较器核对旁车文件规模、区间、字节长度、SHA-256 与有限性，再分块计算真正的逐元素最大绝对差。任一位置超过该字段的 `element_absolute_max` 都会在所属 microbatch 或 optimizer step 形成硬失败，不能被张量长度、抽样位置或线性投影稀释。

上一轮审查给出的反例是长度 1,000,000 的数组，在位置 9004 与 22749 交换 `+50/-50`。新测试使用真实旁车复现该反例，得到 `element_absolute_max=100.0` 和 `passed=false`。原逻辑审查子任务只对此唯一 P1 做窄口径复核，结论为 PASS；未启动新一轮全面审查。

本地正式验证命令为：

```powershell
& 'D:\Anaconda\envs\Pocket_Plus_windows\python.exe' -m pytest -q `
  tests ops/stage1_data_preparation/tests tmp/find1_optimization_dynamics/tests `
  --ignore=tests/test_stage1_producers.py
& 'D:\Anaconda\Scripts\black.exe' --check `
  tmp/find1_optimization_dynamics/optimization_trace.py `
  tmp/find1_optimization_dynamics/compare_optimization_traces.py `
  tmp/find1_optimization_dynamics/tests/test_optimization_trace.py `
  tmp/find1_optimization_dynamics/tests/test_compare_optimization_traces.py
& 'D:\Anaconda\envs\Pocket_Plus_windows\python.exe' -m py_compile `
  tmp/find1_optimization_dynamics/optimization_trace.py `
  tmp/find1_optimization_dynamics/compare_optimization_traces.py
git diff --check
```

结果为 `396 passed, 11 warnings in 89.39s`；Black、Python 编译和差异检查均以退出码 0 完成。直接从仓库根执行无筛选的 `pytest -q` 会递归收集被忽略的旧 release 验证副本 `tmp/find1-production-2bcbb13b285d-verify/`，产生同名测试导入冲突；正式结果使用上面列出的活动测试根，不把旧副本纳入收集。

### a7 隔离部署与完整启动身份

本地从提交 `2fc8f15b142a5ada879b47399434ed4c3f256a98` 创建归档的准确命令为：

```powershell
git archive --format=tar --prefix=Pocket_Plus/ `
  -o tmp/find1-production-2fc8f1517f07.tar `
  2fc8f15b142a5ada879b47399434ed4c3f256a98
```

- 本地归档：`C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\tmp\find1-production-2fc8f1517f07.tar`，7,045,120 bytes，SHA-256 `35bc28f00945b3efc54185351df83ccc447d9ac16224511121e4c8610509ac57`。
- 服务器上传：`/home/penghongen/Feedback/Pocket_Plus/uploads/find1-production-2fc8f1517f07.tar`；使用 MSYS2 `rsync`、密码文件和 `StrictHostKeyChecking=yes` 上传，服务器复算摘要相同。
- 隔离项目根：`/home/penghongen/Feedback/Pocket_Plus/task_roots/find1-production-2fc8f1517f07/Pocket_Plus`；2026-09-01 07:23 前后经临时目录解包、必要文件检查、无字节码缓存检查后原子发布。共享 `/home/penghongen/My_Project/Pocket_Plus` 未修改。
- 服务器文件 SHA-256：`optimization_trace.py` 为 `7197640c24e8f46b03517b93b0dc63a2fe8c1def32119f30d231c6e0896c3d60`，`compare_optimization_traces.py` 为 `52ab36d6ea268f838825c0c37e41d20e1c1e62e94b87a04b8a4ef6020b102209`，`README.md` 为 `af6b910f3733cecad70de9a90ec1630db1d89072d3f182019f3d19a796aec375`。
- AUTO 与生产 `src/`、`configs/` 受信摘要仍分别为 `5c6fb1ea40a1471b9c0ba0cfe9c251697318c6cae72b11fe5268234dbc63fc75` 与 `90c0874db54d7bf058aba5f5039989f5c924e3ebe11b91b34a1cfa8cec0f27b7`；本次只改动一次性门禁目录。

Job `366071` 的动态命令仍位于：

`/home/penghongen/Feedback/Pocket_Plus/allocations/366071/run_cmd_366071.sh`

2026-09-01 07:26:49 +08:00 安装后的文件大小为 8,477 bytes，SHA-256 为 `5102edab331d149ab3731213caa510cf1e58a52793f991c1d80cc3b104a5c62b`，服务器 `bash -n` 通过。该文件保存本 attempt 实际使用的全部十二条 Python 命令；与 a6 相比，科学参数、checkpoint 和执行顺序不变，只更新生产隔离根、生产提交身份、归档摘要，并把最终摘要清单扩为全部 JSON 与 `*.dense-float32.bin`。allocation runner 已把同一文件复制到下述 launch，故该不可变副本是 a7 完整展开命令的权威记录。

在再次核对 Job 为 `366071|RUNNING|hnode02|CPU32|H100×1`、根级 `try_lock_366071` 存在、allocation 内 `after_lock_366071` 存在且动态命令摘要完全匹配后，唯一触发写操作为：

```bash
rm -- /home/penghongen/Feedback/Pocket_Plus/allocations/try_lock_366071
```

- 触发时间：2026-09-01 07:27:23 +08:00。
- attempt：a7；launch 创建时间：2026-09-01 07:27:43 +08:00。
- launch：`/home/penghongen/Feedback/Pocket_Plus/launches/366071/Find_1_job366071_20260901T072743_a7`。
- launch `run_cmd.sh`：8,477 bytes，SHA-256 `5102edab331d149ab3731213caa510cf1e58a52793f991c1d80cc3b104a5c62b`。
- runner 留证 release：`/home/penghongen/Feedback/Pocket_Plus/releases/Pocket_Plus_6ea13863d422/Pocket_Plus`；实际命令显式使用上述 `find1-production-2fc8f1517f07` 隔离根。
- 动力学输出根：`/home/penghongen/Feedback/Pocket_Plus/validation/find1_optimization_dynamics/Find_1_job366071_20260901T072743_a7`。
- 正式开始时间：2026-09-01 07:27:48 +08:00；首条命令为 AUTO `CPC1/Find_1_trunk` controlled 轨迹，checkpoint SHA-256 为 `8b00e3def507119a8e39473d64533617c340922146a392ed1defc0e36547e770`。
- 启动后根级 `try_lock_366071` 已被消费，`after_lock_366071` 保持存在；没有创建、删除或修改 `kill_lock`，也没有释放 H100。

截至本节写入时，a7 正在首条真实轨迹中运行，输出根已出现 `identity.txt`、`gate.log` 与专属临时旁车；尚无 `_FAILED` 或 `_COMPLETE`。后续只在轨迹完成、门禁失败/通过或锁状态变化等明确事件补录，不记录无变化轮询。
