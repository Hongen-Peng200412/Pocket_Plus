# Find_1 训练与历史续训执行记录

本记录对应 `文档/规划文档/Find_1训练与历史续训.md`。它保存实现、部署、资源接管、训练运行与故障处理的可追溯事实；没有状态变化的周期检查不写成流水账。

## Git 与本地实现坐标

- 本地生产工作树：`C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra`。
- 实现分支：`codex/find1-training`。
- 开始实现的 `Learn/CUMULATIVE`：`275fcec06ad088e7c8ebf5f52224a68e0dabac34`。
- 跨节点 DDP 实现提交：`1d231f2`，提交说明为“补齐跨节点多卡 DDP 启动链路”。
- 跨节点 DDP 回归：基础设施与配置 45 项通过；训练侧 DDP、checkpoint 和实验目录 6 项通过；10 个 Shell 文件通过 `bash -n`；`git diff --check` 通过。

## 隔离服务器任务根部署

- 第一次上传尝试：2026-08-31 22:52:00.359（Asia/Shanghai）。
- 第二次上传开始：2026-08-31 22:52:40.798（Asia/Shanghai）；上传、权限和行尾处理在 22:53:07 前完成。
- 哈希与 Shell 语法核验：2026-08-31 22:53:23.179—22:53:32.311（Asia/Shanghai）。
- 服务器任务根：`/home/penghongen/My_Project/Pocket_Plus_Find1`。
- 服务器反馈根：`/home/penghongen/Feedback/Pocket_Plus_Find1`。
- 部署方式：使用本机项目 SSH helper 建立目录，再使用不含 `--delete` 的 `rsync -av` 上传当前生产工作树；共享 `/home/penghongen/My_Project/Pocket_Plus` 未被改写。
- 第一次 `rsync` 因本机子进程的 `PATH` 中没有 `sshpass` 而在发送前失败。远端只建立了空目录，没有收到项目文件。把 `D:\msys64\usr\bin` 加入该调用的 `PATH` 后，以同一非删除式命令重试成功。
- 上传成功后执行 `chmod -R 755`，并只对服务器任务根内的 `.sh` 与 `.sbatch` 文件去除 CRLF 行尾。
- 这两次历史上传命令的 `rsync` 远端 Shell 使用了 `StrictHostKeyChecking=accept-new`。该参数不符合当前项目要求的 `StrictHostKeyChecking=yes`；此次没有发生主机密钥变化。后续部署必须使用项目统一 helper 的严格主机密钥检查，不复用这里的历史偏差。

### 原始部署命令

第一次调用先建立服务器目录，再尝试上传；上传在发送文件前失败：

```powershell
$remoteRoot = '/home/penghongen/My_Project/Pocket_Plus_Find1'; & '.\与服务器交互\other\Invoke-PasswordSsh.ps1' -Command "mkdir -p '$remoteRoot'"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $msysBin = 'D:\msys64\usr\bin'; $rsync = Join-Path $msysBin 'rsync.exe'; $cygpath = Join-Path $msysBin 'cygpath.exe'; $sshpassFile = Join-Path $env:USERPROFILE '.ssh\pocket_plus_sshpass.txt'; $localPosix = (& $cygpath -u (Get-Location).Path).Trim(); $passPosix = (& $cygpath -u $sshpassFile).Trim(); $env:MSYS2_ARG_CONV_EXCL = '*'; $remoteShell = "sshpass -f $passPosix ssh -p 10022 -o StrictHostKeyChecking=accept-new -o WarnWeakCrypto=no -o PreferredAuthentications=password -o PubkeyAuthentication=no -o NumberOfPasswordPrompts=1"; & $rsync -av --exclude='.git' --exclude='.git/' --exclude='__pycache__/' --exclude='*.pyc' --exclude='.pytest_cache/' --exclude='.ruff_cache/' -e $remoteShell "$localPosix/" "penghongen@10.102.33.220:${remoteRoot}/"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & '.\与服务器交互\other\Invoke-PasswordSsh.ps1' -Command "set -euo pipefail; chmod -R 755 '$remoteRoot'; find '$remoteRoot' -type f \( -name '*.sbatch' -o -name '*.sh' \) -exec sed -i 's/\r$//' {} +"
```

第二次调用只增加了 MSYS2 工具目录到本次进程的 `PATH`，随后上传成功：

```powershell
$remoteRoot = '/home/penghongen/My_Project/Pocket_Plus_Find1'; $msysBin = 'D:\msys64\usr\bin'; $rsync = Join-Path $msysBin 'rsync.exe'; $cygpath = Join-Path $msysBin 'cygpath.exe'; $sshpassFile = Join-Path $env:USERPROFILE '.ssh\pocket_plus_sshpass.txt'; $localPosix = (& $cygpath -u (Get-Location).Path).Trim(); $passPosix = (& $cygpath -u $sshpassFile).Trim(); $env:Path = "$msysBin;$env:Path"; $env:MSYS2_ARG_CONV_EXCL = '*'; $remoteShell = "sshpass -f $passPosix ssh -p 10022 -o StrictHostKeyChecking=accept-new -o WarnWeakCrypto=no -o PreferredAuthentications=password -o PubkeyAuthentication=no -o NumberOfPasswordPrompts=1"; & $rsync -av --exclude='.git' --exclude='.git/' --exclude='__pycache__/' --exclude='*.pyc' --exclude='.pytest_cache/' --exclude='.ruff_cache/' -e $remoteShell "$localPosix/" "penghongen@10.102.33.220:${remoteRoot}/"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & '.\与服务器交互\other\Invoke-PasswordSsh.ps1' -Command "set -euo pipefail; chmod -R 755 '$remoteRoot'; find '$remoteRoot' -type f \( -name '*.sbatch' -o -name '*.sh' \) -exec sed -i 's/\r$//' {} +"
```

哈希与 Shell 语法核验使用的原始命令为：

```powershell
$files = @('训练与运行/submit_task.sh','训练与运行/sbatch/task.sbatch','训练与运行/runtime/allocation_runner.sh','训练与运行/runtime/create_launch.sh','训练与运行/runtime/launch_training_python.sh'); foreach ($file in $files) { $hash = (Get-FileHash -Algorithm SHA256 -LiteralPath $file).Hash.ToLower(); Write-Output "$hash  $file" }; & '.\与服务器交互\other\Invoke-PasswordSsh.ps1' -Command "set -euo pipefail; cd /home/penghongen/My_Project/Pocket_Plus_Find1; sha256sum '训练与运行/submit_task.sh' '训练与运行/sbatch/task.sbatch' '训练与运行/runtime/allocation_runner.sh' '训练与运行/runtime/create_launch.sh' '训练与运行/runtime/launch_training_python.sh'; bash -n '训练与运行/submit_task.sh' '训练与运行/sbatch/task.sbatch' '训练与运行/runtime/allocation_runner.sh' '训练与运行/runtime/create_launch.sh' '训练与运行/runtime/launch_training_python.sh'"
```

### 跨节点控制文件哈希

下列 SHA-256 在本地与服务器逐项一致，服务器端 `bash -n` 同时通过：

| 文件 | SHA-256 |
| --- | --- |
| `训练与运行/submit_task.sh` | `f4e8694bfef3a1a61175d651aafdc55f89044acbc0da2cee4462b1b77d7836a7` |
| `训练与运行/sbatch/task.sbatch` | `badab7d448a96249dd52be8959da6bf330041c47dcf798b57193ed25056c5d78` |
| `训练与运行/runtime/allocation_runner.sh` | `46a8e2ae510f37d9fa2f7dbed756da686f13e5c7ffec3220ec102b982e42a0ef` |
| `训练与运行/runtime/create_launch.sh` | `0c08b2bc799712a7596dea7c641464f7965fbe0943950ee94bdc0ea773e867b5` |
| `训练与运行/runtime/launch_training_python.sh` | `a5b766cfd92a9371413a845e09d38a993a2c0c73366725840d77903b75c56118` |

### 双节点提交 dry-run

监视子代理在服务器隔离根使用 `SBATCH_BIN=/bin/echo` 做了无副作用 dry-run。正式命令形状为：

```bash
bash /home/penghongen/My_Project/Pocket_Plus_Find1/训练与运行/submit_task.sh \
  --task-root /home/penghongen/My_Project/Pocket_Plus_Find1 \
  --feedback-root /home/penghongen/Feedback/Pocket_Plus_Find1 \
  --sh Find_1.sh \
  --job-name Find_1_resume_multinode \
  --resource a800 \
  --nodes 2 \
  --gpus 1 \
  --cpus C \
  --nodelist gnode09,gnode10 \
  --multi-node-ddp \
  --pre_hold \
  --after_hold
```

其中 `C` 必须在窗口出现时根据两个节点的实时状态重新计算，不能沿用早先快照。

## 资源监视事件

### 2026-08-31 22:48 A800 首个即时快照

- `nvlink` 仍有等待作业 `366147`，因此接管窗口未出现。
- `350302`：RUNNING，`gnode09`，16 CPU，1×A800；存在 `try_lock` 与 `after_lock`。
- `356946`：RUNNING，`gnode10`，16 CPU，1×A800；正在执行，仅存在 `after_lock`。
- `gnode09`：CPU 已分配 46/48，GPU 已分配 4/8。
- `gnode10`：CPU 已分配 42/48，GPU 已分配 7/8。
- 仅用于校验公式的当时估算：取消两个目标作业后，两节点预测空闲 CPU 分别为 18 与 22，故 `F=18`、`C=17`、每 rank workers=16。该值不作为未来提交参数。
- 没有提交、取消、写锁或释放任何作业。

### 2026-09-01 01:41—01:51 H100 Job 366071 获得资源并回到 try_lock

#### 作业身份与动作边界

- Slurm 记录的准确开始时间为 2026-09-01 01:41:37（Asia/Shanghai）。监视器在 01:41:51 首次观察到 Job 366071 从 PENDING 进入 RUNNING。
- `scontrol show job -dd 366071` 在 01:42:18 核实：`JobState=RUNNING`、`NodeList=hnode02`、`NumNodes=1`、`NumCPUs=32`、`CPUs/Task=32`、`JOB_GRES=gpu:h100:1`、`GRES=gpu:h100:1(IDX:0)`。提交入口仍是共享任务根 `/home/penghongen/My_Project/Pocket_Plus/训练与运行/sh/Find_1.sh`，并启用了 `--after_hold 1`。
- 正式 PDB-centric-1 实现和 AUTO 动力学门禁尚未验收。监视器因此只对 Job 366071 创建具名 `kill_lock_366071`，没有操作任何其他 Job 或锁。
- `kill_lock` 的创建发生在 01:42:18 之后、allocation runner 于 01:43:14 记录消费之前。原始操作没有返回可证明的更精确 touch 时间，本记录不伪造时间点。
- allocation runner 在 01:43:14 记录向旧 attempt a1 的进程组 257942 发送终止信号；a1 退出码为 137。runner 随后创建根级 `try_lock_366071`，没有启动 attempt a2。
- 2026-09-01 01:51:57 的独立复核确认 Job 仍为 RUNNING，资源为 hnode02 的 1×H100 与 32 CPU。`try_lock` 和 `after_lock` 均保留，`kill_lock` 已被 watcher 消费，`pre_lock` 不存在。
- 同期 H100 分区仍有他人作业 366156 PENDING，hnode01 与 hnode02 的节点级 `GRES_USED` 都是 3/3，因此不满足双 H100 切换条件，没有提交或取消其他 H100 作业。

#### 旧 attempt a1 产物身份

此次 a1 只用于解释为何必须立刻回到 `try_lock`，不属于新 PDB-centric-1 的科学结果：

- release：`/home/penghongen/Feedback/Pocket_Plus/releases/Pocket_Plus_5b8ca0631e1c/Pocket_Plus`。
- launch：`/home/penghongen/Feedback/Pocket_Plus/launches/366071/Find_1_job366071_20260901T014214_a1`。
- run：`/home/penghongen/Feedback/Pocket_Plus/logs/AdaLigand_Stage1_pdb_centric-Find_1-CPC1/Find_1-CPC1____Find_1_job366071_20260901T014214_a1_CPC1`。
- allocation 日志：`/home/penghongen/Feedback/Pocket_Plus/allocations/366071/out` 与 `err`。
- run 配置：`config.yaml` SHA-256 为 `3a0d342ece0e09214d084c8a0256cd6fe1c68a8102bb168ff655dfb43404da0a`；`train.yaml` SHA-256 为 `4ca1c9b05f6ba313df92ba43736089d0303fce532f02813a108c5d1aade422d7`。
- 动态命令：`/home/penghongen/Feedback/Pocket_Plus/allocations/366071/run_cmd_366071.sh`，SHA-256 为 `feef2e59312130978c1dc298c51915a6c19259b067fead9f29e57327259db7b3`。

动态命令的完整内容为：

```bash
#!/usr/bin/env bash
set -euo pipefail

# 本文件可以在 pre_lock 或 try_lock 存在、任务未运行时编辑。
# TASK_PROJECT_ROOT 会在每次执行前指向该次刚创建的 release。
exec bash "${TASK_PROJECT_ROOT}"/训练与运行/sh/Find_1.sh
```

a1 已创建运行目录、配置和 `src_snapshot`，日志显示 Dataset 仍是旧 `stage1_find`，随后在模型实例化阶段收到 `kill_lock`。它没有进入正式优化步骤，也没有产生可用于本轮结论的 checkpoint。

#### 监视器执行的准确远端载荷

01:41:51 的状态变化轮询：

```bash
printf '[timestamp]\n'; date -Is; printf '[job_366071]\n'; squeue -j 366071 -h -o '%i|%T|%M|%D|%R|%C|%b|%j'; printf '[h100_pending]\n'; squeue -p h100 -t PD -h -o '%i|%T|%M|%D|%R|%C|%b|%j'; printf '[h100_nodes]\n'; sinfo -N -p h100 -h -O 'NodeList:16,StateLong:16,CPUsState:24,CPUsLoad:12,Gres:28,GresUsed:52'
```

01:42:18 的身份核对：

```bash
date -Is; squeue -j 366071 -h -o '%i|%T|%M|%D|%R|%C|%b|%j'; scontrol show job -dd 366071; squeue --steps -j 366071 -h -o '%i|%T|%M|%N|%C|%j'; allocation=/home/penghongen/Feedback/Pocket_Plus/allocations/366071; if [[ -d "$allocation" ]]; then find "$allocation" -maxdepth 2 -type f -printf '%TY-%Tm-%TdT%TH:%TM:%TS|%p|%s bytes\n' | sort; find "$allocation" -maxdepth 1 -type f \( -name '*lock*' -o -name 'run_cmd*' \) -printf '%f|%s bytes|%TY-%Tm-%TdT%TH:%TM:%TS\n' | sort; for f in "$allocation"/run_cmd_366071.sh "$allocation"/run_cmd.sh; do if [[ -f "$f" ]]; then printf 'path=%s\n' "$f"; sed -n '1,120p' "$f"; fi; done; fi
```

该 Slurm 版本不接受 `squeue --steps` 中的 `%T/%C` 格式；同一载荷中的其他核对均成功，不影响作业身份判断。

紧接身份核对后、创建 `kill_lock` 前的第二次只读核对：

```bash
allocation=/home/penghongen/Feedback/Pocket_Plus/allocations/366071; sed -n '1,200p' "$allocation/out"; sed -n '1,200p' "$allocation/err"; find /home/penghongen/Feedback/Pocket_Plus -maxdepth 4 -type f \( -name 'release.json' -o -name 'launch.json' \) -newermt '2026-09-01 01:40:00' -printf '%TY-%Tm-%TdT%TH:%TM:%TS|%p\n' | sort
```

唯一改变服务器状态的远端载荷如下：

```bash
set -euo pipefail; job_id=366071; allocation=/home/penghongen/Feedback/Pocket_Plus/allocations/366071; resolved=$(readlink -f -- "$allocation"); [[ "$resolved" == "$allocation" ]]; state=$(squeue -j "$job_id" -h -o '%T|%N'); [[ "$state" == RUNNING\|hnode02 ]]; [[ -f "$allocation/after_lock_$job_id" ]]; [[ ! -e "$allocation/try_lock_$job_id" ]]; [[ ! -e "$allocation/kill_lock_$job_id" ]]; printf '[precondition] job=%s state=%s allocation=%s after_lock=present\n' "$job_id" "$state" "$resolved"; touch -- "$allocation/kill_lock_$job_id"; printf '[kill_lock_created]\n'; stat -c '%n|%s bytes|%y' "$allocation/kill_lock_$job_id"; for attempt in $(seq 1 36); do sleep 5; kill_state=absent; try_state=absent; after_state=absent; [[ -e "$allocation/kill_lock_$job_id" ]] && kill_state=present; [[ -e "$allocation/try_lock_$job_id" ]] && try_state=present; [[ -e "$allocation/after_lock_$job_id" ]] && after_state=present; printf '[wait_%02d] kill=%s try=%s after=%s\n' "$attempt" "$kill_state" "$try_state" "$after_state"; if [[ "$kill_state" == absent && "$try_state" == present && "$after_state" == present ]]; then exit 0; fi; done; exit 42
```

该载荷的写操作本身成功，runner 也在 01:43:14 消费了 `kill_lock`。但等待循环错误地在 Job 子目录寻找 `try_lock`；实际 `try_lock` 位于 allocations 根，因此循环没有命中并最终使本机 SSH helper 报出空 stderr 异常。这个错误只影响等待与回显，没有第二次写入，也没有触碰其他锁。01:46:23、01:47:23、01:48:29 和 01:50:21 的后续只读检查逐步确认了退出日志、正确的根级 `try_lock`、三份身份目录、动态命令哈希、H100 排队和节点级 GRES。

#### 当前保留锁

| 锁 | 最终状态 | 绝对路径 | mtime |
| --- | --- | --- | --- |
| `pre_lock` | 不存在 | `/home/penghongen/Feedback/Pocket_Plus/allocations/pre_lock_366071` | — |
| `try_lock` | 存在 | `/home/penghongen/Feedback/Pocket_Plus/allocations/try_lock_366071` | 2026-09-01 01:43:14 +08:00 |
| `after_lock` | 存在 | `/home/penghongen/Feedback/Pocket_Plus/allocations/366071/after_lock_366071` | 2026-09-01 01:41:37 +08:00 |
| `kill_lock` | 不存在 | `/home/penghongen/Feedback/Pocket_Plus/allocations/366071/kill_lock_366071` | — |

后续只有在生产实现审查、部署哈希核验和真实 AUTO 动力学门禁通过后，才允许删除 `try_lock_366071` 启动新 attempt。当前不得删除 `after_lock_366071`，也不得释放 H100。

## 2026-09-01 01:51—03:12 审查收口与门禁加固

### 唯一一轮全面审查

- 表达、代码结构与四项 skill 合规全面审查已经完成；修正报告项后的窄口径复核为通过。本轮不再进行第二次全面表达审查。
- 科学逻辑全面审查确认 P0 为零，并报告六项 P1 与一项 P2：轨迹身份可伪造、完整模型独有损失和其他参数组可由 NaN 假通过、固定位置抽样不能证明逐元素等价、自然 recycle 首批分离可形成零证据假通过、历史续训跨目录丢失 `ModelCheckpoint` 状态、规划文档允许门禁前放行 Job `366071`，以及旧 checkpoint 不包含 DataLoader worker 的随机增强状态。
- 针对上述报告项的逻辑窄口径复核已经完成；身份门禁最后补充进程级 nonce、PID 与两个受信源码树摘要后，复核者确认没有残留缺陷。本轮不再扩大为第二次全面逻辑审查。
- 配置数值、V1/V2 validation、一个 AdamW、353/1,241 两组互斥穷尽、每组 0.5 裁剪、无采样条件分支和 Lightning 每个 optimizer step 只调用一次裁剪均经审查确认正确。
- `tests/test_project_task_runner.py` 的 kill_lock 测试仍要求假 `srun` 收到 TERM 后以 143 退出并出现 `try_lock`；两秒宽限只消除 Windows 调度抖动，没有掩盖 TERM 路径。

### 生产动力学门禁修复

- 每条轨迹自动记录进程级 `process_nonce`、PID、目标项目 `src/` 与 `configs/` 的内容 SHA-256、完整解析配置 SHA-256、role、experiment 和提交身份。比较器拒绝相同输入文件、相同项目根、相同进程 nonce、相同 PID、错误 AUTO 提交或错误生产提交，并要求两个项目源码摘要分别等于正式执行前从只读 release 独立核验并记录的预期值。
- 每个体素输出、累计梯度、裁剪前后梯度、参数、参数增量和 AdamW 状态都计算原始 dtype 完整张量字节的 SHA-256，并记录 `all_finite`。正式比较要求 SHA-256 完全相同；这比固定位置抽样或容差比较更严格，任何未抽样位置差异都会直接失败，不需要发布大型完整张量副本。
- 完整模型强制存在 `atom` 与 `pseudo` 两项损失；两项分别调用 `torch.autograd.grad` 测量对 353 个体素参数的回灌，必须各自严格为零，不能由两项梯度相消得到假零。
- 1,241 个其他参数的裁剪前后梯度、参数、参数增量、AdamW `exp_avg`、`exp_avg_sq` 和 state step 全部记录并检查有限性；其他组裁剪后范数必须不超过 0.5。
- 自然轨迹记录实际 recycle 序列。若 AUTO 与完整模型的自然序列分离，必须再让两模型共同重放 AUTO 序列和完整模型序列；两份 replay 比较都通过且其来源 SHA-256 分别对应两份自然 JSON 后，自然归因比较才可通过。第 0 个 microbatch 即分离时也不能靠零个比较项通过。
- 正式门禁由八条轨迹命令和四条比较命令组成，完整顺序位于 `tmp/find1_optimization_dynamics/README.md`；真实 GPU 门禁尚未执行，本记录不提前宣称通过。

### 历史 checkpoint 与不可恢复边界

- 历史隔离分支新增 `ops/find1_historical_resume/rebase_checkpoint.py`。新 attempt 的 node rank 0 在新运行目录复制历史 top-k checkpoint，重写 callback 的 `dirpath`、`best_model_path`、`best_k_models`、`kth_best_model_path` 与 `last_model_path`，再原子发布 `resume_state.ckpt` 和 `resume_state_manifest.json`；其余节点只等待共享产物。
- 真实 Lightning `ModelCheckpoint` 跨目录测试已证明 `best_model_score`、top-k 映射、`kth_value` 和 last 路径完整恢复。旧运行目录保持只读。
- 源 `last.ckpt` 客观上没有 DataLoader worker 的 Python/NumPy RNG 状态，因此无法重建未中断旧进程本应产生的逐位随机旋转序列。本轮不伪造精确恢复，也不改变原训练的随机旋转制度：首轮剩余 BOX 身份按 45,150 个 rank-local batch 连续，旋转由新 worker RNG 继续。该科学边界已经同时写入历史 shell、迁移 manifest 与历史 `训练与运行/README.md`。
- 规划文档已改为：Job `366071` 在门禁前只能由自身 `kill_lock` 回到根级 `try_lock`，生产实现、部署哈希和动力学门禁通过后才可启动正式 PDB-centric-1。

### 本地验证命令与结果

生产工作树在 2026-09-01 03:11 完成最终验证：

```powershell
& 'D:\Anaconda\envs\Pocket_Plus_windows\python.exe' -m pytest -q `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\tests' `
  --ignore='C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\tests\test_stage1_producers.py' `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\tmp\find1_optimization_dynamics\tests'
```

结果为 `367 passed, 11 warnings in 75.05s`。其中包括身份门禁新增的错误源码摘要与相同 PID 回归测试；该测试文件单独复核为 `8 passed in 0.14s`。`tests/test_stage1_producers.py` 在本轮基点即因不存在既有 `src.artifacts` 包而无法收集，不属于 Find_1 差异；其余全部可收集测试已通过。

```powershell
& 'D:\Anaconda\envs\Pocket_Plus_windows\python.exe' -m compileall -q `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\src' `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\tests' `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra\tmp\find1_optimization_dynamics'
& 'C:\Program Files\Git\bin\bash.exe' -n 'C:/Users/15919/Desktop/Pocket_Plus_worktrees/cross_node_ddp_infra/训练与运行/sh/Find_1.sh'
& 'C:\Program Files\Git\bin\bash.exe' -n 'C:/Users/15919/Desktop/Pocket_Plus_worktrees/cross_node_ddp_infra/训练与运行/sh/Find_1_pdb_centric_2.sh'
git -C 'C:\Users\15919\Desktop\Pocket_Plus_worktrees\cross_node_ddp_infra' diff --check
```

Python 编译、两份 Find_1 shell 及通用 runtime/submit shell 语法、差异检查均以退出码 0 完成。

历史隔离工作树执行：

```powershell
& 'D:\Anaconda\envs\Pocket_Plus_windows\python.exe' -m pytest -q `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\find1_historical_resume\tests\test_adaligand_stage1_configs.py' `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\find1_historical_resume\tests\test_find1_historical_resume.py' `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\find1_historical_resume\tests\test_multinode_ddp_runtime.py' `
  'C:\Users\15919\Desktop\Pocket_Plus_worktrees\find1_historical_resume\tests\test_project_task_runner.py'
```

结果为 `48 passed, 15 warnings in 65.19s`；历史 helper 编译、`Find_1.sh`、allocation runner、跨节点 Python launcher 的 Bash 语法和 `git diff --check` 同样以退出码 0 完成。

## AUTO 当前保留最优与参数边界

- AUTO 仓库：`C:\Users\15919\Desktop\AUTO\Pocket_Plus-v3-macro`。
- autoresearch run：`a44e7f6b849c4f34888835aef1f0c191`。
- 当前候选 HEAD：`1c8a4bac5ed27111ab2415c12f13824606f3e001`，RAUNet 多尺度输出候选；尚未产生保留分数，不能称为当前最优。
- 当前保留端点：`2ad913b5ff945739f85c192f6fcd1ff6cceb7883`。
- 当前正式最优：B_trunk，`macro_joint_score=1.6670933207345788`。
- B_trunk 正式训练提交：`7aae1f126ee0c1636a3f14455f8239a072ac28c9`；W&B run `du58fxi4`；checkpoint SHA-256 `8b00e3def507119a8e39473d64533617c340922146a392ed1defc0e36547e770`。
- 实际训练配置：单张 A800、BF16、物理 batch size 6、8 次梯度累积、全局 batch size 48、AdamW、学习率 `5e-5`、全局梯度裁剪 0.5、2,018 个 optimizer steps。
- 在关闭本地缺失的 FlashAttention 后，仅实例化配置用于参数名称审计：完整 Find_1 有 1,594 个可训练参数张量，B_trunk 有 353 个；二者交集与 B_trunk 完全相同。三个正式体素前缀选中 353 个张量，也与该交集完全相同。

### AUTO 精确参考实现与 checkpoint

- B_trunk 精确代码工作树：`C:\Users\15919\Desktop\AUTO_worktrees\find1_b_trunk_reference`，分离 HEAD 为 `7aae1f126ee0c1636a3f14455f8239a072ac28c9`。该工作树只用于动力学对照，不跟随 AUTO 当前候选分支变化。
- 服务器 checkpoint：`/storage/penghongen/tmp/AUTO/TUNE--Find-v3-macro/trials/baseline-7aae1f126ee0-20260828T034121771-b6/training/logs/Pocket_Plus_AUTO_tune_stage1_v3_macro/baseline-7aae1f126ee0-20260828T034121771-b6____baseline-7aae1f126ee0-20260828T034121771-b6/checkpoints/BEST.ckpt`。
- 原始 launch：`/home/penghongen/Feedback/AUTO/Pocket_Plus-v3-macro/launches/356946/Find_1_job356946_20260828T114043_a11`。
- 原始 release：`/home/penghongen/Feedback/AUTO/Pocket_Plus-v3-macro/releases/Pocket_Plus-v3-macro_2eec850361c7/Pocket_Plus-v3-macro`。
- 动力学对照必须在同一张 GPU 上先后执行 AUTO B_trunk 与完整 Find_1 的受控、自然随机和必要的双向反事实 replay。对照尚未运行，因此本记录不提前宣称动力学等价。

## 当前生产实现

### 配置入口

- PDB-centric-1：`configs/dataset/stage1_find_pdb_centric_1.yaml`、`configs/train/stage1_find_pdb_centric_1.yaml`、`configs/experiment/CPC1/Find_1.yaml` 与 `训练与运行/sh/Find_1.sh`。
- PDB-centric-2：`configs/dataset/stage1_find_pdb_centric_2.yaml`、`configs/train/stage1_find_pdb_centric_2.yaml`、`configs/experiment/CPC1/Find_1_pdb_centric_2.yaml` 与 `训练与运行/sh/Find_1_pdb_centric_2.sh`。该入口只接受配置与测试，不在本轮提交训练。
- 两份 Shell 没有按采样模式分支；科学参数由各自 Dataset、训练和实验 YAML 固定。

### 最小分组梯度裁剪

- `src/wrappers/voxel_point_stage1.py` 以三个已核实参数前缀划分体素组, 其余所有 `requires_grad=true` 参数进入另一组; Find_1 专属边界不放入通用 `src/utils/`。
- `src/wrappers/voxel_point_stage1.py` 仍使用一个 AdamW; `find_voxel_point` 模式仅把同一个 optimizer 中的两个互斥参数集合分别执行 0.5 范数裁剪。
- 其他实验默认继续使用 `global`，历史续训分支也保持原全局 0.5 裁剪。
- 真实配置实例化测试得到 1,594 个可训练参数张量，其中体素组 353 个；三个前缀分别选中 6、4、343 个张量。体素参数名称 SHA-256 为 `57cb249c0410369a60c17b09950432a23069c541ac4ddf56e9ef4cf9e9467e2e`，与 AUTO B_trunk 完全一致。

### 验收工具

- `tmp/find1_optimization_dynamics/optimization_trace.py` 负责在真实 Stage1 batch 上记录受控、自然随机或反事实 replay 的 BF16 优化轨迹。
- `tmp/find1_optimization_dynamics/compare_optimization_traces.py` 比较代码与进程身份、请求身份、共同体素输出、共同体素损失、完整张量摘要、两组梯度与裁剪、AdamW 状态和逐步参数增量，并把非有限值、点监督回灌和未经双向 replay 证明的自然 recycle 分离视为硬失败。
- `tmp/find1_optimization_dynamics/README.md` 说明命令、JSON 契约和一次性生命周期。这些代码只在实现分支与服务器验收副本中留证, 实际对照完成后会在最终学习端点前退出活动代码树。

## 历史续训实现

- 隔离工作树：`C:\Users\15919\Desktop\Pocket_Plus_worktrees\find1_historical_resume`。
- 分支：`codex/find1-historical-resume`。
- 历史基点：`1315d301c867c99e2dc0736feffde86e9cd7fa0a`。
- 跨节点基础设施提交：`e312e2c`；完整 checkpoint 恢复与首轮跳批提交：`604608f`；跨节点信号测试稳定化提交：`fb7e6c3`；跨目录 callback 状态迁移提交：`5b4aa5f52d699d904fa82e97e22099047e607e4c`。历史隔离工作树在提交后保持干净。
- `last.ckpt` 记录 `epoch=0`、`global_step=11287`，首轮 rank-local 已完成 45,150 个 microbatch。恢复时只在第一个 epoch 跳过这些已完成 batch，之后恢复完整采样；模型、optimizer、scheduler、callback 与 Lightning 循环状态通过迁移后的 `resume_state.ckpt` 恢复。
- 原运行使用双卡、物理 batch size 6、梯度累积 4；`45150=11287×4+2`。checkpoint 没有保存最后两个累积梯度，按用户选择接受其丢失，不补算、不修改 global step。
- 历史续训相关 48 项测试、Python 编译、Shell 语法和差异检查已经通过。正式双节点任务尚未获得，因此没有 release、launch 或新训练产物可记录。

## 后续事件模板

每次正式启动或重试必须新增一个小节，并至少写明：

- 服务器准确开始与结束时间、耗时；
- 完整命令；
- Slurm Job、节点、GPU、CPU、workers；
- `pre_lock`、`try_lock`、`after_lock`、`kill_lock` 的实际动作；
- release、launch、run_cmd 路径和 SHA-256；
- Git 提交、配置、checkpoint 和代码哈希；
- W&B run、训练日志、checkpoint 与 validation 产物目录；
- 异常、修复、科学契约变化和复核结果。
