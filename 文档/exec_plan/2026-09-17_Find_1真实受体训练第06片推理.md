# Find_1 真实受体训练第 6 片推理

本文只记录固定 50 片训练 PDB 中用户第 6 片的正式推理、产物与尝试历史。用户片号 6 对应 CLI `shard-index=5`。共享科学契约和跨片状态见 `文档/exec_plan/2026-09-16_Find_1真实受体训练分片推理总日志.md`。

## 当前状态

- 第 5 片 275 个成员已经通过全量产物与数组契约验收，Job `378587` 已停回 `try_lock_378587`；第 6 片的启动前置条件满足。
- 第 6 片正式 attempt a3 已成功完成 probability、冻结阈值 F2 blobs、centered 与冻结 Gaussian score-only；275/275 个成员通过全量产物和数组契约验收。
- Job `378587` 已停回生效的 allocation 根目录 `try_lock_378587`；`after_lock_378587` 保留，`kill_lock_378587` 不存在，也未使用 `scancel`。GPU 空闲且没有残留推理进程。
- 正式产物根为 `/storage/penghongen/AdaLigand_stage1_inference/Find_1/真实受体/artifacts/Find_1/train`。该根目录恰好包含固定第 1—6 片的 1,650 个成员，与第 7 片交集为 0。

## 产物与冻结输入

- 训练清单：`/storage/penghongen/AdaLigand/Ori_Data/stage1_preparation_box_pool_3/split/pdb_split/train.json`，SHA-256 为 `8e7f975ea49ee94e6819f2b35bf596c9bc4abacac9afc0aaebe2018b90d94d00`。
- 第 6 片 preflight：`/storage/penghongen/tmp/find1_real_receptor_train_shard_06_20260917/preflight.json`，SHA-256 为 `90762216b324d985f9ee07ed72cb5d28cd77ab753e736e4b45e4ea60ae9816d2`。
- 第 6 片含 275 个唯一 PDB，与第 1—5 片无交集；成员序列 SHA-256 为 `73e7ddf1cf621d97852d2065e66cf5efb8b213a711d86f60c7d3306cabc4ab9a`。preflight 与完整 50 片固定身份清单 `/storage/penghongen/tmp/find1_real_receptor_train_shards_20260913/preflight.json` 的全部分片逐项一致。
- checkpoint、resolved config、F2 语义参数和 Gaussian 参数继续使用第 1—5 片相同的冻结文件及 SHA-256；不重新调参。
- 每个成员依次生成 `probability/probability_map.npz`、`blobs/F2_blobs.npz` 和 `centered/F2_centered.npz`，最后由 score-only 在 centered 文件中原子写入 `score/selected`。

## 正式运行命令

Job `378587` 的动态命令仅包含以下正式调用，不混入门控或只读核查命令：

```bash
exec bash "/home/penghongen/Feedback/Pocket_Plus/releases/Pocket_Plus_268637d112a5/Pocket_Plus/训练与运行/sh/infer/find1_real_receptor_train_shard_06.sh" 6
```

## 门控与测试命令

以下操作不属于正式推理命令：

- 本地 Shell 语法门控：对 `find1_real_receptor_train_shard_06.sh` 执行 `bash -n`。
- 本地推理测试：在 `Pocket_Plus_windows` 环境执行 `python -m pytest tests/inference -q`，结果为 59 项全部通过。
- 服务器身份门控：重新按 seed `3407` 和 `shuffled[index::50]` 构造完整 50 片，逐片比对既有固定清单，并单独核对第 6 片的数量、唯一性、与第 1—5 片的无交集关系及成员摘要。

## 状态快照

### 2026-09-17 13:17 第 6 片身份门控通过

- 用户第 6 片固定为 CLI `shard-index=5`，含 275 个唯一 PDB；成员序列 SHA-256 为 `73e7ddf1cf621d97852d2065e66cf5efb8b213a711d86f60c7d3306cabc4ab9a`。
- 该片与第 1—5 片的 1,375 个成员无交集，并与完整 50 片固定定义中的第 6 片逐项一致。产物根启动前已有 1,375 个第 1—5 片目录，第 6 片的 275 个目录均不存在。
- preflight 已原子写入服务器临时目录，正式产物仍未启动。Job `378587` 继续由 `try_lock_378587` 阻挡，`after_lock_378587` 保留。

### 2026-09-17 13:42—13:45 Learn release 与动态命令就绪

- 实现端点 `470317f` 与 Learn 端点 `9bbd249` 的 Git tree 均为 `d4c151a7129b872d83ac1e8810ca3352503613d7`；`Learn/CUMULATIVE` 已快进到 `9bbd2494ef54343967643b7d58c0809508207965`。学习历史依次记录说明、正式入口和测试，边界提交为 `1906e6d` 的 `——————开始训练集真实受体推理——————`。
- 两轮全面独立审查和问题窄复核均已批准；Learn 端点的 Shell 语法与 `tests/inference` 59 项测试全部通过。
- 从干净 Learn 工作树安全同步后，正式 release 为 `/home/penghongen/Feedback/Pocket_Plus/releases/Pocket_Plus_268637d112a5/Pocket_Plus`，内容 SHA-256 为 `268637d112a5718dd775c51c049c0b931f98ba058f36ca5322fcc85b448d8f9c`。第 6 片入口 SHA-256 为 `a33226b73ad83430e7000cc3decd1b3c4092808f4050d56d8b52bc5667d756dd`。
- 旧动态命令按 SHA-256 `70b02f7ad33dc53246dadc64809ea9ca600274ce1571931f3735c1bcba772e46` 保存在 `/storage/penghongen/tmp/find1_real_receptor_train_shard_06_20260917/run_cmd_378587.before_find1_shard_06.sh`。新动态命令已在 allocation 目录内原子发布，SHA-256 为 `07c2d07b40dd8373c13a4e8507dd263e9b56372375039c9a15f5a38241788b59`。
- 发布后 `try_lock_378587` 仍存在，`after_lock_378587` 保留，GPU 空闲且没有推理进程；因此动态命令尚未执行。

### 2026-09-17 13:54—13:56 attempt a3 正式启动并进入稳定前向

- 正式 launch 为 `/home/penghongen/Feedback/PocketXMol/launches/378587/train_docking_job378587_20260917T135451_a3`；其中 `run_cmd.sh` 与已冻结动态命令逐字一致，实际调用 release `Pocket_Plus_268637d112a5` 的第 6 片入口。
- probability 进程明确使用固定 checkpoint、resolved config、真实受体训练清单、`--shard-count 50 --shard-index 5` 和 `stage1_v3_h100_32cpu.yaml`。启动约 101 秒时，H100 显存占用 67,732 MiB、瞬时利用率 100%，Python 进程 CPU 使用率约 243%。
- 当前正在处理首个 PDB，275 个成员中尚未出现完成标记；launch 没有严格错误，生效的 `try_lock_378587` 不存在，`after_lock_378587` 保留，`kill_lock_378587` 不存在。
- 13:58 首个成员目录及其 `probability_map.npz` 已出现；当时的监控探针误查了 `probability/_COMPLETE`，而正式契约中的完成标记实际位于 `status/probability/_COMPLETE`，因此不使用该次错误路径的标记计数。H100 显存 67,790 MiB、利用率 96%，Python 进程 CPU 使用率约 546%。运行身份与锁状态没有变化，任务进入首轮 60 分钟静默守护。

### 2026-09-17 14:59—15:00 首轮静默守护结束

- 按正式状态路径复核后，probability 已完成 14/275；对应 14 个 `probability_map.npz` 与 14 个 `status/probability/_COMPLETE` 一一匹配。F2 blobs 和 F2 centered 仍为 0/275，符合顺序执行边界。
- H100 显存 67,820 MiB、瞬时利用率 100%，probability 进程 CPU 使用率约 530%；release、CLI `shard-index=5`、主进程和锁状态均未漂移。
- allocation `err` 中唯一的 `Killed` 文本属于 2026-09-16 的旧训练 attempt；attempt a3 启动时该文件大小为 7,170 bytes，之后没有新增严格错误。后续监控只检查 a3 在 `out` 中的片段和 `err` 的新增字节。任务继续 90 分钟静默守护。

### 2026-09-17 16:32 第二轮静默守护结束

- probability 已完成 37/275，37 个概率图和正式完成标记一一匹配；F2 blobs 与 centered 均仍为 0/275。
- H100 显存 67,834 MiB、瞬时利用率 100%，probability 进程 CPU 使用率约 538%。attempt a3 的 `out` 片段和 `err` 新增字节中严格错误均为 0；release、CLI `shard-index=5` 和三类锁状态未漂移。
- 任务持续产生产物，继续 90 分钟静默守护。

### 2026-09-17 18:03 第三轮静默守护结束

- probability 已完成 50/275，概率图和正式完成标记继续一一匹配；F2 blobs 与 centered 均仍为 0/275。不同 PDB 的三维网格规模不同，本窗口完成数低于上一窗口，但产物计数持续增长。
- H100 显存 67,842 MiB、瞬时利用率 100%，probability 进程 CPU 使用率约 537%；attempt a3 严格错误为 0，release、CLI `shard-index=5`、主进程和锁状态未漂移。
- 继续 90 分钟静默守护。

### 2026-09-17 19:34 第四轮静默守护结束

- probability 已完成 79/275，概率图和正式完成标记一一匹配；F2 blobs 与 centered 均仍为 0/275。
- H100 显存 67,842 MiB、瞬时利用率 100%，probability 进程 CPU 使用率约 539%；attempt a3 严格错误为 0，release、CLI `shard-index=5`、主进程和锁状态未漂移。
- 本窗口新增 29 个完整成员，任务稳定推进，继续 90 分钟静默守护。

### 2026-09-18 06:10 第十一轮静默守护结束

- probability 已完成 247/275，概率图和正式完成标记一一匹配；F2 blobs 与 centered 均仍为 0/275，余下 28 个成员。
- probability 进程 CPU 使用率约 538%，H100 显存仍占用 67,842 MiB；检查瞬间利用率为 0%，对应成员间的数据切换，不是进程退出。attempt a3 严格错误为 0，release、CLI `shard-index=5` 和锁状态未漂移。
- 为接近阶段切换，改用 60 分钟静默守护。

### 2026-09-18 07:10 第十二轮静默守护结束

- probability 已完成 269/275，概率图和正式完成标记一一匹配；F2 blobs 与 centered 均仍为 0/275，只余 6 个成员。
- H100 显存 67,842 MiB、瞬时利用率 100%，probability 进程 CPU 使用率约 537%；attempt a3 严格错误为 0，release、CLI `shard-index=5`、主进程和锁状态未漂移。
- 当前临近 probability → F2 blobs 关键阶段切换，进入短间隔连续核查。
- 07:21 probability 已完成 274/275，仅余最后 1 个成员；H100 利用率 100%，attempt a3 严格错误仍为 0。继续在阶段边界短间隔核查。

### 2026-09-18 07:27 probability 与 F2 blobs 完成，进入 centered

- 275/275 个成员均具备 `probability_map.npz` 与 `status/probability/_COMPLETE`；冻结阈值 F2 blobs 也已完成 275/275，`F2_blobs.npz` 与 `status/F2_blobs/_COMPLETE` 一一匹配。
- 正式入口已按顺序进入 centered。进程命令明确包含固定 checkpoint、真实受体训练清单、`alpha=2`、`forward_min_voxels=8`、`--continue-on-blob-exceed`、`--shard-count 50 --shard-index 5` 和冻结 H100/32 CPU 配置。
- centered 启动约 50 秒时占用 H100 显存 43,202 MiB，瞬时利用率为 0%，对应模型与数据初始化；attempt a3 严格错误为 0，锁状态未漂移。进入 centered 启动短间隔核查。
- 07:33 centered 已完成 16/275，`F2_centered.npz` 与 `status/F2_centered/_COMPLETE` 一一匹配；H100 显存 46,208 MiB、利用率 76%，centered 进程 CPU 使用率约 277%。新阶段已稳定，进入 60 分钟静默守护。

### 2026-09-18 09:20—09:31 正式流程完成并通过全量验收

- attempt a3 已成功退出并停回生效的 `try_lock_378587`。275/275 个成员均具备 probability、冻结阈值 F2 blobs、centered、冻结 Gaussian `score/selected` 和三个正式完成标记；attempt a3 的 `out` 片段与 `err` 新增字节中严格错误均为 0。
- 全量只读验收使用 release `Pocket_Plus_268637d112a5` 的正式 blobs 与 Gaussian 函数逐项复算，确认 11,652 个来源 blob、11,319 个 centered 候选、2,600,862 个完整来源体素、1,824,843 个归档来源体素、3,623,997 个原子、11,510,426 个锚点、11 个超框候选、0 个 blob 数量超过 1,000 的 PDB 和 8,159 个最终选择均符合候选顺序、四组 offsets、稀疏来源映射、48³ 数组与冻结评分契约。
- 最终报告为 `/storage/penghongen/tmp/find1_real_receptor_train_shard_06_20260917/final_verification.json`，SHA-256 为 `bad622fa678b29e107d995ebbd708241814307a24351e4d54856a35f72b56cec`。报告冻结记录训练清单、checkpoint、resolved config、F2 语义参数、Gaussian 参数、动态命令和 preflight 的实际 SHA-256。
- 收口集合门控确认 train 产物根的 1,650 个直接成员目录恰好等于 seed `3407` 固定第 1—6 片的并集，与第 7 片交集为 0。最终 GPU 显存 1 MiB、利用率 0%，没有残留推理进程；`pre_lock=false`、`try_lock=true`、`kill_lock=false`、`after_lock=true`。
- 第 6 片至此完成；未扩大到第 7 片，也未释放 Job `378587` 的 H100 allocation。

### 2026-09-17 21:05 第五轮静默守护结束

- probability 已完成 105/275，概率图和正式完成标记一一匹配；F2 blobs 与 centered 均仍为 0/275。
- H100 显存 67,842 MiB、瞬时利用率 100%，probability 进程 CPU 使用率约 540%；attempt a3 严格错误为 0，release、CLI `shard-index=5`、主进程和锁状态未漂移。
- 本窗口新增 26 个完整成员，任务稳定推进，继续 90 分钟静默守护。

### 2026-09-18 01:37 第八轮静默守护结束

- probability 已完成 168/275，概率图和正式完成标记一一匹配；F2 blobs 与 centered 均仍为 0/275。
- H100 显存 67,842 MiB、瞬时利用率 100%，probability 进程 CPU 使用率约 541%；attempt a3 严格错误为 0，release、CLI `shard-index=5`、主进程和锁状态未漂移。
- 本窗口新增 19 个完整成员，任务持续推进，继续 90 分钟静默守护。

### 2026-09-18 03:08 第九轮静默守护结束

- probability 已完成 196/275，概率图和正式完成标记一一匹配；F2 blobs 与 centered 均仍为 0/275。
- H100 显存 67,842 MiB、瞬时利用率 100%，probability 进程 CPU 使用率约 540%；attempt a3 严格错误为 0，release、CLI `shard-index=5`、主进程和锁状态未漂移。
- 本窗口新增 28 个完整成员，任务稳定推进，继续 90 分钟静默守护。

### 2026-09-18 04:39 第十轮静默守护结束

- probability 已完成 225/275，概率图和正式完成标记一一匹配；F2 blobs 与 centered 均仍为 0/275，余下 50 个成员。
- H100 显存 67,842 MiB、瞬时利用率 100%，probability 进程 CPU 使用率约 539%；attempt a3 严格错误为 0，release、CLI `shard-index=5`、主进程和锁状态未漂移。
- 本窗口新增 29 个完整成员，任务稳定推进，继续 90 分钟静默守护。

### 2026-09-17 22:35 第六轮静默守护结束

- probability 已完成 123/275，概率图和正式完成标记一一匹配；F2 blobs 与 centered 均仍为 0/275。
- H100 显存 67,842 MiB、瞬时利用率 100%，probability 进程 CPU 使用率约 540%；attempt a3 严格错误为 0，release、CLI `shard-index=5`、主进程和锁状态未漂移。
- 本窗口新增 18 个完整成员，任务持续推进，继续 90 分钟静默守护。

### 2026-09-18 00:06 第七轮静默守护结束

- probability 已完成 149/275，概率图和正式完成标记一一匹配；F2 blobs 与 centered 均仍为 0/275。
- H100 显存 67,842 MiB、瞬时利用率 100%，probability 进程 CPU 使用率约 542%；attempt a3 严格错误为 0，release、CLI `shard-index=5`、主进程和锁状态未漂移。
- 本窗口新增 26 个完整成员，任务稳定推进，继续 90 分钟静默守护。

## 之前的尝试与中断历史

- 13:45 首次发送启动信号时，门控脚本按 Job 子目录检查了一个不生效的同名 try lock；旧版四锁执行器实际使用 allocation 根目录的 `try_lock_378587`，因此该操作没有启动 attempt、没有运行推理，也没有写入正式产物。
- 13:54 在只读核对四锁执行器源码和两个候选路径后，删除唯一生效的根目录 try lock，attempt a3 随即正常启动。该路径修正不改变 release、正式命令、科学参数或产物契约。
