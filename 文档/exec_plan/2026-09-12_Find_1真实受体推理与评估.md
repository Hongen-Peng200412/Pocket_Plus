# Find_1 真实受体推理与评估执行记录

本文记录 `Find_1` 模型在真实受体数据上的 Stage1 校准、选参与 held-out 测试，包括冻结模型来源、正式命令、Slurm 资源、产物目录、门控证据和最终结果。任务依据 `talk/global/global_8.30.md` 的“小改推理管线”和“训练 Find_1”背景执行，并沿用 `文档/exec_plan/2026-09-02_unet_c1三种采样模型推理与测试.md` 已冻结的 `unet_c1` 比较口径。

本任务不继续训练 `Find_1`，不动态选择更晚 checkpoint，也不改变 Stage1 指标公式。基础打分与 Gaussian 打分使用同一模型、同一 calibration 概率图和同一 `test_0` 概率图；`test_1` 只从 `test_0` 的逐 PDB 评估事实按原顺序派生，不再次执行模型前向。

## 科学口径

| 流水线 | 语义阈值 | 候选评分 | 选参目标 | 正式测试 |
| --- | --- | --- | --- | --- |
| 基础打分 | 在 calibration 上最大化 PDB 等权 macro semantic F1 | `source_probability_mean` | `objective_beta=1`，最大化 macro semantic F1、coverage F1@0.3 与 one-to-one F1@0.3 之和 | 使用冻结的 F1 semantic 与 F1 basic 参数评估 `test_0` |
| Gaussian 打分 | 在同一 calibration 概率图上最大化 PDB 等权 macro semantic F2 | 对 F2 blobs 执行 centered 前向后使用 Find Gaussian 分数 | `objective_beta=1`，仍最大化上述三项 macro F1 之和 | 使用冻结的 F2 semantic 与 F2 Gaussian 参数评估 `test_0` |

两条流水线均固定 `prefiltered_min_voxel=8`；centered 使用 `forward_min_voxels=8`。任何 PDB 即使候选数严格大于 1,000，也只写 `_BLOB_EXCEED` 提示标记并继续，不因候选数被排除。`test_1` 是 `test_0` 的 149-PDB 保序子集，不是第二次独立推理，也不是与 `test_0` 独立的统计样本。

## 冻结输入

### 模型与训练来源

- 训练 Job：`368455`，H100×2、64 CPU、partition=`h100`、QOS=`h100g2`、节点 `hnode01`。
- 训练 launch：`/home/penghongen/Feedback/Pocket_Plus/launches/368455/Find_1_pdb_centric_2_job368455_20260909T071535_a2`。
- checkpoint：`/home/penghongen/Feedback/Pocket_Plus/logs/AdaLigand_Stage1_pdb_centric_resume-Find_1-pdb_centric_2/Find_1-pdb_centric_2_resume____Find_1_pdb_centric_2_job368455_20260909T071535_a2_pdb_centric_2_resume/checkpoints/TOP_epoch_04_score_0.6654.ckpt`。
- checkpoint SHA-256：`3f5dd715da76a2337ddb94017f442783798566cac584b8dd16882d8afd792ee4`。
- checkpoint 身份：`epoch=4`、内部 `global_step=38623`；对应 W&B 已完成步编号为 `38622`。用户已明确要求固定使用该 checkpoint，不改用更晚权重。
- resolved config：同一训练目录下的 `config.yaml`，SHA-256 为 `9a4ea5cf95d50deb3d42c8420e4dddf1d3a7eb0ebb6d0ee4edbff0388f1884da`。
- 模型代码来源：上述训练目录保存的 `src_snapshot/src`，由正式入口以 `training_snapshot` 方式加载。

### 数据清单

| 用途 | 路径 | PDB 数 | SHA-256 |
| --- | --- | ---: | --- |
| calibration | `/storage/penghongen/AdaLigand/Ori_Data/stage1_preparation_box_pool_3/split/pdb_split/calibration.json` | 100 | `b14c9f4413092454c34f24239f0ccb1b2d506b5105fcff9dea3e6b7e87b0fe7a` |
| `test_0` | `/storage/penghongen/AdaLigand/held_out/split/held_out_06_chain/test_0.json` | 179 | `12473392629c55338460b1111d4449e7a6a692612d7eed780b1db48011acc887` |
| `test_1` | `/storage/penghongen/AdaLigand/held_out/split/held_out_06_chain/test_1.json` | 149 | `ee0697aa22aa1d1618ed42b9504e429e166c0c4fe1423db56eebf21428650df0` |

## 为什么修改 Pocket Plus

1. 现有 `stage1_v3.sh` 已能完成 probability、blobs、centered、tune 与 evaluate，但缺少一条把本任务两套固定科学流程编排起来的短命令。新增正式 shell 只组合既有官方阶段，不新增评分公式或 CLI 参数。
2. Job `368455` 提供两张 H100 和 64 个 CPU。通用 `configs/inference/stage1_v3.yaml` 需要把完整图 batch 固定为 24、centered batch 固定为 12，并按每张 GPU 约 32 个 CPU 的资源边界调整外层线程数。
3. `test_1` 不应重复执行推理。任务专用临时脚本将直接调用正式聚合函数，从 `test_0` 的逐 PDB 评估事实得到同字段契约的 149-PDB 汇总，并明确记录派生关系。长期 shell 与 Learn 测试不调用 `tmp/`；临时脚本只在 `test_0` 正式入口成功结束并重新进入 `try_lock` 后，由下一条一次性动态命令执行。
4. 服务器共享项目中的 `src/inference/evaluation.py` 早于当前 Learn 分支的 P/R 汇报契约；启动前只从完成双线等价核验的 Learn 状态安全同步，不手工复制第二套指标实现。

## 正式产物

所有正式科学产物以以下目录为根：

```text
/storage/penghongen/AdaLigand_stage1_inference/Find_1/真实受体/
└── artifacts/
    └── Find_1/
        ├── calibration/
        ├── held_out_test_0/
        ├── held_out_test_1/
        └── tuning/
```

基础打分评估名为 `f1_blobs_basic_macro_selected`；Gaussian 打分评估名为 `f2_centered_gaussian_macro_selected`。`held_out_test_1` 只发布两套评估的全局 JSON、逐 PDB JSONL 与 provenance，不复制 probability、blobs、centered 或逐 PDB evaluation NPZ。

## 正式运行命令

Job `368455` 先使用以下长期正式命令。脚本内部逐阶段调用官方 `训练与运行/sh/infer/stage1_v3.sh`，不把探测或测试命令混入正式运行。

```bash
exec bash "${TASK_PROJECT_ROOT}/训练与运行/sh/infer/find1_real_receptor_evaluation.sh"
```

上述命令成功完成两套 `test_0` 评估并重新进入 `try_lock_368455` 后，动态命令改为以下一次性正式派生命令：

```bash
exec env PYTHONPATH="${TASK_PROJECT_ROOT}" OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 /home/penghongen/anaconda3/envs/Pocket_Plus_centos7_cu121_allgpu/bin/python -u "${TASK_PROJECT_ROOT}/tmp/find1_real_receptor_evaluation_20260912/derive_test1.py"
```

## 门控与测试命令

本节只记录实现门控，不属于正式推理命令。

- 定向正式代码测试：`D:\Anaconda\envs\Pocket_Plus_windows\python.exe -m pytest -q tests/inference/test_find1_real_receptor_evaluation.py tests/inference/test_stage1_v3.py tests/inference/test_calibration_parallel.py`，结果为 47 项通过。
- 临时派生脚本测试：`D:\Anaconda\envs\Pocket_Plus_windows\python.exe -m pytest -q tmp/find1_real_receptor_evaluation_20260912/test_derive_test1.py`，结果为 1 项通过。该测试覆盖 NPZ 事实恢复、保序筛选、正式聚合函数调用及 JSON、JSONL、provenance 发布。
- 静态门控：临时脚本与测试通过 `py_compile`；正式 shell 通过 `bash -n`；YAML 的完整图 batch 24、centered batch 12、每 GPU 26 个物化线程及 calibration workers 56 均通过解析断言；`git diff --check` 通过。
- OOM 回退门控：完整图 batch 从 24 降到 18 后，同一组 47 项定向测试、正式 shell 的 `bash -n`、YAML 资源断言和 `git diff --check` 再次通过；centered batch 12、每 GPU 26 个物化线程和 calibration workers 56 均未改变。

## 关键事件

- 2026-09-12 18:07，按用户授权在确认 Job `368455`、用户、工作目录、`after_lock_368455` 和旧动态命令身份后创建 `kill_lock_368455`。runner 终止训练进程组 `353442`，第 2 次执行以退出码 137 结束，并在 `/home/penghongen/Feedback/Pocket_Plus/allocations/try_lock_368455` 创建等待锁。
- 接管后再次确认 Job 仍为 RUNNING，`after_lock_368455` 保留，`try_lock_368455` 存在，`kill_lock_368455` 已由 runner 删除；当前没有活动 attempt。旧动态命令尚未改写，因此资源不会在代码和 release 完成核验前启动推理。
- 第一轮三角色独立审查中，科学逻辑审查已批准；Git/代码布局审查要求隔离 `tmp/`，中文注释审查要求补全派生发布契约、科学变量语义和英文标点。现已将 `test_1` 拆为第二个一次性动态命令，长期 shell 与 Learn 测试不再依赖 `tmp/`，并完成对应注释与定向发布测试返工；第二轮全面审查尚待执行。
- 2026-09-12 18:44，正式发布前的服务器只读门控确认 Job 仍在 `hnode01` 运行并停在 `try_lock_368455`，`after_lock_368455` 未改变；checkpoint、resolved config、calibration、`test_0`、`test_1` 的 SHA-256 均逐项等于“冻结输入”所列值，正式输出根下没有既有文件。动态命令仍是哈希为 `141fd66cba79cf8d980fa42deedf83da51739ca34ac51fcd33f895542077b69b` 的旧训练恢复命令，尚未触发推理。
- 第二轮三角色全面审查与后续窄复核已经全部通过：Git/代码布局、科学逻辑、中文注释与 Docstring 均获批准。临时派生目录继续保持 ignored/untracked；发布前必须把它明确复制到主 Learn 工作区，并核对共享服务器与实际 release 中的脚本哈希。
- Learn/CUMULATIVE 已按“契约文档、配置与正式入口、测试、执行记录”的顺序重建至 `506f84cf1f013ee58bc70166c50f6c10bda93688`；实现端点 `41c8f4f` 与学习端点的 Git tree 均为 `ae6af1ef56f6092adeb9d30f904b236da0be44d0`。ignored 临时目录已明确复制到主 Learn 工作区后执行安全同步，共享服务器的正式评估模块、配置、入口和派生脚本分别与本地 SHA-256 `2c966c99...c72cd`、`8b5589db...55b66`、`95e4afe5...e754`、`72e0e2d6...b3984` 一致。
- 2026-09-12 18:58，动态命令已原子改写为本记录的首条正式命令，只删除 `try_lock_368455` 后启动 attempt 3。release 为 `/home/penghongen/Feedback/Pocket_Plus/releases/Pocket_Plus_0b517f1ec15f/Pocket_Plus`，launch 为 `/home/penghongen/Feedback/Pocket_Plus/launches/368455/Find_1_pdb_centric_2_job368455_20260912T185836_a3`；release 内五个关键文件与 Learn/共享服务器哈希一致，`after_lock_368455` 保留。
- 启动阶段两个 calibration probability 分片均已进入正式 CLI，绑定两张 H100 并使用 batch 24。19:00 的核验显示两个进程保持高 CPU 活动，各读取约 185–200 GB 内存，GPU 已建立上下文；尚未落盘首个 probability，未出现 traceback、CUDA OOM 或进程退出。
- attempt 3 随后在两张卡的第一次完整图前向同时发生 CUDA OOM：每张 H100 已占用约 71.72 GiB，继续申请 11.72 GiB 时只剩 7.16 GiB。该 attempt 以退出码 1 结束并重新创建 `try_lock_368455`；正式输出根仍为 0 个文件，`after_lock_368455` 未改变。按用户预授权，下一 attempt 只把完整图 batch 从 24 降为 18，centered batch、worker、科学参数和正式命令均不改变。
- OOM 回退的实现端点 `96ae78cc021aaaac1aafc280e44e3a4e21373518` 与独立学习端点 `3a546325059e8d120aa1d8ffff939a6624a0ffd3` 的 Git tree 均为 `7be35a4a2a92e0f1f5373862e3395b46691fc352`；`Learn/CUMULATIVE` 已快进到该学习端点。安全同步后，共享配置 SHA-256 为 `937aecc3b473c2caf415685583f1a6793c7bb228dd650c0844010a1178395f80`，其余正式入口、评估模块和临时派生脚本哈希均未改变。
- 2026-09-12 19:10，只删除 `try_lock_368455` 后启动 attempt 4。release 为 `/home/penghongen/Feedback/Pocket_Plus/releases/Pocket_Plus_bcb995435809/Pocket_Plus`，launch 为 `/home/penghongen/Feedback/Pocket_Plus/launches/368455/Find_1_pdb_centric_2_job368455_20260912T191102_a4`；release 中五个关键文件与 Learn/共享目录哈希一致，完整图 batch 为 18。19:20 的启动核验显示两个 probability 分片已连续运行约 9 分钟，两张 H100 显存均约 74.05 GiB、利用率 100%，进程和 `after_lock_368455` 保持正常，未出现新 OOM、traceback 或退出。首个 PDB 尚未发布，因此当前稳定性结论只覆盖长时间前向与资源活动，不等同于首个产物验收。
- 2026-09-12 20:26，batch 18 已通过首批真实产物验收：calibration 中 41 个 PDB 分别具有一份 `probability_map.npz`、`geometry.json`、`performance.json` 和 `_COMPLETE`，没有失败标记。两张 H100 仍为 100% 利用率、显存约 74.08 GiB，两个分片进程均存活；`try_lock_368455` 不存在，`after_lock_368455` 保留。
- attempt 4 随后完成 100/100 个 calibration probability，并冻结 F1 语义阈值 `0.733612060546875`、F1 basic 参数 `score_threshold=0.7737352252006531`、`prefiltered_min_voxel=8`、`min_voxels=13`，以及 F2 语义阈值 `0.431304931640625`；F1/F2 blobs 均为 100/100。首次 F2 centered 前向在两张卡上都因输入缺少 `atom_label` 抛出 `KeyError`，因此 centered NPZ 与完成标记均为 0，attempt 以退出码 1 回到 `try_lock_368455`，`after_lock_368455` 保留。

## 当前状态

正式 attempt 4 已在首次 F2 centered 前向处失败并安全停在 `try_lock_368455`。故障来自 centered 无监督请求按契约不读取真实 `atom_label`，而当前 Find 训练快照的 P anchor 注入代码需要该键来构造占位标签；该标签不参与模型特征或评分。下一步只在 centered 推理批次中补充与真实原子轴对齐的全假 bool 占位，不把测试集标签读入模型，不改变 Dataset、评分、阈值、候选或命令行契约。

## 计划与实现差异

- 当前没有科学口径差异。
- 首个正式前向已经发生双卡 CUDA OOM，因此按用户授权把完整图 batch 从 24 降为 18；centered batch 仍为 12。失败 traceback、退出码、零产物事实、release 和 launch 身份均已保留。
- Find centered 的训练快照输入还要求 `atom_label`，但无监督推理请求有意不读取真实标签。修复应由 centered 推理层构造全假占位，仅满足 P anchor mixed-layout 的张量布局与 dtype；读取真实标签会扩大 I/O 并把评估监督引入模型输入，因此不采用。
