# Find_1 真实受体训练分片推理总日志

本文汇总固定 50 片训练 PDB 推理的共享契约、各实验入口和当前状态。每次 allocation 执行只在自己的实验日志中保存命令、产物与尝试历史；本文件只提供跨实验索引和最新状态快照。

## 规划依据与覆盖范围

- 规划依据是 `talk/global/global_9.12.md` 第 (4) 项，以及用户随后对固定 50 片、真实受体、冻结参数和 allocation 接管方式的逐次确认。
- 当前已经实现或执行的范围是用户第 1—5 片。其中第 1—4 片已经完成；第 5 片是本轮唯一执行范围。
- 用户第 6—50 片仍未完成，也不属于本轮授权范围。本文不把这些分片写成已规划资源或待自动执行任务。

## 共享契约

- 训练 PDB 清单为 `/storage/penghongen/AdaLigand/Ori_Data/stage1_preparation_box_pool_3/split/pdb_split/train.json`。固定定义先以 seed `3407` 执行一次 `random.Random(3407).shuffle`，再以 `shuffled[shard_index::50]` 取得 CLI 下标 `0…49`；用户片号等于 CLI 下标加一。
- checkpoint 固定为 Job `368455` 训练产物中的 `TOP_epoch_04_score_0.6654.ckpt`，内部 `global_step=38623` 对应 W&B 已完成步编号 38,622，SHA-256 为 `3f5dd715da76a2337ddb94017f442783798566cac584b8dd16882d8afd792ee4`。
- 真实受体推理固定使用 F2 语义阈值 `0.431304931640625`，以及 `objective_beta=1` 的 Gaussian 参数：`score_threshold=0.9002149105072021`、`lambda_positive=0.064`、`lambda_negative=0.0128`、`tau_angstrom=1.25`、`prefiltered_min_voxel=8`、`min_voxels=27`。
- 每片固定执行 probability → F2 blobs → centered → Gaussian score-only。centered 前向使用 `forward_min_voxels=8` 与 `--continue-on-blob-exceed`；不得重新调参、过滤超量 PDB 或改变候选顺序、来源索引、offsets 与数组字段。
- 正式产物非破坏性续写到 `/storage/penghongen/AdaLigand_stage1_inference/Find_1/真实受体/artifacts/Find_1/train`。训练分片推理不计算测试指标，也不使用 W&B。

## 实验索引与当前状态

| 用户片号 | CLI `shard-index` | allocation | 当前状态 | 实验日志 |
| ---: | ---: | --- | --- | --- |
| 1—4 | 0—3 | Job `368455`，H100×2、64 CPU | 已完成；1,100 个 PDB 的 probability、F2 blobs、F2 centered 与 `score/selected` 已通过全量验收 | `文档/exec_plan/2026-09-13_Find_1真实受体训练分片推理.md` |
| 5 | 4 | Job `378587`，hnode02，H100×1、32 CPU | 正式 attempt a2 已启动，当前进入 probability 的 CLI `shard-index=4` 初始化；`after_lock_378587` 保留 | `文档/exec_plan/2026-09-16_Find_1真实受体训练第05片推理.md` |
| 6—50 | 5—49 | 未分配 | 未开始；不属于本轮授权范围 | 尚未创建 |

## 最新状态快照

- 2026-09-16 16:27：已删除 `try_lock_378587` 启动正式 attempt a2，launch 为 `/home/penghongen/Feedback/PocketXMol/launches/378587/train_docking_job378587_20260916T162710_a2`。主进程正在运行冻结 release 的 `find1_real_receptor_train_shard_05.sh 5`，子进程已进入 probability，参数为 `--shard-count 50 --shard-index 4`，配置为 `stage1_v3_h100_32cpu.yaml`。当前仍在模型与数据初始化，尚未形成稳定 GPU 前向；`after_lock_378587` 保留，`kill_lock_378587` 不存在，未使用 `scancel`。

## 计划与实现差异

- 有益差异：为 Job `378587` 新增单卡、32 CPU 配置，避免沿用第 1—4 片双卡 allocation 的 56 个 blobs worker；科学参数和数组契约不变。
- 中性差异：第 5 片入口按本轮授权专门命名并只接受用户片号 5，不提供其他片号的通用单卡功能。
- 有害差异：当前未发现。
- 未完成：Learn 双线整理、冻结 release、动态命令发布、正式四阶段执行和 275-PDB 全量验收尚未完成；第 6—50 片不在本轮范围。
