# unet_c1 三种采样模型推理与测试执行记录

本文记录 occurrence-centric、pdb-centric-v1 与 pdb-centric-v2 三个 `unet_c1` 模型的 Stage1 推理管线改造、正式命令、Slurm 资源、产物来源、验证证据和最终 held-out 测试结果。本文依据 `talk/global/global_8.30.md` 的“推理 GPU-CPU 并行反哺”“小改推理管线”和“三个版本 unet_c1 正式测试”范围执行；`talk/global/appendix/推理管线对话.md` 保存 Gaussian 候选缺失问题的分析背景。

本次只改动现有 Stage1 V3 推理主线、对应配置、正式 shell、测试和当前契约文档，不扩展模型训练、Stage2 或新的候选评分公式。三个 `unet_c1` 模型的正式比较固定采用 `alpha=1`、`objective_beta=1` 的 blobs+basic 流水线；Gaussian 改动只修复既有 Find 模型能力，不用于本次三模型比较。

## 当前资源与运行边界

| Job | 资源 | 当前节点与状态 | 正式职责 | 锁状态 |
| --- | --- | --- | --- | --- |
| `367332` | A800×1、24 CPU、partition=`nvlink`、QOS=`h200g4`、不限时 | `gnode09`，RUNNING | 依次运行尚未完成推理的 pdb-centric-v1 与 pdb-centric-v2 | `pre_lock_367332` 与 `after_lock_367332` 均存在，尚未启动正式命令 |
| `367411` | A100×1、16 CPU、partition=`a100`、QOS=`a100g2`、不限时 | `gnode04`，RUNNING | 只补做 occurrence-centric 的 held-out test | `pre_lock_367411` 与 `after_lock_367411` 均存在，尚未启动正式命令 |

未经用户明确要求，本任务不触碰任何 `after_lock`、`try_lock` 或 `kill_lock`，也不使用 `scancel`。pdb-centric-v2 Job `358384` 仍在自然训练；只有训练正常结束后，才从其完整 `TOP_*.ckpt` 中按文件名分数选择最高 checkpoint。

正式任务的 release 必须从完成等价核验后的 `Learn/CUMULATIVE` 冻结。实现分支只保存真实实现过程并供审查参考，不得直接同步到服务器或代替 Learn 分支启动 Job `367332`、`367411`。

## 模型、清单与目标目录

| 模型 | checkpoint 来源 | calibration 行为 | 实验根 | 正式数据划分目录 |
| --- | --- | --- | --- | --- |
| occurrence-centric | Job `346737` 的 `TOP_epoch_00_score_0.6030.ckpt` | 不重算；复用既有 F1 semantic 与 F1 basic 参数 | `/storage/penghongen/AdaLigand_stage1_inference/UNET/unet_c1-mainchain-ligand_PRAUC_0.602950` | `artifacts/unet_c1/held_out_test_0` |
| pdb-centric-v1 | Job `356953` 的 `TOP_epoch_01_score_0.4918.ckpt` | 独立完成 100 个 calibration PDB 的 probability、F1 semantic、F1 blobs 与 F1 basic | `/storage/penghongen/AdaLigand_stage1_inference/UNET/unet_c1_pdb_centric_v1` | `artifacts/unet_c1/calibration` 与 `artifacts/unet_c1/held_out_test_0` |
| pdb-centric-v2 | Job `358384` 自然结束后的最高完整 TOP checkpoint；截至 2026-09-03 第一轮审查时为 `TOP_epoch_03_score_0.5932.ckpt`，尚未冻结 | 独立完成 100 个 calibration PDB 的 probability、F1 semantic、F1 blobs 与 F1 basic | `/storage/penghongen/AdaLigand_stage1_inference/UNET/unet_c1_pdb_centric_v2` | `artifacts/unet_c1/calibration` 与 `artifacts/unet_c1/held_out_test_0` |

每个模型的调参文件位于相应实验根的 `artifacts/unet_c1/tuning/`。表中相对目录均以“实验根”为起点；例如 occurrence held-out 的绝对目录是 `/storage/penghongen/AdaLigand_stage1_inference/UNET/unet_c1-mainchain-ligand_PRAUC_0.602950/artifacts/unet_c1/held_out_test_0`。

calibration 清单为 `/storage/penghongen/AdaLigand/Ori_Data/stage1_preparation_box_pool_3/split/pdb_split/calibration.json`，共 100 个 PDB。held-out 测试清单为 `/storage/penghongen/AdaLigand/held_out/split/held_out_06_chain/test_0.json`，其顶层对象通过 `pdb_ids` 字段提供 179 个 PDB。

occurrence-centric 复用参数为：F1 semantic threshold=`0.607086181640625`；F1 basic `score_threshold=0.8210563659667969`、`prefiltered_min_voxel=8`、`min_voxels=16`。本次只在 held-out test 生成 probability、F1 blobs 并固定评估，不改写 calibration probability、F1 semantic、F1 blobs 或 F1 basic 文件。

## 为什么修改代码

1. 现有 CLI 只把顶层 JSON 列表解释成 PDB 标识；held-out `test_0.json` 的顶层是包含 `pdb_ids` 的对象，因此正式入口必须读取这两种已知清单结构。
2. 现有完整图推理为每个 PDB 重建 CPU 物化线程池，也不在当前 PDB 收尾时准备下一 PDB 的几何与首个 batch；本次把 AUTO 仓库已经验证的跨 PDB 线程复用和首批预取移入正式主线，但不引入实验专用产物字段。
3. 现有 centered 只让 `fits_centered_box=true` 的 blob 前向，导致无法被单个 80³ BOX 完整包络的大 blob 从 Gaussian 候选集合消失；本次让它们与普通 blob 一样执行合法 80³ 居中裁块前向，并在 centered NPZ 明确保存完整 blob 是否可包络以及完整体素数。框外来源体素不伪造概率或特征，候选选择和评估继续使用完整 blobs 事实。
4. 现有 Gaussian 两个 lambda 网格不含 0，且分数阈值采用固定粗网格和乘数细化；本次让两个 lambda 都包含 0，并对每个 tau/lambda 组合扫描实际 float32 候选分数断点，使 basic 成为可达端点且阈值选择与 basic 同口径。
5. 现有评估只有 semantic PRAUC；本次对 coverage 和 one-to-one 在 0.3、0.5、0.6 三个双向覆盖阈值下增加 micro 与 PDB 等权 macro PRAUC。PRAUC 固定使用已冻结的两个体素数门槛，但忽略最终 `score_threshold`，按实际 float32 候选分数降序并合并相同分数。

## 正式运行命令

正式 release 中只记录并执行以下短命令。脚本内部使用正式 `训练与运行/sh/infer/stage1_v3.sh` 入口完成固定流水线；正式命令不混入探测、smoke、语法检查或 pytest。

```bash
exec bash "${TASK_PROJECT_ROOT}/训练与运行/sh/infer/unet_c1_sampling_comparison.sh" occurrence
```

```bash
exec bash "${TASK_PROJECT_ROOT}/训练与运行/sh/infer/unet_c1_sampling_comparison.sh" pdb_centric_1
```

```bash
exec bash "${TASK_PROJECT_ROOT}/训练与运行/sh/infer/unet_c1_sampling_comparison.sh" pdb_centric_2
```

## 门控与测试命令

下列命令只验证代码、配置、shell 或最小运行边界，不属于正式推理命令。

基线门控于实现前在提交 `59c5ffe7349f6809b33572e4d7ad095bf27a7303` 执行：

```powershell
& 'D:\Anaconda\envs\Pocket_Plus_windows\python.exe' -m pytest -q tests/inference/test_stage1_v3.py tests/inference/test_calibration_parallel.py
```

结果为 `38 passed in 3.68s`。

第一轮审查修正后的当前门控命令为：

```powershell
& 'D:\Anaconda\envs\Pocket_Plus_windows\python.exe' -m pytest tests/inference/test_stage1_v3.py tests/inference/test_calibration_parallel.py -q
& 'D:\Anaconda\envs\Pocket_Plus_windows\python.exe' -m py_compile src/inference/full_map.py src/inference/centered.py src/inference/calibration.py src/inference/evaluation.py src/inference/pipeline.py src/inference/cli.py
& 'D:\Anaconda\envs\Pocket_Plus_windows\python.exe' -c "from omegaconf import OmegaConf; [OmegaConf.load(p) for p in ['configs/inference/stage1_v3.yaml','configs/inference/stage1_v3_a800_24cpu.yaml','configs/inference/stage1_v3_a100_16cpu.yaml']]"
& 'C:\Program Files\Git\bin\bash.exe' -n '训练与运行/sh/infer/stage1_v3.sh'
& 'C:\Program Files\Git\bin\bash.exe' -n '训练与运行/sh/infer/unet_c1_sampling_comparison.sh'
git diff --check
git diff HEAD -- '*.py' | Select-String -Pattern '^\+[^+].*[，。；：！？（）、]'
```

测试结果为 `45 passed in 5.03s`；`py_compile`、三份 YAML 加载、两份 Bash 语法与 `git diff --check` 均通过；最后一条 Python 新增行标点检查没有输出。

## 当前进度

- 2026-09-02 20:39，提交 A100 Job `367411`；调度事实核验为 A100×1、16 CPU、`TimeLimit=UNLIMITED`、QOS=`a100g2`，随后停在 `pre_lock_367411`。
- 2026-09-02 20:49，确认 A800 Job `367332` 与 A100 Job `367411` 均保有 pre/after 两把锁；pdb-centric-v2 Job `358384` 仍在训练，不提前选用 checkpoint。
- Pocket Plus 与 AdaLigand 均处于干净的 `Learn/CUMULATIVE`，且该提交分别是各仓库按提交者时间的唯一最新提交。Pocket Plus 实现从 `59c5ffe7349f6809b33572e4d7ad095bf27a7303` 建立隔离分支 `codex/stage1-inference-unet-c1-comparison`。
- 用户补充双线提交约束：正式任务只能从本轮学习历史端点启动。当前实现分支完成全部门控、自查与独立审查后，先重建学习分支并核验与实现端点等价，再从 `Learn/CUMULATIVE` 生成 release；在此之前两个 `pre_lock` 保持不动。
- 推理主线已经完成 JSON 对象清单解析、跨 PDB 完整图 session、centered 大 blob 前向、Gaussian 实际分数扫描和 coverage/one-to-one PRAUC 实现；正式 comparison shell 只编排 F1 blobs+basic，不把 Gaussian 或 centered 混入三模型比较。
- 正式资源配置已固定：A800/24 CPU 使用完整图 batch 32、18 个窗口物化线程和 18 个预取批次；A100/16 CPU 使用 batch 16、10 个窗口物化线程和 10 个预取批次。完整图物化以批次为线程任务，因此预取上限必须与物化线程数相同，才能实际形成配置声明的并发；两个配置仍为融合、发布和主线程预留约 6 个 CPU。blobs 与 calibration 阶段分别使用全部 24/16 个 CPU 执行各自的外层并发。
- 第一轮三路独立审查确认并发、超大 blob、三模型职责和正式路径主链正确，同时发现候选 PRAUC 曾从评估 NPZ 的 float32 阈值回读 0.3/0.6，使恰好 `3/10` 或 `3/5` 的包含端点候选被错误排除。修正后，PRAUC 直接使用 `aggregate_stage1_metrics()` 收到的原始 Python 浮点阈值；逐 PDB NPZ 的既有 float32 字段契约不变。两组边界均已加入回归测试。
- pdb-centric-v2 的 Job `358384` 仍在训练，当前最高 TOP 文件不视为最终 checkpoint。正式启动 v2 前必须等待该 Job 正常结束，再把最终最高完整 checkpoint 写成显式固定路径；不得在同一输出根用动态选择混合不同权重。
- 第二轮三路全面审查与随后逐项窄复核均已通过。代码布局审查批准两个异步数据类、`FullMapInferenceSession`、候选 PRAUC 函数和正式入口的现有位置与拆分；注释审查批准本轮新增函数、类和阈值回归；逻辑审查批准 v2 checkpoint 外的全部正式链路。
- 当前门控结果：两份推理测试为 `45 passed in 5.76s`；六个修改 Python 模块通过 `py_compile`；三份 Stage1 V3 YAML 均可由 OmegaConf 解析；`stage1_v3.sh` 与 `unet_c1_sampling_comparison.sh` 均通过 Bash `-n`；`git diff --check` 通过；新增 Python 行未出现中文全角代码标点。

## 计划与实现差异

- 有益差异：资源核对发现 full-map 物化以 batch 为线程任务，原先预取上限 4 无法让 A800/A100 配置的 18/10 个物化线程形成对应并发；现已分别改为 18/10，并以资源配置测试锁定。第一轮独立审查另发现 PRAUC 的 float32 阈值往返问题，现已修复并新增两个包含端点回归。
- 中性差异：pdb-centric-v2 的训练尚未自然结束，因此本轮先完成 occurrence 与 pdb-centric-v1 的双线、发布和正式运行；v2 保持空目标目录，待训练结束后只冻结最终 checkpoint 并执行已定义流水线。
- 有害差异：当前未发现。
- 未完成范围：Git 双线端点与等价核验、服务器 release/launch、occurrence 与 pdb-centric-v1 正式结果，以及训练结束后的 pdb-centric-v2 checkpoint 固定、正式运行和三模型结果汇总。
