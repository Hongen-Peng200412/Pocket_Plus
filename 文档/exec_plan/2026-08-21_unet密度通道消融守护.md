# U-Net 密度通道消融运行与守护记录

日期：2026-08-21

本记录保存 `unet_base` 与 `unet_diff` 两项 AdaLigand Stage1 消融训练的代码修复原因、提交契约、验证证据和关键运行事件。两项训练均已按配置中的三次实际学习率衰减停止条件正常结束，checkpoint 与 W&B 摘要均已核验。Jobs `350302`、`350305` 的 Slurm allocation 均已在训练产物保存后由用户释放，活动锁已经清理。

## 实验意图

两项实验使用相同的 density-only RAUNet64、训练制度和五项体素损失，仅改变输入密度通道：

| 实验 | 输入密度通道 | 通道数 |
| --- | --- | ---: |
| `unet_base` | `ALL_CHANNEL_NAMES` 的权威完整顺序 | 56 |
| `unet_diff` | `exp_clipnorm_nopost`、`diff_clipnorm_nopost` | 2 |

共同训练参数为：每张 GPU 的批量为 8，全局批量为 48，梯度累积为 6，`DataLoader` worker 数为 16，优化器最大学习率为 `1.0e-4`。共同损失权重为配体体素主损失 `1.0`、配体区域辅助损失 `0.1`、配体反距离损失 `0.3`、蛋白主链损失 `0.05`、核酸主链损失 `0.05`。

## 为什么必须修改代码

修复前的 `src/datasets/stage1_dataset.py` 把三类运行行为错误地绑定到 `stage1_model_name` 的固定名称：

1. Dataset 只接受 `STAGE1_MODEL_NAMES` 中已经登记的名称，因此新的合法消融名称会在读取样本前被拒绝。
2. 只有名称以 `Find` 开头时才读取模拟密度整图；`unet_base` 的 `sim/diff/posdiff` 通道和 `unet_diff` 的 `diff` 通道因此缺少必需的模拟密度输入。
3. 只有固定辅助监督名称集合中的实验才加载并返回蛋白主链、核酸主链和配体反距离目标；两个新实验即使启用了结构输出头和全部损失，也得不到对应目标。

这些限制不是数据或模型的真实契约，而是实验名称与 Dataset 行为之间的人为耦合。若只删除报错检查而不修复后两项运行行为，训练仍会在构造密度通道或计算辅助损失时失败。

本次采用最小端到端修复：删除 producer 名称白名单和 `unet_c1` 单通道硬限制；根据 `density_channel_config.enabled_channels` 中是否存在 `sim_`、`diff_` 或 `posdiff_` 前缀决定是否读取模拟密度；将 `unet_base`、`unet_diff` 接入现有辅助监督目标集合。Find 的 56 通道契约、Find 原子表输出、模型结构、损失权重、采样逻辑和训练制度均未改变。

## 验证证据

- 本地 `py_compile` 与两个训练脚本的 `bash -n` 均通过。
- 本地新增参数化测试 4 项通过；`tests/datasets/test_stage1_dataset.py` 全部 14 项通过。
- 服务器同步后，同一组新增参数化测试 4 项通过。
- 使用服务器正式 A–G 数据的验证请求索引 0 读取样本：`unet_diff` 得到 `(2, 80, 80, 80)`，`unet_base` 得到 `(56, 80, 80, 80)`；两者均确认读取模拟密度、不返回 Find 原子表，并具有 `voxel_label`、`protein_mainchain_target`、`nucleic_mainchain_target` 和 `ligand_inverse_distance_target`。
- 本地相关测试文件的 42 项组合运行中有 41 项通过；唯一失败是既有测试仍要求旧 `Find_1.sh` 固定写入 `train.num_workers=16`，不涉及本次两个 U-Net 脚本或 Dataset 修改。
- 安全同步后，9 个本次代码、配置、测试和 shell 文件的本地与服务器 SHA-256 完全一致。

## 提交契约

2026-08-21 已从 `/home/penghongen/My_Project/Pocket_Plus` 提交：

| Job | 任务 | Slurm partition | QOS | GPU | CPU | 首次执行前锁 | 执行后保留 |
| ---: | --- | --- | --- | --- | ---: | --- | --- |
| `350302` | `unet_base.sh` | `nvlink` | `nvlinkg8` | A800 × 1 | 16 | 关闭，`pre_hold=0` | 开启，`after_hold=1` |
| `350305` | `unet_diff.sh` | `nvlink` | `h200g2` | A800 × 1 | 16 | 关闭，`pre_hold=0` | 开启，`after_hold=1` |

提交后 `scontrol show job -dd` 确认两项任务的 `TRES` 均为 `cpu=16,gres/gpu:a800=1`，动态命令均包含 `--pre_hold 0 --after_hold 1`。两项任务最初状态均为 `PENDING (Priority)`；排队期间尚不会创建 release 或 launch。

## 守护规则

- 获得资源后核验 release、launch、最终 `config.yaml`、训练日志、GPU 活动和正常 step。
- 排队期间或训练完全稳定后不停止任务、不创建 heartbeat；每轮静默等待 60 或 120 分钟，等待由连续的 300 秒睡眠片段组成，以便用户消息最多等待约 5 分钟即可被处理。
- 训练启动前后、疑似出现代码错误或训练停止等重要事件窗口，可以连续检查状态，直到事件性质明确。
- 只在任务开始、配置核验完成、稳定训练、失败、进入 `try_lock`、训练结束等关键事件更新本记录，不记录固定间隔流水账。
- 未经用户明确要求，不删除 `after_lock_<job-id>`、`try_lock_<job-id>`，不创建或触碰 `kill_lock_<job-id>`，也不用 `scancel` 代替项目锁协议。

## 关键事件

- 2026-08-21：完成代码修复、本地与服务器测试、正式数据样本验证和安全同步。
- 2026-08-21：Job `350302` 与 `350305` 提交成功并完成 Slurm 资源与锁参数核验；两项任务均因 `Priority` 排队。
- 2026-08-21 08:40–09:55：Job `350302` 在 `gnode09` 启动，launch 为 `unet_base_job350302_20260821T084104_a1`，release 为 `Pocket_Plus_ea0aca3a357c`。最终 `config.yaml` 确认 56 个密度通道、全局批量 48、每卡批量 8、16 个 worker、学习率 `1.0e-4`、结构输出头开启，五项损失权重为 `1.0/0.1/0.3/0.05/0.05`。W&B 本地摘要达到 `trainer/global_step=203`，总损失约 `0.2310`，各辅助损失为有限数值；A800 显存使用约 78.9 GiB、GPU 利用率 94%，没有匹配到 traceback、error、exception、OOM 或 NaN。判定 `unet_base` 已稳定进入正常训练。Job `350305` 仍因 `Resources` 等待。
- 2026-08-21 15:42：Job `350302` 已运行约 6 小时 59 分钟并达到 `trainer/global_step=1154`，训练总损失约 `0.1692`。首次验证已完成：验证总损失约 `0.2399`、配体体素 PRAUC 约 `0.5019`，五项训练与验证损失均为有限数值；判定训练持续稳定。Job `350305` 仍因 `Resources` 等待，尚未创建 release 或 launch。
- 2026-08-22 17:15–17:36：Job `350305` 在 `gnode10` 获得 A800 并启动，launch 为 `unet_diff_job350305_20260822T171529_a1`，release 为 `Pocket_Plus_fdb8a30fa196`。最终 `config.yaml` 确认输入为 `exp_clipnorm_nopost`、`diff_clipnorm_nopost` 两个密度通道，全局批量 48、每卡批量 8、梯度累积 6、16 个 worker、学习率 `1.0e-4`、结构输出头开启，五项损失权重为配体体素 `1.0`、配体区域辅助 `0.1`、配体反距离 `0.3`、蛋白主链 `0.05`、核酸主链 `0.05`。日志确认原始 `voxel_grid` 与体素骨干输入均为 2 通道；W&B 本地摘要已达到 `trainer/global_step=5`，训练总损失约 `0.3799`，五项损失均为有限数值。A800 显存使用约 79.4 GiB，任务仍在运行且未出现 traceback、OOM 或 NaN；继续观察 step 推进和 GPU 活动后再判定完全稳定。
- 2026-08-22 17:44–17:50：Job `350305` 连续从 `trainer/global_step=5` 推进到 23、38，最新训练总损失约 `0.3522`，其余四项辅助损失和配体体素主损失均保持有限值。A800 显存约 79.4 GiB，两次 GPU 利用率抽样分别为 57% 和 73%；标准输出、标准错误没有匹配到 traceback、error、exception、OOM、NaN 或 killed。allocation 目录只有预期的 `after_lock_350305`，没有 `try_lock`。判定 `unet_diff` 已稳定进入正常训练，两项任务从此共同采用 60 或 120 分钟静默守护周期。
- 2026-08-23 02:20：Job `350305` 已达到 `trainer/global_step=1175` 并完成首次验证。验证总损失约 `0.2976`，配体体素 PRAUC 约 `0.2980`，受体 PRAUC 约 `0.2707`；五项训练与验证损失均为有限值，任务随后继续训练。Job `350302` 同时稳定推进到 `trainer/global_step=6998`。两项 allocation 均只有预期的 `after_lock`，没有错误、OOM、NaN 或 `try_lock`。
- 2026-08-31 12:10：Job `350302` 检测到实际学习率衰减 3 次，达到 `stop_after_lr_reductions=3`，随后正常保存结果并结束 `unet_base` 正式训练。最终 W&B 摘要为 `trainer/global_step=39508`，验证总损失约 `0.1334`、受体 PRAUC 约 `0.7658`、配体体素 PRAUC 约 `0.6794`；蛋白主链与核酸主链 PRAUC 分别约 `0.9923`、`0.9923`，全部损失均为有限值。`last.ckpt` 与 `TOP_epoch_00_score_0.6794.ckpt` 于 12:08 写入，现有最高分 TOP checkpoint 为 `TOP_epoch_00_score_0.6809.ckpt`；标准输出明确记录“第 1 次执行成功”，且标准输出和标准错误没有匹配到 traceback、error、exception、OOM、NaN 或 killed。调度包装器已创建 `/home/penghongen/Feedback/Pocket_Plus/allocations/try_lock_350302`；Job `350302` 的 Slurm 状态仍为 `RUNNING`，原因是 `/home/penghongen/Feedback/Pocket_Plus/allocations/350302/after_lock_350302` 正在按提交契约保留资源。两个锁均未被触碰。同期 Job `350305` 继续训练并达到 `trainer/global_step=30827`，没有错误或 `try_lock`。
- 2026-09-01 07:33：Slurm 将 Job `350302` 记录为 `CANCELLED by 1351`，结束时间为 07:33:50；该操作发生在 `unet_base` 已正常保存结果并结束训练之后，因此只终止了由 `after_hold` 保留的 allocation，没有改变最终 W&B 摘要或 checkpoint。调度包装器随后记录“Job 350302 已退出并清理活动锁与动态命令”，`try_lock_350302` 与 `after_lock_350302` 均已不存在，A800 资源已经释放。守护过程未执行 `scancel`，也未删除这两个锁。同期 Job `350305` 仍在 `gnode10` 运行，GPU 利用率 99%、显存约 80.3 GiB，没有错误或 `try_lock`。
- 2026-09-03 14:47：用户再次确认 Job `350305` 是 occurrence-centric `unet_diff` 密度通道消融，并将其纳入当前全局训练守护。Job 仍在 `gnode10` 使用 A800×1、CPU×16；W&B run `nf93buae` 已推进到 `trainer/global_step=41018`，学习率约 `4e-6`、训练总损失约 `0.1938`。最近一次完整 validation 的总损失为 `0.1653458`、配体体素 PRAUC 为 `0.6288677`、受体 PRAUC 为 `0.7104262`；`TOP_epoch_00_score_0.6289.ckpt` 与 `last.ckpt` 已于 11:59 写出，均为 499,688,614 bytes。Job 仍在运行且只有预期的 `after_lock_350305`，没有 `try_lock` 或 `kill_lock`；本次只读核验没有操作资源。
- 2026-09-03 20:48—21:52：Job `350305` 完成下一次完整 validation，共处理 827 个 validation batch；W&B 摘要更新为验证总损失 `0.1614618599`、配体体素 PRAUC `0.6319023967`、受体 PRAUC `0.7162516713`。配体体素 PRAUC 相比上一轮提高 `0.0030347109`，超过调度器的绝对改进阈值 `0.003`。`last.ckpt` 中的 `WarmupPlateauController` 已把 `best` 更新为 `0.6319023967` 并把 `num_bad_epochs` 重置为 0；学习率保持 `4.000000000000011e-06`，`LearningRateReductionStopper.lr_reduction_count` 保持 2，没有发生额外学习率衰减。新 `TOP_epoch_00_score_0.6319.ckpt` 与 `last.ckpt` 均为 499,688,614 bytes，SHA-256 均为 `269d9c2be071d73191b8a2751cb53d9128bcb789cf4e980aa86fa47f88dead89`。`BEST.ckpt` 仍与上一轮 `TOP_epoch_00_score_0.6289.ckpt` 字节一致，SHA-256 均为 `ce837c1e728cffe1a99b04848736d02b64e82ecf461fada09c0fc03bc5570f81`；这是既有的一次 validation 发布滞后，新 checkpoint 内的 `ModelCheckpoint.best_model_path` 已正确指向 `TOP_epoch_00_score_0.6319.ckpt`。21:52 时 W&B 已继续推进到 `global_step=41915`，Job 保持 `RUNNING`，只有 `after_lock_350305`，没有操作训练进程、锁或资源。
- 2026-09-04 04:59—08:35：Job `350305` 又完成一轮 827-batch validation。验证总损失为 `0.1617104411`、配体体素 PRAUC 为 `0.6343953609`、受体 PRAUC 为 `0.7158595920`。原始 PRAUC 创新高，但相对 plateau best `0.6319023967` 只提高 `0.0024929643`，没有超过绝对阈值 `0.003`；因此 checkpoint 中 plateau best 保持不变、`num_bad_epochs=1`，学习率保持 `4.000000000000011e-06`，实际衰减计数保持 2。ModelCheckpoint 独立按原始分数保存 `TOP_epoch_00_score_0.6344.ckpt`；它与 `last.ckpt` 均为 499,688,614 bytes，SHA-256 均为 `8777e7a58ffdb6f17914329a3a2bbdc1bb478b345b6da8b570f50108c9c51409`。磁盘 `BEST.ckpt` 已追上上一轮 `TOP_epoch_00_score_0.6319.ckpt`，SHA-256 为 `269d9c2be071d73191b8a2751cb53d9128bcb789cf4e980aa86fa47f88dead89`。08:35 时 W&B 已继续到 `global_step=43493`，Job 仍为 `RUNNING` 且只有 `after_lock_350305`；本轮只读核验没有操作训练进程、锁或资源。
- 2026-09-04 12:50—13:24：Job `350305` 完成下一轮 827-batch validation。验证总损失为 `0.1607980728`、配体体素 PRAUC 为 `0.6314629316`、受体 PRAUC 为 `0.7157904506`。该分数低于 plateau best `0.6319023967`，因此 `num_bad_epochs` 按预期从 1 增至 2；学习率继续为 `4.000000000000011e-06`，实际学习率衰减计数仍为 2，没有提前停训。`TOP_epoch_00_score_0.6315.ckpt` 与 `last.ckpt` 均为 499,688,614 bytes，SHA-256 均为 `7015034771568afd8d63db6028d27f41f2b2f9bfbe4e3666f52867b14c7cc1c3`。ModelCheckpoint 的原始最佳仍为 `TOP_epoch_00_score_0.6344.ckpt`；磁盘 `BEST.ckpt` 已追上它，SHA-256 `8777e7a58ffdb6f17914329a3a2bbdc1bb478b345b6da8b570f50108c9c51409`。13:24 时训练已继续到 `global_step=44087`，Job 仍为 `RUNNING` 且只有 `after_lock_350305`。下一次未达到 `best+0.003` 的 validation 将满足 `patience=3` 的连续未改进条件，届时需要核对第三次实际学习率衰减和配置化停止行为。
- 2026-09-05 05:00—05:01：Job `350305` 完成最终的 827-batch validation，配体体素 PRAUC 为 `0.6330414414`、受体 PRAUC 为 `0.7154738903`、验证总损失为 `0.1607799530`。该分数没有超过 plateau best `0.6319023967` 加绝对阈值 `0.003`，`ReduceLROnPlateau` 因而执行第 3 次实际衰减；最终 checkpoint 的优化器学习率为 `8.000000000000022e-07`，`LearningRateReductionStopper.lr_reduction_count=3`。标准输出随后明确记录达到 `stop_after_lr_reductions=3`、正式训练完成、总时长 `1,165,496.32` 秒和“第 1 次执行成功”。调度包装器于 05:01:25 创建根级 `try_lock_350305`，allocation 内的 `after_lock_350305` 保持存在。没有释放、重启或改写任何训练产物。
- 2026-09-05 10:14—10:15：用户在完成训练产物验收后释放 Job `350305`。Slurm 记录主 Job 于 10:14:17 结束，调度包装器随后记录“Job 350305 已退出并清理活动锁与动态命令”；10:26 复核确认 `try_lock_350305`、`after_lock_350305` 与动态命令均已清理。训练产物根、W&B 文件与 checkpoint 保持不变。

## Job 350305 可追溯身份

- release：`/home/penghongen/Feedback/Pocket_Plus/releases/Pocket_Plus_fdb8a30fa196/Pocket_Plus`。
- launch：`/home/penghongen/Feedback/Pocket_Plus/launches/350305/unet_diff_job350305_20260822T171529_a1`。
- 正式运行与全部实验产物根：`/home/penghongen/Feedback/Pocket_Plus/logs/AdaLigand_Stage1-unet_diff/unet_diff____unet_diff_job350305_20260822T171529_a1_formal`。
- W&B：`pencounkdual-111/AdaLigand_Stage1/nf93buae`。
- checkpoint 目录：`/home/penghongen/Feedback/Pocket_Plus/logs/AdaLigand_Stage1-unet_diff/unet_diff____unet_diff_job350305_20260822T171529_a1_formal/checkpoints`。
- immutable `launch.json` SHA-256：`f5852fe2c7ec78da3f03250c2d7c47879b5708abac5f5c89fca284a4713611f0`。
- immutable `run_cmd.sh` 与 allocation 当前 `run_cmd_350305.sh` SHA-256：`29c6433271a5f46c8403d5d2a9b9d7de6c3f1ce460b12a9e4c4714b738cedfc5`。
- 运行目录 `config.yaml`、`train.yaml`、`src_snapshot/manifest.json` SHA-256：`b0c162c246980f3d26c7fd76cabdc8222edef2cd5bf303b23cce13c90afd24c3`、`0193469232fbf453c7cd827c7227419b32b2a559687a9bc2121e0fb2fb5b3bb5`、`f47a4c6ce0ab31311d0a0c26584ace79b4b5a28e552bc0b66ba4d8adb0487123`。
- 实际训练命令由 immutable `run_cmd.sh` 冻结为 `exec bash "${TASK_PROJECT_ROOT}"/训练与运行/sh/unet_diff.sh`。
- 2026-09-03 21:50 的 milestone 核验在 `gnode10` 只读执行；没有修改 checkpoint，也没有删除 `after_lock_350305`。
- 2026-09-04 08:29 的最新 checkpoint 核验继续借用 Job `350305` allocation 内 2 个 CPU 只读执行；没有修改任何正式产物或控制锁。
- 2026-09-04 13:24 的下一轮 checkpoint 核验同样只读执行；没有修改训练、命令、锁或资源。
- W&B 本地与同步产物目录：`/home/penghongen/Feedback/Pocket_Plus/logs/AdaLigand_Stage1-unet_diff/unet_diff____unet_diff_job350305_20260822T171529_a1_formal/wandb/run-20260822_172939-nf93buae`；其中 `files/` 保存最终摘要与运行元数据，`logs/` 保存 W&B 运行日志。
- 冻结配置与代码身份位于正式运行根的 `config.yaml`、`train.yaml` 和 `src_snapshot/`；配置文件与 `src_snapshot/manifest.json` 的 SHA-256 分别为 `b0c162c246980f3d26c7fd76cabdc8222edef2cd5bf303b23cce13c90afd24c3`、`0193469232fbf453c7cd827c7227419b32b2a559687a9bc2121e0fb2fb5b3bb5`、`f47a4c6ce0ab31311d0a0c26584ace79b4b5a28e552bc0b66ba4d8adb0487123`。

## Job 350305 最终验收

- 最终 `last.ckpt` 为 epoch 1、`global_step=46282`；最终 validation 的 current/total 进度均为 827，且 `is_last_batch=true`。W&B 摘要中的 `trainer/global_step=46281` 是 callback 写摘要时早一个 step 的记录，不代表 checkpoint 缺失。
- Plateau 最终状态为 `best=0.6319023966789246`、`num_bad_epochs=0`、`validation_index=38`、`last_stepped_validation=[46282,1,37]`；第 3 次衰减后优化器学习率为 `8.000000000000022e-07`。W&B 的 `warmup_lr=4.000000000000011e-06` 是衰减前已经写入的摘要值，checkpoint 是恢复与续用时的权威状态。
- `last.ckpt` 与 `TOP_epoch_01_score_0.6330.ckpt` 均为 499,688,614 bytes，SHA-256 均为 `48ad3b374c32e34edad21a7e9127cf875a5bd42c21327821f84e2b4d45c7dbfd`。
- 全程最高原始配体体素 PRAUC 为 `0.6343953609466553`。`BEST.ckpt` 与 `TOP_epoch_00_score_0.6344.ckpt` 均为 499,688,614 bytes，SHA-256 均为 `8777e7a58ffdb6f17914329a3a2bbdc1bb478b345b6da8b570f50108c9c51409`。
- Slurm 主 Job 继续显示 `RUNNING`，仅因为 `after_hold` 保留 allocation。`sacct` 中 `350305.0` 的 `FAILED 6:0` 是 2026-08-22 17:29:38 开始、仅持续 5 秒的早期步骤；最终正式训练由包装器明确判定为第一次执行成功，不能用该历史步骤覆盖最终训练结论。
- Job `350305` 释放后，`/home/penghongen/Feedback/Pocket_Plus/allocations/try_lock_350305` 与 `/home/penghongen/Feedback/Pocket_Plus/allocations/350305/after_lock_350305` 均已不存在。该 Job 不再占用 gnode10 的 A800 或 CPU。
